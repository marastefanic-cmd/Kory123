# DIARY — how the Arcane Burn Planner evolved (and where it was wrong)

**This file is append-only history. Do NOT prune it.** The living docs (`RULES.md`, `ARCHITECTURE.md`,
`MECHANICS.md`, `EP.md`) are kept current-to-reality and *are* pruned; the **phase docs** (`PHASE*.md`)
and this diary are the project's **memory of the road taken** — including turns that were later reversed.
A future agent should be able to read this and know *what we tried, when, why we thought it right at the
time, and what disproved it* — so settled mistakes are not re-litigated from scratch.

Dates are commit dates (`git log --date=short`). This is a curated narrative, not the raw log — regenerate
the raw arc anytime with `git log --reverse --pretty=format:'%ad %s' --date=short`.

> **On the open items below:** a documented gap here (e.g. the low-haste basin) is **not** an accepted
> state — it is a *recorded debt to be fixed in a later phase*. See `docs/ACCEPTANCE.md` for the standing
> test the model must pass fully before it is called complete.

---

## The arc, by phase

> **The very beginning** — the first idea, the first working tool (commit `7f6713c`), and the day-1→3
> iteration before any plan doc existed — plus a **findings / living-doc evolution timeline**, is
> reconstructed in `docs/archive/00-genesis.md`. This section is the high-level arc; read 00-genesis for
> the origin narrative.

### Phase 1 — the planner exists (2026-07-19)
Built the single-file `index.html`: enter a fight (length, Lust, phases), get the optimal press time for
each on-use cooldown + a burn timeline + copy-as-text plan. Boss presets, a fight-phase editor, pinnable
buffs, real spell icons. Heavy early iteration on **tie-breaks** (how to rank equal-score plans: anchor
burst early, prefer overlaid/grouped presses, snap clusters). Adversarial review pass. The objective was
surfaced as **"effective casts."**

### Phase 2 — a real scoring model + sim validation (2026-07-20)
Threw out ad-hoc scoring and rebuilt it as a **continuous cast-rate integral** (`simulate()`), the model
that still stands. Validated end-to-end against a **headless wowsims build**. Added the **exact-match
regression suite** (`tests/`, deterministic — one setup ⇒ one plan). Redesigned the burn timeline.

### Phase 3 — theorycraft docs + placement + raid buffs (2026-07-21 → 07-22)
Created the `docs/` set (CLAUDE.md, MECHANICS, SOURCES, RULES). Landed the **buff-into-Lust sequential
packing** search fix and the **containment-overlap** placement rule. Ran a **rigorous wowsims harness
audit** (found the APLActionSchedule drop bug; flagged then resolved the AP-cooldown discrepancy).
Modelled **AoE crit-proc amplification** (Clearcasting → Arcane Potency). Built the **EP stat-weight**
calculator two ways (closed-form model partials + wowsims finite-diff). P3 proper: raid-buff/proc
tightening (Drums, Power Infusion), Ashtongue, per-window mana chip.

### Phase 4 — the ramp, exactly + search robustness (2026-07-22 → 07-23)
Replaced the approximate ramp with an **exact discrete stack ramp**, and made the search **basin-stable**
(hop-normalize fixpoint, cd-tick anchors). **Certified haste-monotonicity: 0 violations, 25/25 goldens.**

### Phase 4.5 — brute-force certification + the haste ladders (2026-07-23)
Built `brute-grid.mjs` (full 5s-grid exhaustive) and `haste-ladder.mjs` (brute marched across gear haste
with breakpoint bisection). Certified the search against exhaustive enumeration across **all six trinket
pairs and the full haste range** (~260 rungs, zero misses). Extracted the **haste-morphology band map**
(RULES §16) and a six-kit "law set," **adversarially corrected** (two drafted laws refuted/fixed).

### Phase 5 — AoE phases cracked (2026-07-23)
Verdict: an AoE phase is a **burn ×M(N) modifier + exit-re-ramp + SP-dilution** (RULES §9), thresholds
sim-gated. COMPLETE.

### Phase 5.5 — the app got heavy; make it fast + honest (2026-07-23)
The single-file app was crashing the browser on long fights. Moved the engine into a **Blob Web Worker**,
then a **worker pool** (byte-identical plans), added a **polish cache** + **simulate() memo** (KT 285s →
84s), an **honest labeled progress bar**, and switched displayed times to **actual fire times**.

### Phase 6 — haste-adaptation cross-validation (2026-07-23 → in flight)
The question: does the planner's *re-optimization as gear-haste changes* hold up in the real sim, end to
end, on random fights it's never seen? Instrument: a **holdout cross-validation matrix** (`xval.mjs`) —
optimize a plan at each haste, sim every plan at every haste, check (a) **haste-monotonicity** (a fixed
plan never sims worse with more haste) and (b) **diagonal dominance** (the plan built for haste H wins its
own column). This became a **standing acceptance test** — see `docs/ACCEPTANCE.md`. Status: measurement
fixed (cold open), campaign gathering, one open debt (a low-haste micro-placement slack) recorded for the
next (fix) phase. Full detail in `docs/archive/07-phase6-xval-run.md`.

### Phase 7 — FIX the cross-val deficits (2026-07-24 → in flight)
Phase 6 measured; Phase 7 fixes. Each length-robust deficit was first **partitioned** (SEARCH-MISS vs
SCORER-GAP, `tools/diagnose-deficit.mjs`) and then fixed at its root rather than tuned away. The search
misses closed via polished Cold-Snap chain candidates, a drop-one escape pass, and — the structural
answer — **cross-haste pooling**, which makes model-side diagonal dominance (B1) hold *by construction*.
The scorer earned three new real terms (expected-slippage on wall/kill-flush windows, the external
lattice snap, ramp-cast state sampled at cast START). What remains is the B2 family: an emergent
**joint** haste×damage×kill interaction that is CLEAN in every isolated decomposition — handed to
Phase 8. Closing item **§5.11** was a *product* regression, not a DPS one: after the scorer
recalibration some fights rendered equal-score layouts in an order other fights don't use. Fixed with a
resolve-time exact-tie canonicalizer (`canonicalWindowOrder`) — see the ledger row below for what that
cost to learn. Full detail in `docs/archive/08-phase7-xval-fixes.md`.

**Round 5 (07-25) reframed what is left.** The campaign was re-gathered on the corrected reference gear;
it moved **35.9% of the plans and none of the verdict**, which killed "wrong harness gear" as the
explanation for the diagonal deficit. More importantly it exposed that the phase had been steering off a
banner with no discriminating power: invariant B as *implemented* is an existence test over ~90
comparisons per table whose typical margin (0.003% on the diagonal's own wins) is an order of magnitude
below the harness's ~0.02% resolution, so it reports `B FAILS` for a converged model and a broken one
alike. Replacing it with the one test a length-persistent defect must pass — a single rival layout better
at *most* fight lengths, run unrigged over all 57 kit-columns (`tools/xval-persist.mjs`) — collapses the
work list from **34 failing tables to 2 columns**, both low-haste (`isc-mqg h40`, `isc-skull h20`). The
bar is untouched (zero columns, user-directed); what changed is where to dig. PHASE7 §5.15.

**The cell duel (07-25) closed P7.13 by measurement, and it answered a different question than it asked.**
P7.12 had ended with *"aggregate re-ranking of this corpus is exhausted — target a CELL with a fresh sim."*
That duel (`tools/cell-band.mjs`, PHASE7 §5.17) ran six pre-registered tests on the top over-floor cell and
returned three findings, none of which was the expected one. (1) **The corpus got its first error bar, and it
is not a seed bar.** Seed noise is negligible (sd 0.0058 pp — the single-seed design is *vindicated*), but a
boss cell is a **5-variant wall-jitter mean** whose variant sd is **0.1427 pp**, 12× larger ⇒ a 95 % band of
±0.1251. The ripple floor prices only the **tail** wall; a boss fight has **seven**. So **6 of the 9 boss
over-floor cells were never deficits** — they sat over a ruler that admitted, in its own header, to being a
lower bound for them. Class cells are unaffected (`downtime`/`aoeWins` are BOSS-gated in `xval.mjs`, so they
genuinely have one wall), which is the whole of the 2.8× boss over-floor enrichment. (2) **The arbitration
tool's band was identically ZERO.** `plan-duel.mjs` defaulted to contiguous seeds `11..15`, which at iter 6000
share 5999/6000 iterations; measured, they reproduced each other **to the printed decimal**, so `|mean| > band`
was true for every nonzero delta. TOOLING had stated the mechanism correctly for weeks while the code shipped
the wrong default — the lesson is *put the guard in the tool*. No committed verdict rested on it. (3) **The
survivor is 100 % AoE-window valuation.** Ablating the AoE window (identical presses, walls, geometries, seed)
collapsed the deficit from **+0.2930 ±0.0068 pp to −0.0063 ±0.0048** — statistically zero. Outside the window
the model ranks the pair exactly right, so this is not a burn-model, search, or lattice error but
**within-window placement**: the model packs the cooldown cluster against the AoE window's end, the sim wants
it ~10 s earlier. Filed as the RULES §9 **Correction-3 candidate**, deliberately *not* patched — one cell
family, cause not yet named. Phase 7's remainder is that one term.

### Phase 8 — the B2 scorer-gap family (2026-07-24 → in flight)
Inherited from Phase 7 as *"an emergent joint haste×damage×kill interaction."* **Round 1 demolished its own
premise**: the decomposition that produced it never placed MQG where it claimed — wowsims silently RETIMED
the press onto a shared trinket lockout and printed a plausible number for a plan never run. The joint
theory was withdrawn, six mechanisms ruled out, and the deficit re-characterised as **catastrophic
cancellation** (two ~2.4pp opposing terms leaving 0.36pp). **Round 2 then demolished round 1's proposed
fix** — measuring at T=40 silently drops a trinket use from one of the two plans, making it a
cooldown-*presence* comparison — and replaced it with **single-buff steady-state marginals swept across
haste**. That instrument immediately paid: **THE FLOOR LAW** (a value window covers exactly `floor(D/Δ)`
casts in the sim; 10/10 with a source-level mechanism proof, both integer crossings captured), a second
harness input error (effective SP ≈1450, not 1387), and — together — a **zeroed mean model-vs-sim bias**
(+0.0895 → +0.0084 pp). The round's most useful output is a **negative** one: the surviving residual's sign
is *wrong* for B2, so "the model misprices SP under haste" is retired as a candidate, and a plausible
denominator lead ("the model fits more casts") closed as a pure start-vs-completion counting convention.
The phase's own guiding lesson is now explicit: **every measurement here has had to be audited as hard as
the model it was auditing.** **Rounds 3–5 then retired the entire single-buff instrument class** — first
per-buff valuation (the residual is a per-window *level* that cancels in B2), then window×window (§14, F3
wrong-signed), then window×kill (§15, F4 wrong-signed, the clause's fifth firing). With no candidate left
that a single-buff fight can even express, **round 6 changed instrument class**: stop decomposing, and walk
the five-press path between the two real layouts in both directions. It fired immediately — **one move, the
trinket pair, carries 82 % of B2** — and **round 7 split that pair down to a single press**. The phase's
long arc is therefore: *five rounds of increasingly refined decomposition, all negative, then two rounds of
differential walking that localized the defect completely.* B2 is now **one press**: `MQG` moving onto
`IV@202` in the last 27 s, where the model pays +0.26…+0.36 % and the sim pays ≈ 0. `Icon` — suspect since
round 6 — is **exonerated** by an explicit control. **Round 8 crossed {stacked, solo} × {mid-fight, at the
kill} and answered it: the STACK, not the kill.** The same press with 78 s of runway still costs −0.298 pp
(81 % of the kill-adjacent version), while a *solo* haste buff pressed at the kill is priced right to
0.012 pp — so fight-end truncation is falsified and the fix is not in how the integral terminates at `T`.
And for the first time the combat log named a mechanism rather than just a location: **the stacked window
runs at exactly the 1.000 s GCD floor**, the sim's window census is *exactly* symmetric (+3 casts solo, +3
casts stacked, 230 = 230), and `AP`'s value window covers the same 15 cast starts either way — the floor law
holding even when Δ itself is manipulated. The model books +1 net cast instead. **The scorer is not missing
the floor** (it clamps correctly); the remaining hypothesis is a banked *fractional* cast plus an
`AP`-packing credit, and — per the project's own methodology — that is a **sim-setup audit trigger** to
discharge before the model is blamed. Detail in `docs/archive/09-phase8-b2.md`.

### Phase 9 — performance, with zero plan drift (2026-07-24 → notes)
User: *"the tool has gotten a bit slow again and takes a lot of CPU."* Opened as a **measure-first**
phase under a hard determinism constraint (every emitted plan must stay byte-identical; exact-match 25
is the gate). Baseline CDP profile + a call census established the shape before any code moved: one
long solve does ~2.0M `repair`s and ~2.0M signature builds against only ~0.6M real simulations, and
~19% of all CPU is **memo bookkeeping rather than simulation**. The structural finding is that the
inner loop walks the same small schedule **five times** (repair · counts · clip · sigOf · simulate)
where one fused walk would do. Notes, hypotheses and landing order in `docs/archive/10-phase9-performance.md`; nothing landed
yet. (07-25: writing the ladder's **first** item up as a landable patch instead **corrected** it —
§4.3's sig-swap is 2.9× *slower*, not 20× cheaper, and is rejected; only its hoist half survives. The
bench that killed it found the bigger win: the **native `JSON.stringify` is a cheaper memo key than the
hand-rolled `sigOf`**, ≈1.8 s CPU/solve, now ladder item 1. `docs/archive/10-phase9-performance.md` §4.12–§4.13.)
The phase's larger contribution turned out to be the **iteration gate** (§5): a bare-node plan sweep +
A/B differ + a sim-free DUEL, replacing "exact-match or the hours-long acceptance round, nothing in
between." That instrument is what made the rest tractable — and it is also what localised the phase's
one real *correctness* find. **First two landings (07-25):** the groom loop's early exit (§5.12,
plan-neutral, −10.1% CPU) and **`groupSeeds`** (§5.14) — the seed class that closes the long-standing
`5:40 lust 0:05` SEARCH-MISS, root-caused in §5.13 by asking the cheap decisive question (*can the
operators reach it from here, given EVERY anchor?* — 89 s, answer +0.000) after eight guesses had not.

### Phase 8 — the B2 ranking error: charter DISCHARGED, the error itself UNSOLVED (closed 07-26)

Opened to explain **B2**, the one place the model *ranks* two plans against the sim rather than merely
mis-sizing them: at T=229 the sim prefers the haste-front-loaded h40 plan by +0.360 % while the model
prefers h70. Ten rounds narrowed the field by elimination — the floor law established and generalised
(§5/§11/§13), two harness input errors found and fixed (`t5two`, effective SP ≈1450, §6/§7), the
SP-under-haste candidate falsified on sign (§8), the "emergent joint interaction" framing **withdrawn**
after it turned out to rest on a press the sim had silently retimed. Round 10 (§21.5) decomposed the
P3 context asymmetry and found **C-BE dominant** — the model's continuous credit, 56 % of the asymmetry
and 64–71 % of both levels — which by §21.4's pre-registration meant the phase "collapses to
implementing one term."

**It did — and the term lost.** The charge was designed against §21.5's constraint (price each window's
*anchored* lattice from the board, because a flat `frac(D/Δ)` is the wrong shape), implemented in
`simulate()`, audited (two real defects: an AP premium that ignored additive Tirisfal pooling, and a
memo-signature collision that made the first gate read as a clean null), and gated on the B2 pair
itself. **The sign gate fails, 6/7 fight lengths worse**, robust to the 15 % premium correction. Four
independent measurements agree (§22 anchored proxy, §23 across length, §25 the real scorer, §25.4 after
the fixes). §13.8's retirement of the whole lattice-quantization family therefore **survives** §21.5's
invalidation of its ruler — the better ruler is 4.3× *more* anti-B2.

What the phase leaves behind: `cfg.boundaryCharge`, shipping **OFF and unreferenced** (the OFF path is
provably plan-identical, so the phase changes no plan the tool has ever produced); a **U-shaped length
profile** for the bias (§23) that constrains every future candidate; F3's residual retired as an
artifact of an ill-posed additive test (§24); and three "the failure mode looks like a clean result"
instrument traps in the ledger. **B2 stands at ≈0.445 pp, unexplained**, with 0.16–0.91 pp of
quantization headwind against any successor mechanism — and that hunt is **new, sim-gated work**, not
Phase 8's remainder. Full closing statement and the eight settled findings: `docs/archive/09-phase8-b2.md` §26.

★ The phase's transferable lesson: **a term dominating an ASYMMETRY implies nothing about its sign in
the LEVELS.** §21.4 wrote that implication into a pre-registration, and pre-registration protects
against post-hoc rationalisation — not against a bad inference embedded in the plan itself.

---

### 07-26 — the UI rework: one board, one honest assumptions page

Not a model phase — a **first-time-user** phase, done at the user's direction on the published site.
Four things landed, none of them touching the engine (all 25 exact-match plans bit-identical):

1. **The live Pressboard is DELETED** — the play/stop/reset clock, the next-up countdown banner, the
   timeline playhead, and the `follow`/`pressPlan` machinery behind them. It was a second copy of the
   plan competing with the schedule for the reader's attention, and a stopwatch is not what a raider
   reads mid-pull.
2. **The activation schedule inherits its layout** (below the timeline, where it already sat): the old
   one-row-per-buff table repeated the same timestamp on consecutive rows, which reads as *separate
   actions* when it is really **one macro press**. It is now one line per press second with the
   co-pressed activations clustered as icon tiles, inside window cards that keep the burst-level
   readouts (peak haste, AB cast time, mana to spam). Copy-as-text is untouched — it is the golden.
3. **The model assumptions were re-verified against the engine, and one was WRONG** — see the ledger
   entry below. They are now sectioned by subject, carry per-claim `sim-verified` / `beta` /
   `not modeled` chips, render at page load (not only after a run), and have a masthead button that
   scrolls to them.
4. **`Beta` badges on Ashtongue, Drums and Power Infusion** (user call): their physics is
   source-verified but their *placement* has never been certified end-to-end against the sim, and the
   tool should say so rather than let a first-time user assume every number carries equal weight.

Also: the masthead is a short title + eyebrow + three-step strip instead of a four-sentence paragraph
(titled "WoW Anniversary cooldown optimizer for Arcane mages"), and the phase editor's type dropdown
no longer overlays its own label while its trailing number field carries its unit inline (`× damage`,
`targets`) instead of relying on the user's intuition.

**Then a full copy audit of everything the user reads** (`index.html` + `README.md`), at the user's
direction — verify, rewrite in plain language, and hide what is technically true but irrelevant:

- **`README.md` carried the SAME disproven ramp claim in three places**, plus two features that no
  longer exist (per-press "price tags", clipped-press value tags — both permanently rejected), plus a
  description of press timing ("the phase-averaged start the integral wants is the press itself") that
  Phase 7's press-execution physics had superseded. All corrected against RULES §3/§3b.
- **The IV-out-of-Lust crossover numbers in the README were stale AND self-inconsistent** ("~100
  rating" in three places, "~150–200" in a fourth) — see the ledger row below.
- **Plain-language pass on the page:** the four result tiles now carry tooltips explaining what they
  mean (a first-time reader could not have known what "effective casts" was); the timeline legend
  states each reference line in words with the mechanism on hover; the fight-column and phase-editor
  hints stop reciting model internals; "Debugging presets" — a name that means nothing to a mage — is
  now "Reference fights — test cases, not bosses", collapsed behind a `<details>`.
- **The assumptions block got a "Show the numbers behind each claim" toggle** (`.deep` spans, hidden by
  default): the player-relevant claim stays visible, the mechanism (per-cast proc bookkeeping, the SP
  coefficient formula, the exclusive-category source read, the full tie-break order) is one click away.
- **Two things were MISSING and are now stated**: that the search is multi-start local + polish, i.e.
  **a very good plan, not a proven-optimal one** (with ACCEPTANCE's open residual — median 0.035%,
  worst 0.38% — behind the fold), and that the output is **deterministic and reproducible** from the
  copy-text. A tool whose button says "Find optimal overlay" owes the reader the first one.

---

### 07-26 — the sim comes to the page (gear-agnostic, same chain, one button)

The user's ask: *"a button in the HTML that does exactly what we do — wired up so it's clickable"*,
gear-agnostic (only SP / crit / haste, which the planner already collects), under the model's own
assumptions (infinite mana, cold open), reusing the established verification rather than a new one.

**Scouted first, built second.** The rig rebuilt from `tools/wowsims-patches/` exactly as TOOLING
documents (clone `wowsims/tbc-new@ade9f39cc`, both patches apply, `innerSpell`=3 / `CD.Use`=0, protoc
v1.36.10, 18 MB runner). Then the decisive experiment: **`GOOS=js GOARCH=wasm`** builds the same
engine to **22.8 MB (4.1 MB gzipped)** — and upstream already ships `sim/wasm/main.go` exposing
`raidSimJson(request)`, so no new Go had to be written. Measured **wasm == native runner to the
printed decimal** (1351.5/1351.5 and 1345.6/1345.6 DPS at 10k iterations) despite the wasm running
`RunRaidSim` and the runner `RunRaidSimConcurrent`. That equality is the whole licence for the
feature, and `tests/sim-duel.mjs` now asserts it.

Deliberate choices, each with a reason:
- **No server.** Netlify Functions would have meant a Go binary in a lambda, a 10s timeout, and an
  abuse surface on a free site. WASM in the page costs nothing per click and can't be rate-limited
  into uselessness.
- **The artifact is committed, not built at deploy time** — the bytes users run are the audited
  bytes, and a deploy cannot break because upstream moved.
- **The request is not hand-assembled.** The first attempt built the `RaidSimRequest` by hand and got
  the raid proto wrong (`debuffs` lives inside `raid`; a party carries `buffs`, not `bonuses`). It now
  patches `sim/model-ref-request.json`, which *is* the runner's own `--dumpreq` output.
- **`genapl.mjs` was split into a pure `genapl-core.mjs` + CLI** (proven output-identical), so the
  page and the terminal share one APL builder. Same for the transcription convention
  (`sim/planspec.mjs`) — fire times, floored, Cold-Snap split, the rule whose earlier violation cost
  −1.5% on a KT plan.
- **Gear-agnostic** by construction: fixed synthetic mage, no gear/buffs/consumes, hit pinned at the
  16% cap (202 rating; a 1% miss floor is irreducible and cancels), infinite mana, cold open,
  `var 0.5`. **Absolute DPS is meaningless and the UI says so** — only the paired, same-seed
  difference is reported.
- **It declines what it cannot do**: Burn phases have no encounter knob (refused outright), and
  Drums / PI / Ashtongue have no genapl press (both arms run without them; the UI names the omission).
- **A "too close to call" band** (|Δ| < 0.05%) so the button reports noise as noise, and a warning
  when the sim and the model disagree on *sign* — the shape a scorer gap actually takes.

Wall clock in a browser: ~10s for both arms at 10 000 iterations each, run concurrently in two
workers. Cold-start adds the one-time ~4 MB download.

**Same-day follow-up, two user calls.**

*(1) "Make it obvious this isn't a sim of their gear."* The button is now **"Check in the benchmark
sim"**, with a **"?"** dialog that opens on demand — *"It is not a simulation of your character. It
never asks for your gear, and it couldn't use it if you gave it to us. It runs the benchmark this
project uses to check its own model."* — plus what the benchmark character is, how to read the number
(**"read the gap, not the DPS"**), and what it can't see. The result panel repeats the short form next
to the number and links back to the dialog, because that is where the misreading would happen.

*(2) "I don't want the page to reuse it verbatim — that smells like we change one and not the other."*
Correct, and it was already true: `--var 0.5` and `--mana 100000000` were typed into `plan-duel.mjs`,
the seed into three files, `22.08` into two. Fixed properly rather than documented:

- **`sim/benchmark.mjs` is now the one definition** of the duel protocol. The page, `plan-duel.mjs`,
  `sim-duel.mjs` and `sim-request.mjs` all import it, and `runnerFlags()` **generates the native
  command line** from the same object — so the shell invocation cannot drift from what the page sends.
- **What is deliberately NOT shared: the character.** `reference-gear.mjs` (the cross-val's real geared
  export — a file that already says "NOT for index.html") answers a different question from
  `model-ref.json` (the synthetic benchmark mage). They must be free to differ.
- **And it is checked, not trusted.** `tests/sim-request.mjs` asserts the page's request equals the
  native runner's, field for field, across plain / geared / Cold-Snap / intermission / AoE / odd-stat
  cases — with *semantic* protojson comparison (an `EmitUnpopulated` default equals a missing field; a
  non-default present on one side only does not).

★ **The lesson from the negative control.** The first attempt at one — flip `BENCH.variation` to 0.6 —
**passed**, because both sides read the same constant and therefore still agreed. Sharing a value can
only ever prove *agreement*, never *correctness*; a typo propagates to both consumers and the equality
gate waves it through. So a second, complementary gate was added: **protocol invariants** that assert
the values themselves (variation ≠ 0 with the reason, cold open, mana ≥ 1e7, seeds spaced ≥ iterations,
hit cap 16). The real negative control — break *one* side — then failed as it should.

---

### 07-26 — the bench becomes the practice: `tools/bench.mjs`

The user asked whether the new gear-agnostic approach should become the standing way this project
sims — noting that phases 6/7/8 kept *setting it up again* and that the repo held no consistent tool.
Checking rather than agreeing produced a better answer than either of us had:

- **`docs/BENCH.md` already declared exactly that**, written hours earlier the same day: it supersedes
  the ad-hoc reference-export convention, **reverses the "never commit an export" policy** (that policy
  was what made the rig unreproducible when a container recycled), and specifies
  difference-in-differences as the primary normalisation.
- **But nothing implemented it.** No tool read `tools/bench/export.json`; all 15 sim-capable tools
  still resolved `RUNNER` + `EXPORT_BASE` out of a session scratchpad. The design had been standing
  practice on paper for a few hours and zero runs.

So the answer was not "adopt the new thing" but **"implement the thing you already decided"** —
`tools/bench.mjs`, BENCH.md §2.1 with a never-press control, a seed band on the *paired* difference,
and the model's own Δ printed alongside with a sign-agreement check.

**What the sim/ work actually contributed was the missing half: zero setup.** BENCH.md §5 requires
that "a fresh container must be able to produce a number from the repo alone" — true only in the sense
that you could *rebuild* the runner (clone, protoc, `go build`, ~2 min + network). With the committed
`sim/sim.wasm` it is now literally true: **~10 s, cold, `node tools/bench.mjs --preset X --vs naive`**.
The native runner keeps exactly one job — `tests/sim-request.mjs`, the gate that proves the wasm and
the native path agree.

**Two traps found while building it, both by the repo's own prior scar tissue:**

1. **The model cfg must describe the character the sim runs.** The first working version simmed gear-B
   while scoring the model at the *preset's* gear (sp 1387, no T5) — reproducing PHASE8 §6/§7's
   defect, where "agrees/disagrees" compares two different mages. Fixed by having each character carry
   its model parameters and **spreading `REF` from `tools/reference-gear.mjs`** rather than re-typing
   it, exactly as that file's own ★ instructs. The model Δ moved 1.434% → 1.314% on the same duel when
   the operating point was corrected — a real repricing that had been silently wrong.
2. **The tool finished its work and then hung** at 1.9% CPU, looking (through `tail`) exactly like a
   slow tool. `breathe()`'s MessageChannel and the Go runtime inside the wasm both keep node's event
   loop ref'd. `tools/plan-sweep.mjs` had already met this and documented the fix — *"a gate that
   HANGS reads as a slow gate, which is the same false signal as one that passes wrongly"* — so the
   fix was one `process.exit(0)` and a comment pointing at its ancestor.

**One conflict was left open rather than silently resolved.** BENCH.md §3's RNG table lists `--var 0`
as "standard for gates"; TOOLING ★★ says `--var 0` quantizes DPS to a staircase and has faked a result
twice. §2.1's control does not rescue it (the difference of two staircases is still a staircase, and
quantization is not zero-mean, so pairing cannot cancel it). `tools/bench.mjs` defaults to **0.5** —
TOOLING's rule, and what the website uses, so both fronts agree — and the conflict is now written into
BENCH.md §3 as a **user call**, to be resolved there and in `sim/benchmark.mjs` in one edit.

---

### 07-26 — two claims put to the instrument instead of to memory

**`--var 0` vs `--var 0.5`: SETTLED, and neither doc had it quite right.** The user asked for it to be
decided once and for all, noting their intuition said 0 while they remembered earlier sessions landing
on 0.5. `tools/var-decision.mjs` is the pre-registered experiment (decision rule written before the
first run). Result: **0.5**, on evidence that surprised in three places —

- The **staircase is real and huge**: mean casts flat for 1.5 s, then **+0.97 casts in one 0.1 s step**
  (concentration 19× vs 1.5×).
- **But it cancels in a paired comparison** whenever both arms truncate identically — the
  pre-registered "decisive" test came back a **tie**, twice. That is *why* `--var 0` survived: it is
  not wrong in the comparisons people happened to run.
- **It bites where the arms differ in terminal cast RATE** — the planner's most routine call. Measured:
  the size of a real effect swings **−32.8 → −0.9 → −31.8 DPS** across 0.1 s of fight length (worst
  step 31.4 vs 3.3 DPS). Nobody controls the kill second to a tenth.
- **And `--var 0` is not quieter, which was its entire justification**: seed band 0.06/0.40 vs
  0.04/0.25 DPS. Removing a random source made the estimator *worse*, because a fixed duration parks
  the fight end exactly **on** the discontinuity and any residual jitter is amplified into whole-cast
  jumps. The variation was doing the smoothing.

★ **The methodological lesson is the tie, not the win.** The pre-registered decisive test failed to
decide, and the honest response was to ask *why* — which produced the actual mechanism (cancellation
under a shared terminal lattice) and a sharper experiment. A tie on a pre-registered test is
information about the test.

**"The sim can't press Drums or PI" — right conclusion, wrong reason, found by the user asking "how
come it can do Bloodlust?"** The shipped copy said the *harness* had no press for a raid external.
The truth is one layer down and is an upstream fact: a spell is APL-addressable only if registered
with **`SpellFlagAPL`**. `registerBloodlustCD` has it; `drumsSpellConfig` (35476) and
`registerExternalConsecutiveCDApproximation` (PI, 10060) do not — they are auto-fired `MajorCooldown`s,
i.e. the sim's scheduler picks the timing, which is precisely what a press-timing duel must not
delegate. **Measured, and this is the bad part: scheduling them produces no error and no effect —
2128.9 DPS with and without, bit-identical, zero aura uptime.** A silent no-op, the harness's signature
failure mode wearing the engine's clothes. It is **patchable** (a third entry in
`tools/wowsims-patches/` adding the flag, at the cost of re-certifying the trust anchor) and that is
now recorded rather than assumed impossible. Ashtongue stays genuinely unpressable: it is a proc.

---

### 07-26 — closing the session: the alarm that was recommended twice and built once

Session-end sweep for anything discussed but never built. One thing survived the audit: the
**search-miss alarm**, which I had recommended in two separate turns and filed in ROADMAP as a
"next step" without ever writing it. Built now.

**What it is:** lock a hand-edited timeline that is **legal** and outscores the optimizer's own output
**under `robust`**, and the page says so loudly — in effective casts — and asks for the Debug export.
This is `tools/plan-duel.mjs`'s **confession rule** moved into the UI: `newEngine(hand-edit) >
newEngine(model plan)` cannot be manufactured by repricing, because the search was free to keep the
better layout and score better by its own measure and didn't. It is therefore gradeable **with no
simulator at all**, and it can only ever turn "no finding" into "bug", never into a false green.

Three things had to be right, and two are easy to get wrong:
1. **Compare `robust`, not `total`.** The comparison tiles' headline is at-kill damage; the optimizer
   maximizes the kill-window-smoothed `robust` (RULES §8). Beating `total` while losing `robust` is
   the objective working, not a miss — wiring the alarm to the visible number would have made it cry
   wolf on every terminal-window edit.
2. **The plan must be legal** — `repair()` fixpoint. Out-scoring from outside the feasible set proves
   nothing, because the search was never allowed there.
3. **Threshold in meaningful units** — effective casts, not damage, and > 0.01 so float ties and
   sub-cast tie-breaks stay quiet.

Verified both directions: a legal-but-worse edit gets the ordinary "Validated" message; a plan that
genuinely beats `robust` trips the banner with the gain printed.

**And the Debug export now carries the sim transcription plus a ready-to-run
`node tools/bench.mjs …` line**, so a user's bug report arrives reproducible: paste the command, get a
sim number in ~3 seconds. Tested end-to-end against the actual `dist/` deploy artifact, including the
netlify build command run verbatim.

---

### 07-26 — closing the session: the baseline is marked, and Phase 10 is written

Two housekeeping acts that are really one act — making sure the next session cannot mistake an old
number for a current one.

**Every gear-A artefact is now marked at its point of use.** `PHASE7`, `PHASE8`, `ACCEPTANCE` and the
archived `PHASE6` each open with a banner saying the same thing: the numbers below are gear A under
the pre-07-26 protocol; the *reasoning* survives permanently, the *verdicts* do not survive until
re-measured. `tools/xval-results-archive/gearA-pre-20260726/` gained a README naming exactly what
rests on it (ACCEPTANCE's "NOT PASSING", B2's 0.445 pp, PHASE7's basin debt) and what does not depend
on the baseline at all (every trap and correction). `tools/xval-results/` — which had still been
advertising "round 6 is being gathered" — now says plainly that it is empty on purpose.

★ **The distinction worth keeping: un-denominated is not withdrawn.** Nothing in the gear-A corpus was
wrong. It is true of a character that is no longer the reference, measured with an instrument that has
since changed in three ways. Marking it that way costs a banner; *not* marking it costs a future
session re-litigating a deficit that may not exist any more.

**`docs/PHASE10.md` is the plan** the session's own recommendation implies: re-measure ACCEPTANCE from
scratch on gear B with `tools/bench.mjs`, restate every debt as a first measurement rather than a
delta, and close whatever does not reproduce. It carries pre-registered gradings (a deficit is real
only at ≥3 seeds with |Δ| > 1σ of the paired band; B2 is closed only by absence, never by a sign
change) and one trap aimed squarely at this session's own output: **the new instrument has run gates
and a handful of duels, not a corpus** — so the phase opens with a shakedown on known-shape cells, and
if that fails it becomes a tool-debugging phase instead of a 36-table campaign.

---

### 07-27 — PHASE 10 CLOSED: the acceptance verdict is defined again, and three debts get real prices

**The problem Phase 10 existed to fix was not a failing test — it was the absence of one.** On 07-26
the reference character was re-exported and the whole gear-A corpus archived, so every model-vs-sim
number the project owned was denominated in a currency that no longer existed. ACCEPTANCE's status
block honestly read **"⏳ NO READING YET"**. Round 1 on gear B removes that.

**The arc.** A 36-cell campaign (30 class + 6 boss) gathered unattended over ~2 days of wall clock and
~50 CPU-hours, through a silent 10-hour death (`nohup` from an agent shell leaves the process in the
session's process group — fixed with `setsid` and a watchdog singleton), a corrupted-but-parseable
table (fixed with a writer lock), and a mid-round backend switch from the committed wasm to the native
runner for the boss half. It ends **36/36 under one protocol on one engine**, certified by a
provenance gate that did not exist when the phase started.

**The verdict: invariant A PASSES (`monoDip = 0.0000%` on all 36). B2 FAILS** — 142 borrowed-win
columns of 345 across 33/36 tables, worst 0.380%, median 0.035%. **ACCEPTANCE NOT PASSING**, on a
bar of zero.

★ **The phase's central model result is that the low-haste basin is NOT a gear artefact.** The
threshold-free persistence test names three of 57 kit-columns, and its first two are gear A's entire
work list reproduced **cell for cell** — same kit, same haste column, same rival haste, same win
count. The *denomination* changed and the *cell* did not. (No number crosses the baselines; what is
compared is which cells a structural test names, which is unitless.)

**And that target is now diagnosed rather than merely located.** It is **one terminal cast**: at the
worst cell the native plan casts 176 Arcane Blasts and the rival 177, and the same +1 appears at the
second-largest column (233 → 234), with the measured DPS margins at ≈⅔ of the cast fraction in both —
exactly what a *marginal*, unbuffed tail cast should be worth. The model scores that pair **0.014%
apart**, below its own resolution, against a sim that is emphatic at ~13σ. **So the model does not
mis-rank these layouts; it cannot distinguish them** — which is why cross-haste pooling cannot help
and why no search fix reaches it. The obvious repair is already falsified: discretizing the scorer
measured *worse* across a full column (r 0.7910 vs 0.9337).

**The three debts, re-priced.**
- **B2 SURVIVES.** Its cell *is* the round's worst column, so the corpus prices it directly for the
  first time: **+0.368 ± 0.020 pp, 5/5 seeds, REAL**, agreeing with BENCH §3e's `+0.389` and
  `plan-walk`'s `+0.375` on the other engine. Ranking error **≈0.38–0.41 pp**; the ≈0.43 pp target
  stands. §5's pre-registered closure test ("no residual of the same shape above the noise band")
  fails emphatically.
- **The low-haste basin REPRODUCES — and is misnamed twice.** A third persistent column sits at
  **h130**, mid-grid, so "≤70" described gear A's two cells rather than the mechanism; and the family
  is not homogeneous — two columns are the terminal-cast blindness above, the third has **equal cast
  counts** and is a sub-resolution *value* difference.
- **★ The KT/AoE cells DO NOT REPRODUCE, discharging a standing prediction.** ACCEPTANCE's gear-A
  record ended that thread with *"the boss-side re-run has NOT been re-gathered under the fix — the 3
  surviving boss cells are expected to move, but that is a prediction until a round is run."* This is
  that round. All three survivors were `isc+scb` KT; they now read **0.07%**, **0.05%**, and **not a
  borrowed-win column at all**, and the whole table tops out at 0.08%. PHASE7 §5.19's AoE press-snap
  fix landed *after* those measurements and its predicted blast radius was exactly one preset. The
  debt is **re-priced, not closed**: `mqg+skull` KT `@0`/`@100` now carry the family's magnitude.

**⚠ Two instrument findings, and both were found by reading a tool's output instead of its summary.**

1. **"wasm == native" never meant bit-identity.** `tests/sim-duel.mjs` asserts `delta < 0.05` DPS and
   prints both sides at one decimal; §8.26 summarised a gated re-run as *"BIT-IDENTICAL"* and the
   handoff doc turned that into *"the re-run changes the stamp, not the numbers."* Re-gathering all 30
   class tables on the native runner gave **24 identical, 6 changed** — five re-rounding a derived
   percentage by one step, and one **flipping a published verdict** (`scb-mqg-medlong` DEFICIT →
   CLEAN) off a ~1e-6 relative difference, because the matrix prints at `toFixed(1)` while `diagWorst`
   is computed from full-precision doubles. **Never derive a headline at finer precision than the
   artefact you validated it against.**
2. **`ripple-audit` fails two of its own pre-registered self-checks** (P5 monotonicity, marginally;
   P3 KT discrimination, 3/13 against a bar of 7) and says so in words — *"this tool is suspect, stop
   here"* — while its machine-readable footer stamps **`mono=0`**, which means FAILURE even though the
   `vacuous=0` beside it means success. **Opposite polarity on one line**, and it had already caused
   §8.22 to record a failing self-check as clean. ⇒ no ripple decomposition is quotable on this round.

**What the round also demonstrated about its own headline metric.** The `CLEAN 3/36` figure is worth
one table: two of the three are low-plateau (the tool emits 2–3 distinct plans across 10 haste points,
so adaptation is barely being tested) and the third is the rounding artefact above. That is
ACCEPTANCE's own *"the bar is right, the banner is nearly powerless"* confirmed from a new direction —
**a verdict banner that a 1e-6 engine difference can flip is not a thing to steer work by.**

---

## The corrections ledger — what we believed, and what disproved it

The most valuable part of this diary. Each entry: the belief, why we held it, what overturned it, and
where the corrected truth now lives. **Do not silently re-open these** — if you think one is wrong again,
add a *new* entry rather than deleting the old.

| 07-26 | **The shipped page linked to `wowsims.github.io/tbc`** as "the wowsims simulator" — in the sim help dialog and the model-assumptions lede, both added this session. | **User spotted it and asked the right question: "I hope that hasn't been impacting all our progress?"** It had not — the ENGINE has always been `wowsims/tbc-new` (what `wowsims.com/tbc` deploys), and TOOLING already carried a ⚠ about not deriving that URL from the Go module path. But the *citation* pointed at the ARCHIVED 2021 pre-APL sim, whose own page now says "This sim is outdated!". ★ **A wrong citation costs exactly as much credibility as a wrong dependency**, and it is not caught by any test — the copy audit two turns earlier verified the *claims* and never checked the *hyperlinks*. Fixed, and the two-repos table is now in TOOLING, SOURCES, BENCH §4d, sim/README and CLAUDE.md's conventions. The deeper fix is `tools/upstream-drift.sh`: one command that reports the distance from the pin and filters upstream commits to the paths that could move an arcane cast stream. First run: **21 behind, all inert** — the only mage-side change (#422's Mana Gem MajorCooldown) leaves the APL-castable gem spell untouched and adds an auto-cast our APLs never trigger. | TOOLING (the ★★★ table); `tools/upstream-drift.sh`; BENCH §4d. |
| 07-26 | **`--var 0` is "already standard for gates"** (BENCH.md §3's RNG table, written the same day) — fewer random sources ought to mean a cleaner A/B. | **Measured against `--var 0.5` side by side** (`tools/var-decision.mjs`, pre-registered). `--var 0` quantizes mean casts into a staircase (+0.97 casts in one 0.1 s step after 1.5 s flat), and where two arms differ in **terminal cast rate** it swings the measured size of a real effect **−32.8 → −0.9 → −31.8 DPS** for a 0.1 s change in the kill second. It is also **not quieter** — seed band 0.06/0.40 vs 0.04/0.25 DPS — because a fixed duration parks the fight end *on* the discontinuity, so residual jitter becomes whole-cast jumps. ★ The subtle part: the quantization **cancels** whenever both arms truncate identically, which is why two pre-registered "decisive" pairs tied and why the convention survived unchallenged. A var-0 result is therefore not reliably wrong — it is *unfalsifiable without checking whether the arms share a terminal lattice*, which is worse. | BENCH §3 (settled); TOOLING ★★; `sim/benchmark.mjs`. |
| 07-26 | **"The sim harness has no press for Drums / Power Infusion"** — stated in the shipped UI as though it were a genapl limitation. | **User challenge: "How come it can do Bloodlust? That feels like a hasty assumption."** It was. The real reason is upstream: only spells registered with **`SpellFlagAPL`** are APL-addressable. Bloodlust has it; Drums (35476) and PI (10060) do not — they are auto-fired `MajorCooldown`s. Verified empirically, and the failure is **silent**: scheduling them gives *no error and no effect* (2128.9 DPS either way, zero aura uptime). Conclusion unchanged, reason corrected, and it is now known to be **patchable** rather than impossible. | `sim/planspec.mjs` header; the in-page dialog; `sim/README.md`. |
| 07-26 | **"Icy Veins leaves Lust at ~100 haste rating"** — stated three times in `README.md`, with "~150–200 rating" in a fourth spot, as a sim-verified headline result. | **Re-measured on the current engine** during the copy audit (`tools/engine-node.mjs`, 2:40 fight, Lust@0:05, sweeping h0–h300): the crossover is **kit-dependent and far lower**. IV+Icon leaves Lust by **~15 rating**; with Arcane Power in the cluster it is ~80 (the SP payout pays for a little overcapping); and with a full opener kit (Drums + Skull) the window is floored from h0, so IV is **never** in Lust at any gear level. This matches RULES §5, which had recorded ~15/~80 all along — the README simply never got the update, and its two numbers disagreed with each other in the same document. ★ **A number repeated in four places is not four confirmations.** Self-consistency inside one doc was never checked, and the contradiction sat in public copy. When auditing user-facing text, re-derive the numbers from the engine rather than from the neighbouring paragraph. | README "The optimizer" / "Bloodlust overlay rules"; RULES §5. |
| 07-26 | **The shipped "Model assumptions" told users the AB stack ramp was NOT modeled** — "Arcane Blast is modeled at a steady 3 stacks… every candidate plan pays [the ramp] equally, so it never changes which overlay wins." True when written; **false since Phase 4 landed the exact ramp.** | Re-read the engine while rewriting the assumptions for the UI rework: `simulate` opens at **0 stacks**, runs the ramp casts at their true 2.5/2.17/1.83s lengths, re-ramps after every ≥8s AB gap, and scores them **discretely** — and RULES §3 records the consequence the old text denied (damage windows step off the ramp, sim-confirmed +0.10–0.44%). The user-facing doc had simply not been pruned when the model changed. ★ **Living docs include the ones inside the product.** `index.html`'s assumption list is a living doc with a *bigger* audience than `docs/` — prune it in the same commit as the model change. Also fixed in the same pass: the trinket-lockout bullet named only Skull + MQG when `OFF_TRINKETS` has always included the Icon. | `index.html` `renderAssumptions()`; RULES §3, §17. |
| 07-26 | **The sim rig's source was "not publicly recoverable."** Asserted after the container was recycled, on three seemingly independent proofs: a full clone of `wowsims/tbc` had no `ade9f39` and had never contained either patch target; `proxy.golang.org` returned `unknown revision ade9f39`; and GitHub refuses bare SHAs. A substitute rig was then built on the archived legacy repo. | **ALL THREE PROOFS SEARCHED THE WRONG REPOSITORY.** The repo is **`wowsims/tbc-new`** — which declares Go module `github.com/wowsims/tbc`. I *derived* the clone URL from `runner-main.go`'s imports instead of reading it, and it is written plainly in **four** places (`TOOLING`, `SOURCES.md`, `README.md`, `archive/07 §6`). `ade9f39` is right there (`ade9f39cc`, *"Merge PR #421 fix/armor-reduction"*), both patches apply, `assets/database/*.bin` are committed, the `archive/07` provenance checks pass (`innerSpell`=3, `CD.Use`=0), and `runner-ap180` builds. **Nothing was lost.** ★★ **THE LESSON — independent methods sharing one derived premise are not independent.** Three techniques agreeing felt like overwhelming evidence and was worth exactly as much as the premise underneath them, which was never checked against a doc that stated it outright. **When a search comes back empty, re-verify the search TARGET before believing the absence.** Sibling of this session's other three (round-4 collector, vacuous F3, `cfgSigOf` memo): each was a confident null produced by an instrument pointed slightly wrong. Two real gaps in the recipe were found and are now written down (`sim/core/proto` must be `protoc`-generated with a plugin matching the repo's protobuf v1.36.10; `cmd/runner/` must be created from `runner-main.go`). The gear export is absent **by design** — user data, never committed (`archive/07 §6.3`). | TOOLING "Building the runner"; ROADMAP §0. |

| 07-26 | **The boundary charge's first sign gate came back a CLEAN NULL** — `ON` and `OFF` bit-identical at all seven fight lengths. Read at face value that says the charge is a no-op: implemented, wired, and simply too small to matter. | **IT WAS NEVER COMPUTED (PHASE8 §25.2 / PHASE9 §5.19).** `simulate()` memoizes on `cfgSigOf(cfg)`, which enumerates scoring inputs **by hand** — `T, hasteRating, sp, critPct, coldSnap, t5two, enabled, fixed, segments` — and the new `boundaryCharge` field was not in it, so the `ON` call hit the `OFF` cache entry and returned it. No error, no warning, and the result looked like a *clean measurement of nothing*. Once the field was added to the signature the charge was worth **−278.7 damage** on the very first probe. ★ **Rule now recorded at the function: every cfg field that can change a SCORE must appear in the signature.** The list is hand-maintained and nothing checks it against the fields `simulateRaw` actually reads, so a `Proxy`-wrap guard (collect keys read, assert ⊆ signature) is filed in PHASE9 §4 — the structural fix, not just this instance. **Third member of this session's family and the most dangerous: the round-4 collector passed on ZERO data, §21.5's F3 compared a quantity against ITSELF, and this one cached the answer to a different question. All three fail as a confident-looking PASS/null.** | PHASE8 §25.2; PHASE9 §5.19. |

| 07-26 | **F3's 0.0724 pp residual was a signpost to a THIRD TERM.** §21.5 reported it as "unexplained, not absorbed" per pre-registration — the disciplined move — and both the erratum and §21.4's follow-up treated it as a real quantity awaiting an explanation (the kill taper, an estimator approximation, or a genuine third mechanism). | **THE ADDITIVE TEST THAT PRODUCED IT IS ILL-POSED (PHASE8 §24, sim-free).** C-BE and C-CASCADE are estimated from the same board, so independence is checkable: re-estimate C-BE with the cascade already applied (IV#3 at its §17.5 *executed* time) and ΔC-BE moves **0.1105 → 0.0311**, an interaction of **−0.0794 pp = 1.10× the residual**. ⚠ The magnitude match is a trap — the **sign goes the wrong way**: applying it makes the decomposition explain *less* (22.6 %, down from 63.1 %) and the residual *grows* to 0.1518 pp, so it does **not** absorb it. What it establishes is that the terms are **not independent**, so F3's 0.05 pp bar can be neither met nor missed meaningfully — the quantity under test is basis-dependent. The residual is an artifact of decomposing two interacting terms additively, **not evidence for a third mechanism**, and it is retired as a lead rather than solved. F1/F2/F4 are basis-robust; C-BE's dominance is untouched. ★ **Lesson: pre-register the INDEPENDENCE check alongside the additivity bar.** A basis-dependent estimator yields a confident-looking residual every time, and reporting it honestly (as §21.5 did) does not make it mean anything. Sibling of the vacuous-F3-taper entry below: same round, same section, two different ways for a check to be null. | PHASE8 §24; the §21.5 F3 sentence, annotated in place with both withdrawals. |

| 07-26 | **B2's differential bias was a thing to be explained by ONE mechanism, and §22's anti-B2 verdict was a T=229 result.** §2 had recorded the model negative 7/7 across lengths but nobody had asked where the bias sits, so implicitly it was one term measured at the headline cell. | **IT IS U-SHAPED, AND §22 HOLDS AT EVERY LENGTH (PHASE8 §23, sim-free — §2's sim column was already recorded, so only the model side needed computing).** `model − sim` reads **−0.197** pp at T=40, mean **−0.116** across T=70..205, **−0.397** at T=229: both ends ≈2–3× the middle, and 7/7 negative on the **corrected** harness, so §2's sign test was never a harness artifact. That is a hard constraint — a terminal-only mechanism cannot explain T=40 (none of h70's late presses exist yet) and an opener-only one cannot explain T=229, so it needs **two terms, or one scaling with how much of the fight is ramp-or-kill vs steady**. The obvious ramp story (start-vs-completion divergence ∝ cast length, maximal in the 2.5–4.7 s ramp where h70 spends `AP@4`+`isc@4`) was **tested and falsified on sign**: ΔL = **+0.912 pp** at T=40 against a −0.197 pp bias, ~4.6× too large and pointing the wrong way. ★ **Load-bearing: §22's verdict is not a T=229 artifact** — the quantization family points wrong at every length and is *strongest in the shortest, pure-opener cell*, which is the opposite of what a back-edge/terminal story predicts. | PHASE8 §23; ROADMAP §0. |

| 07-26 | **Phase 8's finale was "implement the boundary charge."** §21.5 found C-BE **dominant** — 56 % of the P3 context asymmetry and 64–71 % of both levels — and §21.4 had pre-committed that a dominant C-BE ⇒ *"Phase 8 collapses to implementing one term with its own sign guard and sim gate."* The only doubt was §13.8's anti-B2 verdict, and that had been reached with the flat `frac(D/Δ)` ruler §21.5 proved is the **wrong shape** (flush-vs-offset moves it 0.11 pp), so the expectation was that anchored re-pricing would flip the sign. | **✗ FALSIFIED, AND THE BETTER RULER IS 4.3× WORSE.** Re-priced against the anchored lattice (PHASE8 §22, `tools/p8-boundary-charge.mjs`, sim-free): `ΔL` goes `+0.036 pp` (flat) → **`+0.156 pp`** (anchored), where B2 needs a **negative** charge to close its `−0.380 pp`. The anchoring barely moves h40 (`+0.140 → +0.021`) but drives h70 **negative** (`+0.104 → −0.135`) — on its own flush `IV@202`/`MQG@202` lattice the model **under**-credits its value windows, the opposite of what the whole `frac` story predicts, which is why the differential widens instead of closing. §13.8's retirement of the lattice-quantization family therefore **SURVIVES** §21.5's invalidation of its closed form; B2's target stays ≈0.445 pp. ★ **THE TRAP, NAMED: a term dominating the ASYMMETRY tells you NOTHING about its sign in the LEVELS.** §21.4's collapse-to-one-term was an invalid *implication* drawn from a valid measurement — and it was pre-registered, which is the lesson: pre-registration protects against post-hoc rationalization, not against a bad inference written into the plan itself. Corollary worth carrying: **right physics can be wrong medicine** — RULES §3b-note's completion-rule asymmetry is source-verified and untouched; what died is the claim that charging it improves the model's *ranking*. | PHASE8 §22; RULES §3b.3 verdict note; ROADMAP §0. |

| 07-26 | **PHASE8 §21.5's F3 sensitivity check ruled the kill taper OUT of the 0.0724 pp residual** — "identical in the untapered currency (`total`), so the kill taper is excluded", recorded as a passed robustness check on round 10. | **THE CHECK COMPARED A QUANTITY AGAINST ITSELF.** `simulate()` returns `total` and `robust` as bit-identical numbers at round 10's config (T=229) — verified `r.total === r.robust` exactly, not just to printed precision. The kill taper only reweights damage inside `[T−KW, T+KW]` with `KW = KILL_WINDOW = 0.5` (`index.html:764`), so with no scoring breakpoint separating the currencies at that `T`, "they agree" is a **tautology** and the check could not have come out any other way. The taper is therefore neither excluded nor implicated, and the residual is **wholly unexplained** — one candidate short of where §21.5 left it. Blast radius is nil: F1/F2/F4 and the `dQuant` bonus finding all read `robust` deltas and board damage, never the `total`-vs-`robust` contrast, so C-BE dominance (56 % of the asymmetry, 71 %/64 % of both levels) stands and every figure re-derives exactly. **Lesson — the sibling of the round-4 collector that passed on zero data: a robustness check whose two arms can be the SAME NUMBER is not a weak check, it is a NULL one.** When a control is "recompute in another currency", assert the currencies actually differ *before* believing the agreement; `p8-round10.mjs` now prints `[total===robust? …]` per arm and refuses to imply a result when they don't. | PHASE8 §21.5 erratum; `tools/p8-round10.mjs` degeneracy guard. |

| 07-25 | **The xval drivers' hardcoded scratchpad path is just a convenience constant** — every driver had the same `SP=/tmp/claude-0/…/<session-id>/scratchpad` line and every run worked, so it read as settled configuration. | **★★ IT IS A SESSION-ID LEAK AND A SILENT TIME BOMB, AND THE OBVIOUS REPAIR IS ALSO WRONG.** (1) It writes a **session identifier into a committed, shareable artifact** — exactly what CLAUDE.md's identity rule enumerates; `tools/ladders/verify{,2,3}.mjs` carried the same line while the JSONs they read **are committed right beside them**, so the leak bought nothing at all. (2) It rots **invisibly**: a reclaimed container yields a fresh session dir, and `set -u` cannot catch it because the variables *are* set — just to a dead path — so a campaign dies minutes in instead of in the first second. (3) **The natural fix — "resolve the newest scratchpad" — would have picked the wrong one:** the box held **five** sibling scratchpads from earlier sessions and exactly **one** contained the runner, so discovery must probe by **content**, with mtime only ordering candidates. Now `tools/xval-env.sh`, sourced by all three drivers: override > discover > **loud exit 2** naming the variable to set. ★ **Lesson: an absolute path that embeds a session, container, or user id is not configuration — it is an expiry date plus a leak, and it will present as a mysterious mid-campaign failure rather than as a path bug.** Cost to find: a `git grep` for uuid-shaped paths that should be standing hygiene. TOOLING "Where the drivers FIND the runner". |
| 07-25 | **"The xval harness burns 123 s of buff while the boss is untargetable — the acceptance corpus is compromised."** The first P7.15 scan counted every buff-second that overlapped an intermission and reported the total. It looked like a corpus-wide catastrophe. | **★★★ 92 % OF IT WAS CORRECT BEHAVIOUR — the scan conflated CLIP with ARTIFACT.** A buff *tail* running into a wall (CLIP) is already priced: `simulate()` accrues no cast rate during an intermission, and KT opens `Icon@0` into the 0:15 wall in *every* plan by design. Only a press whose *time itself* is inside the downtime (ARTIFACT) is transcription damage. Reading `genapl.mjs:20-90` — every cooldown is an *unconditional* `schedule`, only Arcane Blast is gated on `_intermissions` — forced the split. Real figure: **2 plans of 60 (3 %), 10.0 s.** Cost of the check: one file read. Cost of believing the first number: a re-gather of the whole corpus chasing a bug that mostly was not there. **Generalized lesson (TOOLING lesson 5): run the IDENTITY FILTER first — before pricing how much an effect is worth, check whether the input differs at all.** The instrument that settled P7.15 took 30 seconds to write and was built *third*. PHASE7 §5.22. |
| 07-25 | **"The intent transcription DEFLATES the affected plans"** — it makes the sim burn buff during downtime, so those plans should sim *worse* than the fire-time version. Stated as a prediction before the duel ran. | **★★★ THE SIGN IS INVERTED: intent INFLATED them by ~0.26 %.** The duel (`tools/xval-emit-duel.mjs`) on KT `mqg+skull` @235/@265 reads **−0.267 % / −0.254 %** going *to* fire times. Mechanism, traced rather than shrugged off: `MQG@100` deferred past the [94,105) wall fires at `105` and **gains** 5 s of targetable buff — as predicted — but MQG's **300 s cooldown** then pushes the second press `400 → 405` against a fight that ends at **420**, repaying the gain with interest at the truncated tail. **The consequence is the part worth keeping: an INFLATED plan sitting in a BORROWED column manufactures a PHANTOM DEFICIT.** The §5.21 framing worried only about cells looking *worse*; over-valuation is an independent failure mode it did not anticipate, and it means some recorded deficits may **vanish** on re-gather rather than merely shift. PHASE7 §5.22; ACCEPTANCE harness-fidelity block. |
| 07-25 | **"The acceptance test tells us the model is correct."** Implicit in calling it *the standing completion test* — the thing the model must pass FULLY before it is called complete. | **★★ IT IS PURELY RELATIVE AND CANNOT SEE UNIFORM ERROR.** It asks one question: does the plan optimized at haste H beat every *borrowed* plan at H? That detects **misallocation across haste**. Every cell could sit 2 % below the true optimum and the diagonal would still come back CLEAN. The sim trust anchor (~0.4 %) certifies the **physics**, not the **search** — so a systematic search weakness (the optimizer always missing the same *kind* of layout) is invisible to the entire campaign by construction. The absolute anchor already existed and was never used as one: `tools/brute-grid.mjs --tool`. In-tool "exact mode" is permanently rejected as a *product* feature; that rejection never applied to enumeration as a **research check**. First standing run 07-25 (T=80): `isc+scb` and `mqg+skull` both **3/3 PASS** across h0/h195/h300 (worst Δ −0.062, inside the 0.15 pressability slack). So "ACCEPTANCE PASSING" means *no plan is beaten by a plan built for other gear* — **not** *every plan is optimal*. ACCEPTANCE standing-limitation block; TOOLING lesson 7. |
| 07-25 | **The harness is an independent check on the model.** Assumed by the whole duel methodology — the sim is the outside voice. | **★★ IT IS ONLY PARTLY INDEPENDENT: `xval.mjs` computes the fire times it feeds the sim with `simulate()`, the function under test.** If `simulate()`'s intermission handling is wrong, model and transcription are wrong *identically*, the sim agrees, and the round reports **CLEAN**. Not hypothetical — this is exactly the Vashj phase-drop bug. ⚠ **`EMIT=fire` makes the coupling worse**, because the harness input now routes *through* `simulate()` where it used to bypass it. Still the right call (a duel must execute the plan the tool prints), so it was paid for with a check that does not share the suspect code: the **ARTIFACT GUARD** reads only the emitted spec + the preset's own `_intermissions` table, pure arithmetic, and must read **0** under `EMIT=fire`. `artifact=N` is stamped on `XVAL-DONE` so the count is in the permanent record, not on a screen someone watched. **Generalized: whenever an instrument computes its own input with the code under test, write down what an independent recomputation would be — and run it.** TOOLING lesson 6. |
| 07-25 | **"Simming naked / hit-capped / crit-less would make the numbers cleaner and cheaper"** (user proposal — strip the stochastics, sim fewer iterations). | **Four of the five are safe-but-not-worth-it; ONE IS A TRAP; and the question found a REAL GAP.** ⚠ **0 % crit is not rank-neutral**: crit cancels on single target but *not* through `aoeCritAmp(N, crit)` (Clearcasting→Arcane Potency, N proc rolls per AE cast, magnitude **falls as crit rises**), so zeroing it reweights `M(N)` — i.e. it changes the ranking in **exactly the AoE regime the surviving deficits live in.** **Naked does not generalize**: gear is the operating *point*, not a contaminant; naked just moves it somewhere no player stands, and it is still one point. **Forcing 100 % hit / averaging the damage roll are genuinely rank-neutral** (flat `×(1−m)`, zero-mean noise) but buy little, because the biggest variance reduction available — **common random numbers** — is already in place via the paired fixed seed, and the wall-clock saving they were meant to fund was delivered *losslessly* by `DPS_CACHE` instead. **The real gap the question surfaced: we sweep HASTE exhaustively but hold SP at one value**, while an additive +SP buff's worth *relative* to a haste buff provably depends on base SP. Tracked as the SP-axis cross-val. TOOLING "Should we sim NAKED…". |

| when | we believed… | …until | corrected truth lives in |
|------|--------------|--------|--------------------------|
| 07-19 | Model AB at a **steady 3 stacks** — drop the opener ramp as second-order. | The ramp materially changes opener/post-intermission value; sim disagreed with the flat model. | Rebuilt as the cast-rate integral (07-20) then the **exact discrete ramp** (Phase 4). RULES §3. |
| 07-19→22 | Various **tie-break / "press-anywhere" leeway bands + reasoning tags** would help the user. | A plateau tie is conditional on everything else staying put, so the bands over-promise. **User decision: rejected.** | Deleted from `index.html`; CLAUDE.md "permanently REJECTED" list. |
| 07-21 | The **Vashj icon-count** mismatch (tool plays 4 icons, sim implied fewer) was a model valuation bug. | Audited the sim setup: the **sim was wrong** (a drop bug), the 4-icon model plan was correct. | RULES / PHASE notes; the "sim is rarely wrong, we usually used it wrong" methodology (CLAUDE.md). |
| 07-21 | The APLActionSchedule harness **reliably fired** scheduled presses. | It **dropped on-cooldown presses** — a real harness bug. | `apl-schedule-strict-ready.patch`; TOOLING. |
| 07-21 | AP's cooldown in the sim is **195s** (a real discrepancy vs the 180s source). | It's a **wowsims quirk** (cd measured from buff-end); real TBC is 180s cd-on-activation. | The AP-180 patched runner; TOOLING ★. |
| 07-21 | On the 3:20 fight, **free Cold Snap sequencing was "a wash."** | Re-scored: it's **+3.6 effective casts**, not a wash. | RULES §8 (CS spending rule). |
| 07-22 | A **finite-mana layout model** was worth building (beta MODE, toggle). | Too many unreliable inputs; it doesn't change *layout* ranking. **User decision: rejected**, layout-first. | The per-window mana chip is the mana-UX ceiling; EP.md; CLAUDE.md REJECTED list. |
| 07-23 | The sim's **haste non-monotonicity** (h130=53 casts, h140=52) was **real TBC GCD-floor physics.** | It was the harness **PREPULLING** (a fixed −2.3s cast that doesn't scale with haste) while the model opens COLD. | Cold open (`_prestack:0`); PHASE6 §4.7; the ★★★ never-prepull rule in TOOLING/RULES/CLAUDE. |
| 07-23 | `xval.mjs` should **equip the Mana Emerald (22044)** for the scb kit. | 22044 is a *consumable* — equip the **Serpent-Coil Braid trinket (30720)**, *cast* 22044 to proc it. | PHASE6 §4.6. |
| 07-23 | "deficits **shrink with fight length** ⇒ all boundary quantization, no model error." | True for isc+scb, **false for mqg+skull** (its low-haste deficit is length-independent). | PHASE6 §2.1/§4.5. |
| 07-23 | The low-haste cross-val deficits **are the §4.1 IV-into-Lust basin, sim-confirmed.** | Spot-checking native-vs-borrowed plan diffs showed a **heterogeneous** mechanism (late-cluster spacing, AP timing, Zerk/Lust alignment) — broader than §4.1's specific basin. | PHASE6 §2.1/§4.1/§4.5 (walked back same day). |
| 07-23 | The low-haste deficits are **length-persistent (not boundary quantization)** across kits. | An adversarial re-check of the raw grids showed this is **mqg+skull-ONLY**: `isc+scb` *reverses to CLEAN* medium→medlong (the quantization fingerprint), `isc+mqg`'s low-haste deficit is gone by the long fight. The universal claim was cherry-picked from one kit. | PHASE6 §2.1/§4.5. |
| 07-23 | The campaign's worst deficit (`isc+scb` medium, 0.77%) is a **low-haste (sim20) micro-placement** slack. | Adversary: the tool emits ONE plateau plan across haste 20–245 and **both** endpoint plans beat it (plan@400 wins all 10 columns) — a **whole-band plateau** suboptimality, not a low-haste nudge. | PHASE6 §2.1/§4.5. |
| 07-24 | Steady-state **press-to-boundary phase-averaging is value-neutral** (§4.3, "documented, accepted, ~0.1%"). | It is neutral only for INTERIOR windows; a window sequenced to end **flush against a wall or the kill** loses the clamped slippage for real (Al'ar stagger: model tie, sim **−0.66% at every kill-variance**). | RULES §3b.1 (expected-slippage term); PHASE7 §5.3. |
| 07-24 | **Raid externals act from their call second** in the scorer. | The aura lands at the call, but the STREAM only accelerates from the next cast boundary (in-flight cast keeps speed) — and near the pull the lattice is deterministic, so the call's phase is not averageable (VR: compressed ramp stranded 1.19s of Lust@10, −0.18%). | RULES §3b.2 (external lattice snap). |
| 07-24 | **IV@0 ramp compression is worth ~+0.16 eff casts** (engine head-to-head, RULES §3). | Never sim-gated; decomposed at Al'ar/VR the sim says compression ≈ **wash** — the re-phased lattice strands the near-pull raid call by what compression gains. | RULES §3b (compression tempered); the engine now weighs both. |
| 07-24 | **Ramp-cast damage samples its buff state around the COMPLETION** (Phase 4). | The completion choice was right for damage *time*, wrong for *state*: a press-snapped ramp-exit window was credited ~half of the cast it fired after; sim/game snapshot at cast START (T=98 h140 pair: model +0.12 vs sim −0.40% → agreement after the fix). | RULES §3b.3; `rampCastDmg(ts, tc)`. |
| 07-24 | **var10 is the cross-val metric** (var0 is the trap, var10 the protocol read). | var10 asks a question the model deliberately doesn't answer (±10s kill hedging → late-window premium, deltas 2–4× inflated); the scorer's `robust` is EXACTLY expected damage under a uniform kill in **T±0.5s** → var0.5 is the model-matched read (the §16 h150 ramp-hug gate flips −0.08%→+0.37% var0→var0.5, matching the model to 0.01%). Fixed **walls** need their own treatment: wall-jitter averaging (no kill-variance smooths wall parity). | TOOLING (metric bullets); ACCEPTANCE protocol; xval `VAR`/`WJITTER`. |
| 07-24 | The cross-val's sub-1% deficits are mostly **quantization to be measured away**. | The §2 diagnostic partitioned all 167: **19 real search misses** (CS-chain geometries, drop-a-use alignment — closed by polished chain candidates + a drop-one pass; every probed miss is also reachable from a neighbor-haste champion), 26 KT measurement-caveat (closed by AE emission), and a small set of REAL scorer terms (above) under the metric artifacts. | PHASE7 §5; tools/diagnose-deficit.mjs. |
| 07-24 | Search misses need per-target anchor surgery (§3a.1) to reach neighbor-haste basins. | A general **cross-haste pooling** wrapper (`optimizeAsync cfg.poolHastes`: emit argmax over the fixed champion set scored at H) makes model-side B1 hold **by construction** — no per-target anchors needed. Design analysis: B1 22→0. End-to-end: scb+mqg medlong 0.20%→**0.01%**. Three correctness invariants earned by debugging (raw-score not re-polish; equal pool `starts`; baseline anchored to `simulate(base.s)` past the Cold-Snap normalize/val mismatch). | ARCHITECTURE (pooling); PHASE7 §5.10; ACCEPTANCE B1. |
| 07-24 | The residual ≥0.3% deficits (e.g. isc+mqg medlong MQG@202-vs-@9, sim +0.46%) are a single crackable scorer term. | Each candidate mechanism is CLEAN in isolation (haste-over-damage 0.000; haste-on-IV model & sim agree; lone-haste early-vs-late a wash both). The deficit is an **emergent joint interaction** (MQG stacked on IV + on the terminal cluster + near the kill, simultaneously) — no single term. Reserved for the highest-effort next phase. | PHASE8 (scoped); `scratchpad/{posindep,iso2,clincher}.mjs`. |
| 07-24 | A legibility canonicalizer belongs **inside `normalize`** (that's where the other placement normalizers live). | Placed there it *lost* 0.0136 eff casts on Hydross: `normalize` runs inside the hop↔normalize fixpoint, whose downstream passes re-drift the rotated layout and re-converge on the old shape. Moved to the three `resolve(...)` sites — the LAST thing to touch a plan — it lands the exact tie (`robust=196077.764863`, bit-identical) with the cluster-first reading. Corollary found the same day: the canonical packed form was **unreachable** from the raw window second `A` (not a legal press boundary during the opener ramp), so the packing pass needed the incumbent's own snapped span start `A2` as a second anchor — which turned out to be a **strict** +0.0067-cast win on `4:00 lust 0:05`, not just a cosmetic reach. | ARCHITECTURE (`canonicalWindowOrder`, packing-pass anchors); PHASE7 §5.11. |
| 07-24 | The tool's CPU cost is dominated by **simulation** (it's a simulator). | Profile + call census: `simulateRaw` is 41.5%, but **~19% is memo bookkeeping** (`sigOf` 12.3% + the wrapper 6.4%) and `repair` another 10.6% — and `Map.get` costs 0.011 µs against 1.158 µs to *build* each key, so the memo's whole overhead is key construction. One solve repairs and signs each schedule ~3.3× per actual simulation. The win is in the plumbing, not the physics. | PHASE9 §1/§3. |
| 07-24 | The sim **seed drives crit rolls and SP-trinket procs** — that's what CRN pairing cancels. | A source enumeration (`tbc-new` @ `ade9f39`, every line re-verified) found **one shared SplitMix64 stream** in production — `RandomFloat(label)` ignores `label` unless `sim.isTest` — consumed **5–6× per AB cast**: a **base-damage roll** (AB is 668–772, not flat), a partial resist (drawn even on a miss; ~13.7% land partials vs the lvl-73 target), a hit roll under a hard **1% miss floor** no hit rating clears, the crit roll, Arcane Concentration, then procs. **The drawn fight length rides the same stream** (`sim.Duration += RandomFloat("sim duration")…`) and is often the largest single variance source. So CRN cancels far more than crit — and *any* press-count change desyncs all of it, which is the real mechanism behind the count-preserving rule. Also: APL evaluation consumes **zero** RNG; player reaction-time jitter is dead code; AoE rolls base damage **once per cast** and shares it across targets. ⚠ `RandomSeed == 0` falls back to wall-clock — never seed 0. | TOOLING "RNG-consumer inventory" + the per-question trust ledger; SOURCES (AB base roll). |
| 07-24 | **Rigid wall-jitter** (shift walls + post-wall presses by one common δ) washes wall parity. | It is a pure TRANSLATION — every segment's internal cast-parity vs its own bounding walls is preserved exactly, so it washes nothing (Vashj still 0.64% under it). The parity is per-SEGMENT whole-cast truncation (proven by a 2-wall minimal pair with per-interval log verification: sim cadence == model cadence exactly; stacked window model +1.65 vs sim +1.04 casts, split +1.48 vs +1.54). The wash must vary SEGMENT LENGTHS: independent per-wall seeded δ_i, presses tracking their segment's wall. The model's continuous credit is the right real-world expectation — no scorer change. | TOOLING wall-jitter bullet (★); xval.mjs jitter v2; PHASE7 §5. |
| 07-24 | The B2 deficit reduces to ONE placement: **moving only MQG@202→@9 captures it, sim +0.461%** — and since each candidate mechanism is clean in isolation, it must be an **emergent joint haste×damage×kill interaction**. | **The measurement never placed MQG at 9.** The h70 layout presses Icon@4, and every on-use offensive trinket shares a category-1141 lockout whose duration is the trinket's own **20s buff** (`shared_utils.go`: `SharedCD.Duration = config.Duration`) — so the sim **silently RETIMED** MQG to **25.64** and printed a plausible number for a plan never tested (`SIMLOG=1` log in TOOLING). The +0.461% was @25.64-vs-@202. The joint-interaction theory built on it is **withdrawn**. | TOOLING ★ (shared trinket lockout); PHASE8 §1. |
| 07-24 | The B2 residual is a ~0.36pp scorer term to be found at the representative target (T=229). | It is **catastrophic cancellation**: the h40 plan is **+7.850%** at T=40 decaying monotonically to **+0.360%** at T=229 — two ~2.4pp opposing components (haste-loaded opener vs h70's last-24s MQG+IV burst). A 4% relative bias in either term *is* the whole answer, which is why every isolated probe "reports clean" — the signal was being measured where it is smallest. **Measure at T=40 instead** (7.85% signal, 0.316pp error, same sign). Also newly ruled out with evidence: `KILL_WINDOW` width (model KW-invariant), GCD floor, boundary straddle, window membership, and press-latency asymmetry (**wrong sign** — a later opener press starts on a higher AB stack, favouring h70). | PHASE8 §2–§4. |
| 07-24 | The model over-credits **Arcane Power by 2.374pp** (implied sim multiplier 1.2407 vs the model's 1.30) — a scorer bug. | Already-known physics + a **mis-configured probe**: Tirisfal 2pc (+20% AB) pools **additively** with AP, so AP is ×1.25 on a T5'd AB stream (SOURCES; TOOLING ★, Phase 5), and an additive base of 1.2465 reproduces the measurement exactly. My probe cfgs simply never set `cfg.t5two`. **The engine is correct** (`t5add` on the AB sites 831/899, correctly absent from the AE sites 829/898). But the same omission is in the **harness** — `xval.mjs:111` / `xval-model.mjs:53` build cfg without `t5two` while the export wears T5 (items 30206/30196/30207) — so the campaign has scored a no-T5 model against a T5 sim. Impact is small on AB-only fights (nearly rank-preserving: B2 delta −0.02%→−0.04%), so gathered rounds stay valid; fix **between** rounds, not mid-round. | TOOLING (⚠ xval `t5two`); PHASE8 §6. |
| 07-24 | The B2 conditioning fix is to **measure at T=40**, where the same layout question shows a +7.850% signal instead of 0.360%. | **Truncating the fight changes the question.** The h70 plan presses `MQG@202`/`IV@202`; at T=40 they never fire, so the delta is h40's full kit vs **h70's kit minus a trinket use** — a cooldown-*presence* comparison, not a layout one. That confound (monotonically weaker as T grows) fully explains the smooth +7.850%→+0.360% decay with no physics. The real conditioning fix is **single-buff steady-state marginals swept across haste** (one buff at t=30, T=100, window fully interior, CRN-paired): the ~1.3% signal is the *whole* quantity rather than a residue of two ~2.4pp terms. | PHASE8 §4 (the round-1 §4 is now the disproof). |
| 07-24 | A value-multiplier window's worth is the model's fractional `D/Δ` casts — the press-phase average makes it unbiased. | **THE FLOOR LAW:** in the sim it is **exactly `floor(D/Δ)`**, verified **10/10** across haste 0→300 *including both integer crossings* (h78→h82 straddles 13.993/14.027 in a 4-rating-point window; h160→h200 straddles 14.686/15.024). Mechanism proved, not fitted: wowsims applies the modifier at **cast COMPLETION** (`sim/core/cast.go:216/258/338/356`) and the APL can only press at a **cast boundary**, so `firstBoostedCast − auraGain == Δ` at all ten hastes (1.500…1.260). The phase-average argument is **conditional on a uniform press phase** — true for a human (off-GCD trinkets/AP are pressable mid-cast), false for the boundary-locked sim, which realises only φ=0, the *minimum*. So it is a harness expressiveness limit first (~+0.036pp), a candidate back-edge model refinement second. **Haste buffs are EXEMPT** (compression rolls to the fight end). Two counting traps found the hard way: timestamp membership in `(gain, fade]` double-counts both edges (floor+1 in 9/10) — count by **CRN damage-difference**; and log-derived Δ quantizes to 0.01s so it cannot resolve a crossing — use analytic `Δ(R)=1.5/(1+R/1577)` (valid: the R=0 log gives exactly 1.5000, so the export's base spell haste is 0%). | TOOLING ★ ("the sim cannot press mid-cast"); PHASE8 §5; RULES §3b-note. |
| 07-24 | RULES §3b.3's mechanism: *"the sim (and game) snapshot a cast's buff state at cast START."* | The **fix** is right, the **mechanism** is wrong — wowsims snapshots at cast **completion**. They coincide at a window's front edge only because the press is boundary-snapped ("completion of the cast in progress" *is* the next cast's start); they **disagree at the back edge**, where a start-snapshot model over-credits the window's unfinished last cast by `frac(D/Δ)×premium`. | RULES §3b.3 (mechanism correction); PHASE8 §5b. |
| 07-24 | Tirisfal-4pc uptime **rises with haste** (more casts ⇒ more crits ⇒ more +70 SP procs), explaining the haste-growing model-vs-sim residual. | **Measured and disproved:** uptime is **88–94% with no haste trend**. The correction is a *flat* one — effective SP ≈ **1450**, not the harness's 1387 (the log states it outright: every AB `[DEBUG]` line reads `SP: 1386.2` or `SP: 1456.2`, an exact +70 from `SpellID: 37444`). A second harness input error alongside `t5two`; fix both between rounds. With floor-law + effSP the mean model-vs-sim bias on a clean single-buff marginal goes **+0.0895 pp → +0.0084 pp** — the model's SP valuation is unbiased once the harness is described correctly. | TOOLING (⚠ effective SP ≈1450); PHASE8 §7. |
| 07-24 | The surviving haste-correlated residual (r≈+0.91, −0.147pp@h0 → +0.103pp@h300) is the B2 mechanism. | **Wrong sign — a falsification.** Positive at high haste means the model *over*-credits an SP window as ambient cast rate rises. In the B2 pair h40 puts **Icon@29 riding MQG+IV** (highest effective haste) while h70 puts **Icon@4 bare** — so correcting it would *lower* h40 and make the model prefer h70 **more**, widening B2. "The model misprices SP under haste" is retired as a B2 candidate. | PHASE8 §8. |
| 07-24 | The model **fits more casts than the sim** (+1 low haste, **+2** high haste) — a denominator effect with exactly the residual's shape. | **A counting convention, closed.** `model == sim completions + 1` at **every** haste, exactly: `index.html:834` counts casts that **START** before `T`, the combat log shows **COMPLETIONS**, and one cast is always in flight at the kill. The apparent haste-growing "+2" was my own regex — wowsims enforces a hard **1% miss floor**, a miss logs as `Miss` not `Hit`/`Crit`, and exactly 1 miss appears from h120 up (~70+ casts). **No score impact**: `castCount` is board/UI only; scoring is the `rateAt` integral. | PHASE8 §9. |
| 07-24 | **Haste buffs are EXEMPT from the floor law** — their value is time-compression that rolls to the fight end rather than expiring with the window. | A **`--var 0` artifact**, caught by round 3's pre-registered sweep. The law's general form is `floor(D_eff/Δ_inside)` where `Δ_inside` is the interval **in force inside the window** (`Δ_buffed` for a haste buff): IV steps at `R≈98.6` and `R≈196.9`, Zerk at `R≈143.4` — and MQG, **same `D=20` but a smaller bonus so no integer is crossed, correctly does not**, which is what makes it a law and not noise. At `var=0` the **fight-end quantizer exactly compensates the window floor** (the window hands its lost fraction back at the kill, where a fixed `T` re-floors it); jitter past one cast interval and the cancellation breaks. Round 2 had only ever measured haste buffs at `var=0` and so saw the sum, not the terms. **This cancellation is a haste × kill-boundary interaction — i.e. exactly B2's shape, and invisible to every single-axis decomposition by construction.** Live lead. | PHASE8 §13 (verdict), §13.4 (the lead); RULES §3b-note. |
| 07-24 | AP and SCB share `D=15`, so the law says they step at the **identical** haste, to the rating point. | They don't — SCB steps at 156→157, AP at 157→158 — and the cause was **measured, not fitted**: SCB (`SpellID: 37445`) is a **proc off the Mana Emerald use**, not a direct on-use, so its aura lands at `[30.47]` where every other on-use lands at the cast boundary `[30.46]`. `D_eff(SCB) = 15.010 s`, and at R=157 that 10 ms **is** the step (`15000/Δ = 10.9968` floor 10 vs `15010/Δ = 11.0041` floor 11). Predicted 156.6 ⇒ `(156,157]`: exact. Two wrong hypotheses died first — *different gear ⇒ different base haste* (base staircases are identical) and *the Emerald eats a GCD ⇒ a shorter window* (wrong sign). | PHASE8 §13.3. |
| 07-24 | A window's coverage is `floor(D_eff/Δ_inside)` for **both** buff classes — round 3 pinned every step location, so the count form must be right too. | **Step locations cannot distinguish `floor` from `ceil`** (same crossings, same step height) — only the *level* can, and the level says the classes differ. wowsims reads a **value** modifier at cast COMPLETION (`floor`) but computes **cast time at cast START and freezes it** into `Hardcast.Expires` (`sim/core/cast.go:138`/`:187`), so a cast begun a tick before fade runs fast for its whole duration → **`ceil`**. Scored on 6 buffs × 13 hastes: per-sampling-rule 0.0439 pp mean residual vs 0.1114 raw, and 5× better than forcing `floor` on IV/MQG; 37 of 39 haste points sit closer to `ceil`, both exceptions at exact-integer `n` where the two agree. **The model's bias is therefore opposite-signed between the classes** — over-credits value windows, under-credits haste ones. | PHASE8 §13.1/§13.7; RULES §3b-note. |
| 07-24 | **The floor correction pushes B2 the CORRECT direction** (§12: "sim bias favouring h40 = 0.036 pp, ~9.5% of B2"). | A **sign error, mine.** `L(h40)=0.140% > L(h70)=0.104%` is how much the sim **fails to deliver** of what the model credited — a *larger* `L(h40)` means the sim rates h40 **lower**, so the term is **anti**-h40, pointing the same way as §8's residual rather than opposite to it. Adding the haste class under the corrected `ceil` rule makes it worse, not better: net quantization predicts the model should read h40 **+0.065 pp higher** than the sim, while it actually reads **0.38 pp lower**. **The whole lattice-quantization family is retired as a B2 candidate, and B2's residual target RISES from 0.38 pp to 0.445 pp.** | PHASE8 §13.8 (§12 kept in place with a correction banner — its table is §13.8's input). |
| 07-24 | The `var=0`/`var>0` haste dichotomy is a **live, highest-priority B2 lead** (it has B2's exact haste × kill-boundary shape). | **Demoted on measurement, not on argument.** The shape claim stands — the cancellation proves model and sim disagree about *where in time* a haste window's benefit lands, invisibly to any single-axis probe. But its magnitude on the two B2 plans is **anti-B2 by 0.030 pp**. It is B2's shape without B2's sign. | PHASE8 §13.4 (as demoted), §13.8. |
| 07-24 | The round-3 sweep is **too CPU-expensive** to run alongside the acceptance round, so it must wait. | Never measured. One runner invocation is **0.716 s** ⇒ the whole 9-leg × 13-haste sweep is **~4 minutes**. The block was self-imposed for hours on an unchecked assumption. Same lesson as the sim-audit rule, applied to our own tooling: **time it before you schedule around it.** | PHASE8 §11 (rig note). |
| 07-25 | The isolation batteries keep **failing to find** B2 (six mechanisms "clean in isolation", §10) — so they need a better probe. | They are not failing, they are **excluding**. The signed residual (sim − corrected-count prediction) across 6 buffs × 13 hastes is **positive everywhere** — a real **0.369 pp level** the model gives away on a layout's worth of windows — but **no leg's haste slope clears its own noise**, and the two B2 plans carry **identical buff-window multiplicities**, so any haste-independent per-window error cancels *exactly* in `h40 − h70`. Scaling every slope by its B2 press count, at face value, in whichever direction it points, gives **0.048 pp** against a 0.445 pp target (**0.024 pp** dropping the R=197 instrument artifact) — ×9 to ×18 short. **B2 is not in per-buff valuation; it is a property of the layout** (window×window or window×kill). Positive result, not a null one: the batteries have a domain and B2 is outside it. | PHASE8 §13.9. |
| 07-25 | Round 3 never reached the **GCD floor** (max single-buff `m = 1.428` vs the floor at `m ≥ 1.5`), so the whole non-linear regime — where B2's h40 opener lives (IV+Zerk+MQG, `m = 1.655`) — was unmeasured. Stacked-haste interaction is B2. | **Half right, and the half that was right is a confirmation, not a bug.** The floor's *location* is now **certified against the sim**: three crossed sets cross at three different ratings (IV+MQG 64.25, IV+Zerk 215.05, MQG+Zerk 243.45), each bracketed to one rating point, and the sim's marginal slope turns over inside the *same* point the model's does in all three — while the singles, which never cross, kink nowhere. But the interaction `ΔI = I_sim − I_model` is **negative at 13/13 ratings for the triple** (−0.259 ± 0.032, t = −8.1) where B2 needs it positive; on the actual B2 pair it is worth **−0.114 pp** against a +0.445 pp target. Retired same-day per the pre-registration. Its mechanism is not new: `ΔI ≡ resid(S) − Σ resid(singles)`, so §13.9's per-buff under-credit simply **fails to stack** — it is a **per-window** constant (three buffs in one window carry ~⅓ of three buffs' worth, no R-trend) and therefore cancels in B2 by the same multiplicity argument. **Window×window is out; window × kill is the sole survivor** — the one axis every probe so far is blind to, because all of them measured steady-state fights that never end. | PHASE8 §14.6. |
| 07-25 | `--var 0` is the clean, noise-free way to measure a marginal (deterministic ⇒ no sampling error). | **Backwards — `--var 0` is unusable for marginals.** With zero jitter all 20 000 iterations are the *same* fight, so DPS quantizes to **integer cast counts**: R=64 and R=65 return byte-identical base DPS, IV and MQG return byte-identical marginals at four ratings, and Berserking's 10 s window measures **+0.03 pp** — *zero* extra casts — at five of thirteen ratings. It is the count law's `ceil` sampling at maximum exposure: **true**, but orders of magnitude below the resolution of the sub-cast quantity being measured. `--var 3.0` dithers the cast phase and recovers the expectation. (Second time this bit: it is also what faked "haste buffs are exempt from the floor law" in round 2.) | PHASE8 §14.6; TOOLING "`--var 0` quantizes to integer casts". |
| 07-25 | With per-buff valuation and window×window excluded, **window × kill** — where the buff sits relative to the fight's end — is B2's last surviving axis, and every probe so far was blind to it. | **Retired wrong-signed, the fifth firing of that clause.** The position sweep (5 buffs × 12 positions × 2 hastes, `T=100`, `var 3.0`) says the model's *error* barely moves with position at all: away from the last-second band, `max|resid(AT) − resid(30)| ≤ 0.035 pp` (≤ 0.018 at R=40). The two "live" flags are both **clip-boundary snap** — AP@84 (one cast short of its 85 s clip onset) and IV/MQG@78 — a band **neither B2 plan enters** (h40's last window closes at 220, h70's at 222, on a 229 s kill). And the pre-registered sign combination is **negative at both hastes** (−0.063 / −0.109 pp) where B2 needs positive. So B2 has **no candidate expressible in a single-buff fight at all** — an instrument verdict: stop decomposing, measure the h40/h70 pair directly (§16, the two-plan differential). | PHASE8 §15.5, §16. |
| 07-25 | §15.5 priced **`Icon` solo at exactly 0.000** and **`MQG` solo at ≤ 0.053 pp** at every position — so neither trinket can be carrying a 0.445 pp deficit, and B2 must be diffuse across the layout. | **★★ The pair is not the sum of its parts.** Round 6 stopped decomposing and walked the five-press path between the two real layouts in both directions: one move — the **trinket pair** (`Icon 29,183→4,182` + `MQG 9→202`) — carries **−0.325 pp of the −0.396 pp endpoint gap (82 %)**, same-signed in both directions, against a −0.02…−0.06 pp background on the other three moves. G4 leaves only a 0.063 pp joint residue (16 %); G5 says every move is context-asymmetric but only the pair's sign is stable, so the "diffuse" branch does **not** fire. The solo measurements were right and irrelevant: the defect lives in the two presses *moving together*. Structural by-product: the category-1141 lockout makes **every single-trinket intermediate illegal in both directions**, which is why five rounds of decomposition could never reach this move. | PHASE8 §16.5. |
| 07-25 | A hand-built probe spec is legal if nothing in the log **drifts far** from where it was requested (press lag is only ~0.05–1.2 s, so a big drift is the tell). | **Drift magnitude is not a legality test.** Two of round 6's four illegal plans were retimed by only **2.13 s and 2.00 s** — *under one unbuffed cast interval at gear haste 70* (`2.5/1.0444 = 2.394 s`). Any threshold loose enough to accept honest press lag also accepts a lockout retime. The order is now fixed: **a-priori structural check first** (trinket pairs ≥ 20 s; presses of one cooldown ≥ its cooldown), combat log **second, to confirm it**. Same round found the **cooldown chain**: `AP@[8,188]` is exactly 180 s apart, the first press boundary-snaps to 9.06, so AP isn't ready until 189.06 and the second fires at 189.51 — deterministic, and a structurally different plan than the one requested. | TOOLING ★★ ("verifying a hand-built spec — the order matters"); PHASE8 §16.5. |
| 07-25 | B2 is *"the trinket pair"* — two presses that only misprice jointly, so the mechanism must be an interaction between them. | **★★★ It is ONE PRESS.** Round 7 walked `M34` through a **parked** `MQG@100`, making every intermediate lockout-legal. Path additivity holds to **0.0007 / 0.0003 pp** (~70× under the bar), so the split is real; the **`Icon`-only step costs +0.0044 / +0.0027 pp** — §15.5's exact 0.000 survives context and **`Icon` is exonerated**; and the whole defect sits on `MQG 100 → 202`, arriving on `IV@202`: **−0.2106 / −0.4068 pp, i.e. 53–103 % of the gap**, carrying essentially all of the context asymmetry too (0.196 pp of it). Plainly: the model pays **+0.26…+0.36 %** for moving `MQG` onto `IV` at the end of the fight; the sim pays **≈ 0**. Two causes remain conflated — the **stack** (composing haste multipliers) vs the **kill** (a haste window with no runway left to cash its saved time) — and round 8 crosses them. | PHASE8 §17.5, §18. |
| 07-25 | The engine has **no shared-trinket-lockout model** — a real gap, since wowsims enforces one. | **A grep artifact, mine.** I searched `index.html` for wowsims' *category number* `1141` and for "shared timer" — the engine's own name for it is **`OFF_TRINKETS`** (`index.html:618`), and `simulate()` enforces it directly (`:760` a trinket can't start before the previous trinket's window ends; `:790` records that end), with `repair()`/candidate generation honouring it at `:1028`, `:3213`, `:3794`. The engine retimes an illegal press exactly as the sim does. **Lesson: never search one system for another system's identifier** — search for the concept, then for the local name. | `index.html:618/760/790`; TOOLING ★ (shared trinket lockout). |
| 07-25 | In a differential probe, "hold everything except X fixed" means the controls *are* fixed — the spec says so. | **A property of the request, not of the execution.** A press moving perturbs every **downstream boundary-snapped** press: across round 7's pure-trinket step the third Icy Veins slid **1.00 s** in one rest-context (and only 0.10 s in the other), while the a-priori legality check passed on all 8 plans. The confound was ruled out rather than merely bounded because the **clean** context carried the **larger** residual — but the general fix is a standing gate: for every contrast, verify the **non-moving** presses shift < 0.30 s, or report the contrast as contaminated. | TOOLING (the cascade gate, step 3); PHASE8 §17.5, §18.4 H5b. |
| 07-25 | *"Haste shortens a span but never changes its cast COUNT… so haste gains nothing from covering the ramp"* — the model's hardcoded position-independence axiom (`index.html:926-928`, RULES §3). | **★ True in the steady stream, FALSE on the ramp — and now measured.** The model side is exact: its haste marginal is flat in press position to **0.0000 pp** across the whole interior, so it credits ramp overlap *precisely* zero. The sim does not: `resid@AT=0 − resid@AT=10` is **positive on 6/6 haste legs** (IV +0.094/+0.092, MQG +0.098/+0.088, Zerk +0.040/+0.061 at R=40/70), mean **+0.079 pp**. On the ramp the cast interval isn't the steady-state one, so shortening it genuinely changes the count. It is ramp-**coverage**, not being-early — `AT=5` shows no bonus because the ramp ends at ~6.3 s and a press at 5 snaps to the *next* boundary, already past it. The phase's first right-signed structural finding since round 2 — **but not B2**: the only ramp-coverage difference between the two plans is `Zerk@0` vs `Zerk@6` on a ramp `IV@0` already covers in both, worth 0.009–0.027 pp against 0.445. (An earlier scaling of this said "h40 stacks two haste buffs on the ramp" — wrong, h70 presses `IV@0` too; corrected in place.) Recorded, deliberately **unpatched**: it would move goldens, and no scorer change lands mid-acceptance-round. | PHASE8 §15.5 F5; RULES §3 (the ★ MEASURED EXCEPTION bullet). |
| 07-25 | The `MQG 100 → 202` defect is plausibly **fight-end truncation**: the press sits 27 s from a 229 s kill, and the model over-credits a haste window with no runway left to convert its saved time into a completed cast. | **★★★ Falsified — it is the STACK, not the kill.** Round 8's 2×2 (`Icon` off, so `MQG` is the only on-use trinket and the crossed design is finally legal) puts the **identical press at the identical position with 78 s of runway** and it *still* costs **−0.298 pp — 81 % of the kill-adjacent version's −0.367 pp**. Meanwhile a **solo** haste buff pressed *at the kill* is priced correctly: **+0.0115 pp**, model and sim agreeing to 0.012 pp. So the fix does **not** live in how the integral terminates at `T`; only ≈19 % (`K1−K3 = −0.069 pp`) is a residual kill term. Both model calibrations agree. One clause is still untested — the stacked-on-`BL` cell was **contaminated** by the cascade gate (`AP#2`/`Zerk#2` slid −0.89 s) and carries no verdict. | PHASE8 §18.6. |
| 07-25 | The model must be **missing or mis-applying the GCD floor** when two haste buffs compose — that would explain crediting a stacked window the sim says is worth nothing. | **★★ No: the scorer clamps correctly, and the log says the floor is exactly *why* the sim pays zero.** `index.html:819-822` / `:903-904` compute `interval = max(cast, max(GCD_FLOOR, GCD_BASE/m))`, and the combat log confirms the physics: in the stacked arm the casts run **exactly 1.000 s apart from 202.13 to 222.13** (unfloored `IV×MQG×gear = 1.5157` ⇒ `1.5/1.5157 = 0.9896 s`, so ~1.05 % of the stacked haste is clipped), while solo `MQG` runs at 1.195 s with nothing clipped — **the floor is only reachable by stacking, which is why the defect is stack-conditional.** The sim's window census is *exactly* symmetric (solo `MQG` **+3 casts**, stacked `MQG` **+3 casts**, fight total **230 = 230**), and `AP`'s 15 s value window covers **15 cast starts / 14 contained casts in both arms** — the floor law surviving a case where **Δ itself** was manipulated. The model books **+1 net cast (229 → 230)**. The live hypothesis is therefore subtler than a missing clamp: a *continuous* cast-rate integral banks the **fractional** +0.38-cast advantage (≈ +0.17 pp) that the sim's discrete chain re-absorbs, plus an `AP`-packing credit (≈ +0.12 pp) worth zero in the sim — together reconstructing the observed 0.29 pp. **Hand-arithmetic on log-read intervals, not an instrumented result**, and per CLAUDE.md a clean model number disagreeing with a sim number is a **sim-setup audit trigger first**. | PHASE8 §18.6; `index.html:819-822`, `:903-904`. |
| 07-25 | The acceptance **collector** is a settled instrument — it has produced every ledger since Phase 6, so reading round 4 with it is a formality. | **★★ It could report `PASS ✓` on ZERO data, and did.** A dry run of `node tools/xval-collect.mjs <scratch dir>` over a copy of the 35 complete round-4 tables printed `## Cross-val ledger (0 tables, 0 borrowed-win columns)` → `**Invariant B:** ZERO deficit columns — PASS ✓`. Two defects: (1) the positional-arg filter `args.filter((a,i) => a !== '--json' && i !== jsonIdx + 1)` drops **argv[0]** when `--json` is absent, because `jsonIdx === -1` makes the test read `i !== 0` — the supplied directory was discarded and the default (then holding only a README) silently used; (2) an empty read still fell through to the `totalCols === 0 ⇒ PASS` branch. Both closed: the flag/value pair is excluded only when the flag is actually present, and zero tables — or zero *parseable* tables — is now a hard error with **exit 2**. The bug never bit before only because the documented command passes the directory that happens to **equal** the default. Sibling `tools/xval-verify.mjs` uses `process.argv[2]` and was never affected. **Lesson: an instrument whose failure mode is a PASS must be dry-run against known-nonempty data before its verdict is believed** — same family as the round-8 legality checker below, one level up: there the tool wrongly *rejected*, here it wrongly *accepted*, which is the far more dangerous direction. | `tools/xval-collect.mjs:13-31`; ACCEPTANCE "The instruments". |
| 07-25 | A structural legality checker written for a probe can be trusted once it passes; a rejection means the *plans* are wrong. | **The checker is as much a suspect as the plan.** Round 8's first run rejected all six arms with `IV@0 -> IV@20 (< 180s CD)`. The plans were fine — the checker omitted the **Cold Snap exemption** (`CS@20` resets Icy Veins' cooldown, which is the entire point of the CS chain). Same family as the `1141`-vs-`OFF_TRINKETS` grep artifact the day before: **when a tool contradicts a design you have reason to trust, audit the tool first.** | PHASE8 §18.6 (H5). |
| 07-25 | `optimizeAsync` returns `{val, s}`, so `val` **is** the score of `s` — the effective-AB count on screen describes the plan on screen. The project's one arbiter is self-consistent by construction. | **★★★ It is the score of a plan the tool does not ship.** All three resolve sites carry `val` across a transform of `s` that is never re-scored: `:2919`/`:2921` (Cold Snap) apply `canonicalWindowOrder(normalize(…))`, `:2928` (plain) applies the canonicalizer. `canonicalWindowOrder` is exonerated — it re-anchors each iteration and gates at `castVal/1000` ≈ 0.001 casts (`:2770-2771`). The leak is **`normalize()`** (`:2785` = `dodgeDowntime ∘ slideEarliest ∘ spreadLoneHaste ∘ coPressAlign`) running **after** the last `simulate`, where `coPressAlign` spends a *designed* pressability trade of up to `castVal/8` = 0.125 casts (`:2780`) that the reported `val` never learns about; four stacked transforms is why the worst exceeds any single bar. Measured over all 25 shipped presets: **4/25 drift, worst over-report 0.1533 eff casts (Al'ar), and all four on a Cold-Snap path** — none on the plain path, so the prevalence pattern confirms the root cause independently of reading the code. Drift is **bidirectional** (2 over, 2 under), so it is not a constant to subtract. The same function already does this right at `:2792-2796`, where the fixpoint re-scores. **Does NOT corrupt the acceptance rounds** — `xval.mjs` takes `.s` and re-scores it (`:121`, `:125-126`), so rounds 1–4 stand. **Did enable a false pass in `brute-grid --tool`**, which graded `best.val` against a `simulate`-computed grid optimum under a 0.15 band ≈ the 0.153 worst error (fixed: it now scores the emitted plan). Bearing on invariant B is plausible — "chosen on an inflated score, emitted slightly worse" is the deficit signature — but **unproven**, and the fix is gated on exact-match 25/25. | PHASE7 §5.14; `index.html:2919/2921/2928`, `:2780`, `:2785`. |
| 07-25 | The 07-25 audit's 36 false-pass defects were the harness's *tooling* problem; the cross-val instrument at the centre of it had been repaired and could be trusted. | **★★ `xval.mjs` itself held two of them, and both were demonstrated on HEAD rather than argued.** (1) **NaN → double PASS**: the runner line was parsed with an unvalidated `parseFloat`, and both invariant loops compare with `>`, which is **false for NaN** — so an all-`NaN` matrix printed `monoDip=0.00%`, `DIAGONAL DOMINANCE: CLEAN`, exit **0**, and a well-formed `XVAL-DONE` line a collector would ingest. Reproduced with a runner stub emitting one warning line. (2) **The legacy `intermission:[from,to]` field was dropped** — only `p.phases` was read, but *The Lurker Below* and *High Astromancer Solarian* ship with the legacy form, so their downtime vanished from **both** sides at once (model planned a plain fight, genapl cast through the untargetable window) and nothing looked wrong. On Lurker: `eff` **129.588** with the window deleted vs **101.674** with it restored — a 27 % inflation, graded `CLEAN`, filed as `class=BOSS:Lurker`. Five more of the same family closed alongside (mistyped `TCLASS` → generic length band; `HASTES=""` → coarse default, and `xval-kit.sh` builds it from a `python3` lookup that can fail; `WJITTER=""` → `+'' === 0`, since `??` does not catch empty string; non-numeric seed → seed 0, making a campaign's rows identical but independent-looking; `KIT` with duplicate/unknown/extra keys). **Lesson extended: "dry-run against known-nonempty data" is not enough — the instrument must also be run against known-BAD data and required to fail.** | `tools/xval.mjs`; ACCEPTANCE "The instruments". |
| 07-25 | I had just written the false-pass lesson down, so I was inoculated against it. | **★ I reproduced it the same day, in my own throwaway script.** The first version of the `val`-drift sweep called a function that did not exist; all 25 presets threw, and the summary line still printed **"0/25 presets where reported val != simulate"** — a confident CLEAN over **zero graded rows**. It was caught only because the per-preset error lines happened to be printed alongside. Knowing the class does not protect against it; only the guard does. The rebuilt instrument exits 2 on pageerror, on any preset erroring, and on zero rows. | PHASE7 §5.14 (instrument note). |
| 07-25 | The false-pass audit was about the *leaf* instruments (`xval-collect`, `xval`, `brute-grid`). The four **shell wrappers** only launch them in order and echo what they print, so there is nothing in them to get wrong. | **★★ Every wrapper converted a child's exit 2 into a green banner — demonstrated on HEAD, not argued.** `git show HEAD:tools/xval-kit.sh` run against a nonexistent kit printed five `XVAL-FAIL` lines and then **`KIT-DONE bogus1`, exit 0**; HEAD's `xval-campaign.sh` reaped three exit-2 children and printed **`CAMPAIGN-DONE`, exit 0**. Neither ever consulted `$?`. Three more in `xval-rerun.sh`, the driver that produces the acceptance evidence: (1) **`wait $PIDS` returns the LAST pid's status only** — replicated standalone with two children (first `exit 2`, second `exit 0`): per-pid accounting saw 1 failure, `wait $PIDS` returned **0**, so a first kit that died was invisible; (2) the **restart-skip probe checked only the two ENDPOINT classes** (`short`, `xl`), so a kit whose middle classes had failed looked complete and was skipped *forever*, leaving the re-run three tables short with nothing in the log saying so; (3) **`git push … || true`** — the scratchpad is ephemeral and these tables *are* the acceptance evidence, yet a push that never reached the remote was still called a durable checkpoint. All closed: `clean=/deficit=/failed=` banners, a `*-INCOMPLETE` line + **exit 2** on any child failure, exact-count reaping (draining on `jobs -rp` misses jobs that already exited), per-pid accounting, all-five-classes skip probe, and a 5-attempt push retry that is LOUD on exhaustion. **Exit 1 stays unused by design on this path**: `xval.mjs` gathers data and "draws NO conclusions", so a `diag=DEFICIT` is an *observation*, not a failure. Both halves of the lesson were then run: seven negative controls (each required to exit 2) **and** a positive control on healthy data — `scb,mqg` × all five classes, `monoDip=0.00% diag=CLEAN` throughout, `KIT-DONE … clean=5 deficit=0 failed=0` → `CAMPAIGN-DONE kits=1 failed=0`, exit **0** — so the new guards reject bad data without rejecting good data. | `tools/xval-{kit,boss,campaign,rerun}.sh`; ACCEPTANCE "The instruments". |
| 07-25 | A spec→APL generator is a pure formatter; a mistyped spec key is a *user* error and would be obvious in the emitted JSON. | **`genapl.mjs` dropped unknown keys in total silence** — the harness's dominant failure mode wearing a new hat. `{IcyVeins:[20]}` (or `icon`, or any typo) emitted a **well-formed APL with that cooldown simply ABSENT**; the sim ran happily, printed a plausible DPS, and every comparison built on it was wrong with nothing anywhere saying so. Sibling `genae.mjs:18` had had exactly this guard all along. Two more closed alongside: a **missing or empty CLI spec** fell through to a silent **exit 0 having written nothing**, so `node genapl.mjs "$spec" out.json` with an unset `$spec` left a **stale `out.json`** for the runner to sim under *this* experiment's name; and non-finite press times formatted straight through `` `${t}s` `` into the schedule string ("nulls", "NaNs"), worth nothing but indistinguishable from a scheduled press. `explore.mjs`'s `simDPS` held the same unvalidated `parseFloat` that was `xval.mjs`'s worst defect, wired the other way: NaN makes every comparison false, so the verdict falls through to `‼ DISAGREE (real ranking flip)` — a false **alarm** rather than a false pass, which corrupts the P4.measure gate just as thoroughly. Behaviour-neutrality of the `genapl` guards proved by **5/5 byte-identical** emissions vs HEAD across specs covering every key (full trinket set + intermissions + aoe, bare BL, `_prestack:2`, legacy `_intermission`, multi-window `_aoe`). | `tools/genapl.mjs`, `tools/explore.mjs`; ACCEPTANCE "The instruments". |
| 07-25 | The false-pass class is about instruments that wrongly say **yes**; a tool that *rejects* is at worst annoying, and a certification tool that grades the optimizer's own reported score is grading the optimizer. | **★★ Both halves are wrong, and the last three audited tools proved it.** (1) **`genconserve.mjs` rejected a key it documents.** `_noAutocast` is **read** by `build` at the autocast step but was **missing from the known-key set**, so the guard meant to catch typos threw `unknown spec key(s) _noAutocast — would be silently dropped from the APL` on the one flag `docs/TOOLING.md:75` lists as supported — demonstrated on HEAD (`D1 THROWS: …`). **A guard that over-rejects is the same defect class as one that under-rejects**; it merely fails loudly, which is exactly why nobody chased it. (2) **`haste-ladder.mjs` certified the optimizer against a number the optimizer does not ship.** It graded `best.val` under a `0.15` pressability-slack band — the same width as the worst `val`/emitted-plan drift that used to leak out of `optimizeAsync` (0.153 eff casts, row above), so a genuine miss could hide inside the slack while the reported score looked clean. It now grades `simulate(best.s, cfg).robust`, matching the `brute-grid --tool` fix, so the certification no longer **depends** on the engine re-scoring; neutrality proven not asserted — a purpose-built equivalence probe (itself exiting 2 on pageerror / non-finite / zero rows) gave **7/7 ladder cfgs h=0…300, 0 with drift, worst `0.00e+0`**. (3) Two `genapl.mjs` defects verbatim in `genconserve.mjs` — a missing/empty CLI spec exiting **0 having written nothing** (demonstrated: `D2 EXIT=0`, outfile still reading `{"STALE":"previous experiment"}`, i.e. the runner would sim the previous experiment's rotation under this one's name) and non-finite press times formatting through `` `${t}s` `` — plus `ladder-analyze.mjs` returning **exit 1** ("graded and failing") for *no input files*, which a caller whose glob expanded to nothing read as a real negative verdict; now exit 2. Six negative controls each exit 2, positive control **6/6 byte-identical** vs HEAD across specs covering every key. **This closes the 07-25 false-pass sweep: all 14 audited harness tools remediated.** | `tools/genconserve.mjs`, `tools/haste-ladder.mjs`, `tools/ladder-analyze.mjs`; ACCEPTANCE "The instruments"; TOOLING (genconserve ★). |
| 07-25 | PHASE9 §4.3: comparing schedules by signature instead of `JSON.stringify` is **"exact and ~20× cheaper. Pure win, tiny blast radius. Good first landing"** — the top of the performance landing ladder. | **★★ Wrong three ways, and the headline claim has the sign backwards.** (1) *Site count*: not "at least three" but **twelve** comparison sites, in two shapes (four already-hoisted `curSig` sites + eight in-loop). (2) *"Exact"*: **false as stated** — `sigOf` sorts keys, `JSON.stringify` preserves insertion order, and `repair` **demonstrably reorders keys** (it emits the shared-lockout trinket group first, `index.html:1033`/`:1061`). It happens to be exact *here*, but only via a five-step argument §4.3 never gave: `repair` preserves the key set, its output order is a pure function of (input order, `cfg.fixed`), hence `R(R(x)) = R(x)`; every schedule in circulation is a `repair` output (`cloneS` is `for…in`, order-preserving); mutators change values, never keys ⇒ both operands always share key order. Backstopped empirically by an instrumented build over the whole corpus: **228,685 equality tests, 0 disagreements**, with the 25 emitted plans still matching `golden.json` (the probe returns the JSON answer, so it is its own positive control). (3) *"~20× cheaper"*: **measured 2.9× SLOWER** (`sigOf === sigOf` 2.580 vs `JSON.stringify === JSON.stringify` 0.879 µs/test) — betting a hand-rolled JS string builder against native serialization. **The proposed half is REJECTED; only the un-proposed half — hoisting the loop-invariant operand out of the candidate loop, 1.72×, byte-identical by construction — lands, and at just 4 of the 12 sites where the enclosing loop is actually long.** The same bench then found the real win: **native `JSON.stringify` is a cheaper memo key than `sigOf`** (0.483 vs 1.400 µs × 1.98M calls/solve ≈ **1.8 s CPU/solve**), which also retires the ladder's "cheaper hand-rolled encode" item — the answer was *delete* the hand-roll. **Lesson: a perf claim written without a measurement is a hypothesis, not a plan item, and belongs in the hypothesis table with a verdict column — not at the top of a landing ladder where it reads as settled.** | PHASE9 §4.12–§4.13 (revised ladder in §4.13.1); §4.3/§4.8 annotated in place. |
| 07-25 | Row 201's lesson was learned: I now write instrument guards *first*, so the EQ probe — whose builder asserts a hit count for every rewrite and whose runner exits 2 on zero recorded calls — is trustworthy once it reports 0 disagreements. | **★ Third first-person instance in one day; the guards I wrote were the wrong guards.** The probe classified each disagreement by direction with `site + (j ? ':JSONdiff/SIGsame' : ':JSONsame/SIGdiff')` — **inverted**, since `j` true means *JSON said same* — so every disagreement would have been filed under the opposite name, and the dangerous direction (a `SIGsame` that makes the engine skip a candidate it evaluates today) is exactly what a reader takes off that label. Found in seconds by a **negative control** that feeds `EQ` the one pathology the swap risks (identical key→values map, reversed key insertion order) and requires it to be counted, in the right direction, at the right site — then feeds it two agreeing pairs and requires it *not* to count them. The corpus verdict survived only by luck (`__EQ.dis` increments *before* the label is computed, and was 0, so no label was ever emitted). Its sibling: the accompanying micro-bench's first draft stringified **both** operands in its "hoisted" variant — the same work as the baseline — and reported `1.01×`, i.e. a confident "not worth it" about a transform it was not testing. **Lesson: hit-count assertions prove the instrument *ran*; only a negative control proves it *discriminates*. Both are required, and neither is optional at 30 lines** — minutes of control against a 16-minute corpus run whose output you would otherwise simply believe. | PHASE9 §4.12.6–§4.12.7; `$SP/p9/negctl.mjs` (scratch). |
| 07-25 | `--var 3.0` dithers the fight and 20 000 iterations average the noise out, so a `var 3.0` model-vs-sim contrast is phase-neutral — which is why round 9's whole design compares the model's **phase-averaged** press model against it. | **★★★ `--var` dithers DURATION, not press PHASE — and cast times depend only on haste, so no number of iterations averages phase away.** `index.html:762-785` says the engine *deliberately* fires a steady-state press at the intent time and prices snap as an *expected* `slip = prevInterval/2` ("the phase-averaged start the scoring integral wants IS the press moment"); the sim realizes **one** phase, and the two arms realize **different** ones because the treatment re-phases its own controls (C0 `MQG@100` adds ~3 casts before 162, so `BL` fires 162.05 vs C1's **163.26** — a **1.21 s differential** in a supposedly-fixed control). Measured cast-for-cast at zero sim cost: physics agree (first AB 1.995 s = `2.5/(1.20×1.0444)` ✓, quiet interval 1.43588 vs 1.43623 ✓) and the streams diverge **only at press seams** — C0 ends **+0.479 s behind** the sim, C1 **−0.055 s ahead**, and that 0.53 s differential reproduces K3's `Δresid = −0.2978 pp` exactly (`d_model` +0.2902 % from `robust`, `d_sim` −0.0076 %). Two named mechanisms: a chained `IV#2` executed at the boundary 20.85 buys the sim **40.85 s** of fast casting for a 40 s window, and C1's slid `BL` tail overlaps a floored `IV+MQG@202.13` cluster and is wasted. Round 8's `K1−K3` survived only because a **second** difference cancels the drift; a first difference does not. | PHASE8 §19.7.2–§19.7.3; `$SP/p8/r9align.mjs`. |
| 07-25 | Round 9's apparatus was written before any number existed, so it cannot be tuned to its answer — the pre-registration *is* the safeguard. | **Pre-registration protects against tuning, not against a broken instrument — and both defects were in the reduction.** `r9drift.mjs` printed `0.000` (**clean**) for two EMPTY logs, because a max over zero presses is 0; `r9l1stat.mjs` printed *"NEITHER falsifier fires"* plus §19.3's audit-trigger clause over an **empty row set** (`!(NaN < CTOL)` is true, so every point is dropped and the verdict still prints). And §19.4's own honesty clause — *"gate-dirty haste points are excluded from every verdict"* — would have excluded **100 % of points** by construction, since the `BL` drift is 1.21 s against `CTOL = 0.30`: a vacuous round that would have *looked* rigorous. Fifth and sixth first-person instance of the class in one day. Gate replaced by `r9press.mjs` (NODATA/COUNT/DRIFT on the 0/1/2 contract) and proven by `r9pressctl.sh` — **8/8, four of them required to FAIL**. | PHASE8 §19.7.1; `$SP/p8/r9press{,ctl}.{mjs,sh}`. |
| 07-25 | The press-phase confound of the row above **reproduces** K3's `Δresid = −0.2978 pp` exactly, so K3 — and plausibly the whole B2 family — is that confound: a single-phase measurement artifact, with B2's 0.40 % and the acceptance low-haste slack possibly at or below phase noise. | **★★ Reproducing a residual at one operating point is not explaining it — phase is worth only 23 %.** The pre-registered L0 leg δ-averaged the *sim* over a full cast interval (12 δ × `u = 1.436 s`, both arms, `T=300`, `var 3.0 iter 20000`, cold open, Icon OFF; all 12 points gradeable): **mean `Δresid` = −0.2280 pp** of the −0.2978 at δ = 0, so **L0b fires — the defect is REAL** and §19.1's candidate stands. Three things fell out, two of them the *opposite* of the hedge: (1) `sd(d_sim) = 0.0492 pp`, five times under the 0.25 pp note threshold — **press phase is NOT a large noise source, so B2's 0.40 % and the low-haste slack are not noise artifacts**; (2) `sd(d_model) = 0.0171 pp` **certifies** `index.html:762-785`'s slip-invariance claim, which narrows the hunt to a **level** error the scorer makes *equally at every phase* — no re-timing fix can close it; (3) phase-averaged the sim **agrees with the model on the sign** but is **5.3× smaller** (`+0.053 %` vs `+0.281 %`), and the apparent *ranking inversion* exists at only 3 of 12 phases — so the target is "the model over-credits a stacked press ~5×", not "the model reverses a ranking". Structural bonus: the sim's executed schedule is a **step function of δ** (6 distinct executed configurations across 12 δ, `k=4…9` identical to the centisecond), which is correct snap-to-boundary behaviour and makes uniform δ-averaging the right reduction — and is distinguished from a dead sweep on the *executed* presses (`BL` spans 1.440 s vs `MOVETOL = 0.50`), which is exactly the false-pass L0 was built to be unable to fake (`r9l0ctl.sh` **13/13**, six required to exit 2 and **three positive verdict-discrimination cases**). **Lesson: a mechanism whose size matches a residual is a candidate, not a cause; vary the mechanism and require the residual to move with it.** *(Input-side sibling of the false-pass class, caught by a guard this time: the model leg's spell-power knob was `SP`, which is the scratchpad path in every shell here, so it scored `sp = NaN`; the output guard fired and nothing false was reported. Knob renamed `MODSP` and inputs now validated **by name at parse**, not only outputs at point of use.)* | PHASE8 §19.8; `$SP/p8/r9l0{build,model,stat}.mjs`, `r9l0{,ctl}.sh`. |
| 07-25 | §19.7.5 item 3's own wording: the second, independent phase removal is *"score the **executed** press times"* — feed the sim's realized presses into `simulate()` and read the residual. | **★ Removing a confound with an instrument that re-adds it: scoring executed times through the stock engine DOUBLE-COUNTS the snap.** `index.html:785` adds `slip = prevInterval/2` whenever a press lands mid-cast — the *expected* snap of a press whose phase is unknown. An **executed** time has already snapped, so its residual expected snap is **zero**, and at `R = 70` that spurious term is ≈ **0.72 s** of stream time per press — *larger than the ≈0.3 pp confound being removed.* Taken literally, item 3 would have reported a bigger error than the one it corrected, and reported it as the round's trust anchor. Fixed by suppressing exactly that term in an **in-memory** copy of the engine (read `<script id="engine-src">`, replace the single occurrence, `new Function(...)` in an isolated scope — the page's own Worker trick), so §19.5's freeze on `index.html` holds and the page's `simulate()` stays available as the positive control. With that, the two routes **AGREE**: aligned **−0.2122 pp** vs L0's δ-averaged **−0.2280**, gap **0.0158** against a pre-registered `AGREETOL = 0.10` (chosen *below* `REALTOL = 0.20` so agreement could not flip L0b either way) — so L0b is a property of the scorer, not of either reduction. Three unplanned corroborations: `maxMove = 0.006 s` (the model demonstrably fires where fed, via `actEff`); residual spread **halved** (sd 0.024 aligned vs 0.047 as-is) — what removing a phase confound *should* look like; and `castCount` `230→230` at **every** δ, confirming §19.7.3's "+1 cast" was a symptom of stream drift, not a mechanism. Controls 5/5 driven + 3 in-page, the load-bearing one being negative: *the patched engine must disagree with the unpatched one*, else a silently-missed replacement would report the **unpatched** model as "phase-aligned" — a false pass aimed at the round's own cross-check. **Lesson: when an instrument is built to remove an effect, prove it removed it and did not quietly re-add it — and check the correction's size against the thing being corrected before trusting the leg.** | PHASE8 §19.9; `$SP/p8/r9l0align.mjs` → `r9l0/align.json`. |
| 07-25 | §19.8's target statement, carried into ROADMAP: *"the model over-credits a stacked press ~5×"* — a **level** error, sized ≈0.23 pp, that the scorer commits equally everywhere (`sd(d_model)` = 0.017 pp across press phase certified the phase-invariance, so the level reading looked safe to generalize). | **★★ It is a CROSSING-LOCATION error, not a level error — and `R = 70` was the single most adversarial haste in the range.** L1 swept `R ∈ {0,30,53,70,120,200,300,400}` on the same K3 contrast: the model reproduces the *entire* haste dependence of stacking — the sign flip and the slope (`d_sim` **+0.3991 → −1.0505 %**, `d_model` **+0.3307 → −1.0107 %**) — with **mean `Δresid` = −0.045 pp (sd 0.086)**, i.e. essentially **no average bias across haste**. Neither pre-registered falsifier fires: **L1b fails** (max |Δresid| below the crossing 0.1163 < 0.15, so §18.6's floor story is **not** retired) and **L1a fails on sign** — Spearman(clip %, |Δresid|) above the crossing is **−0.800**, not ≥ +0.8, because the residual **peaks AT the crossing** (`R=70`, `u_stack = 0.9898`, −0.1808 pp) and decays to 0.014–0.046 pp once clipping is deep. Absent below a boundary, maximal *at* it, absent well above it = a **discretization** disagreement at the boundary (continuous rate integral vs THE FLOOR LAW's `floor(D/Δ)`), so the floor is at most a *component* and the mechanism is mixed. What remains is one sentence: the sim's sign flip lies in `(53,70)`, the model's in `(70,120)` — **one grid step late**, ≈20–50 rating too pro-stacking. And gating on the sim's own noise (`sd(d_sim)` = 0.0492 pp, bar = 2 sd), **the model inverts nothing the sim can resolve: 6 AGREE / 0 DISAGREE / 2 ties** — both alleged inversions sit at `d_sim` = **−0.0076 %** and **0.0000 %**, 6× and ∞× below the sim's ability to tell the arms apart, where there is no rank to invert. L0's phase-invariance finding still stands; what was wrong was **generalizing a single haste point into the model's bias**. Verdict identical on the REQUESTED reference column (0.1363 pp, ρ = −0.700), so it does not depend on the phase treatment. **Lesson: a residual measured at one point on an axis is a point estimate, not a bias — sweep the axis before naming the mechanism, because the point you first noticed is disproportionately likely to be the worst one on it.** | PHASE8 §19.10; ROADMAP round-9 block; `$SP/p8/r9l1{,model,stat}` → `r9l1/{model,l1}.json`. |
| 07-25 | A synthetic "healthy" fixture only has to be **well-formed** — nudging each requested press to `t + 0.05` produces a plausible-looking executed-press record for the L1 control battery. | **★ A fixture must be realistic in the very dimension its guard measures — otherwise a correct guard looks broken.** Those nudged times are **mid-cast**, a shape **no gate record can contain**: a sim can only fire an on-use *between* casts, so every executed time sits on a cast boundary (which is *why* L0 measured `maxMove = 0.006 s`). Fed mid-cast times, the model correctly snapped them forward by up to a full cast (**1.367 s** at `R = 0`) and the alignment guard correctly refused to grade — the **fixture** was wrong, and the first instinct (loosen the guard) would have destroyed the primary column's only protection. Fixed by `r9l1fixgen.mjs`: derive the times from the engine's own cast boundaries, iterate to a **fixed point**, then round to the sim log's **2 dp** — deliberately, so most presses land a few ms inside a cast and the fixture still exercises §19.9's slip patch instead of trivially bypassing it. The rounding cost **0.0036 s**, an unplanned corroboration that L0's real 0.006 s is exactly the scale boundary-realistic times should show. The old naive shape is **kept** as the negative control (`$F/midcast`) that must still produce `NOT ALIGNED` at the production tolerance — without it the generator would be able to talk the guard out of firing, which is the false-pass class again. Sibling finding the same day: *"the slip patch must change the score at **every** (haste, arm)"* was an **over-rejecting** guard — at `R=300/C1` no press lands mid-cast, so the patch is *provably inert there*; materiality had to become a **sweep-level** requirement (12/16 on real data) plus an unconditional **textual** proof that the replacement took. **Lesson: when a guard fires on data you built to be healthy, suspect the fixture first — and prove discrimination with a separate, deliberately-bad fixture at the production tolerance, never by relaxing the tolerance.** | PHASE8 §19.10 "Instrument"; `$SP/p8/{r9l1fixgen.mjs,r9l1ctl.sh}` (20/20). |
| 07-25 | Round 9's leg L2 asks whether the surplus is a duration-truncation artifact, and its negative verdict **L2b** is stated as *"the var-0 sweep is flat"*. A sweep that comes back flat is therefore the answer — the controls that mattered were the ones already built for L0/L1 (gate, press alignment, slip suppression). | **★★★ A VERDICT WHOSE EVIDENCE IS AN ABSENCE IS THE MOST DANGEROUS INSTRUMENT SHAPE THERE IS — a flat reading and a BLIND instrument are the same number, and the blind one exits 0.** Both pre-§19.7 L2 scripts had exactly that hole in different places: the shell would have read a `--dur`-quantizing runner as a flat sweep (four identical fights ⇒ L2b, manufactured), and the reducer turned a **missing var-0 leg** into `sw0 = NaN`, making `NaN>=0.30` and `NaN<0.10` *both* false — it printed "both falsifiers fail" and exited **0**, having graded nothing. The fix is a **positive sensitivity control**: L2b may not be reported unless the same instrument, *in the same units and at the same bar*, has first demonstrated it can SEE the effect whose absence it is about to report — here the var-0 LEVEL sawtooth must clear L2b's own 0.10 % bar (it clears it **2.8×**), and that bar must itself clear 5× the DPS print quantum's swing floor (0.1 DPS ⇒ 0.0038 pp per contrast, 0.0076 pp per swing). The load-bearing negative control is `flatlevel`: a *perfectly flat contrast* whose levels are also flat, on which L2b would otherwise "fire" and must instead refuse to grade. Two more L2-specific controls fell out: **CONTROL 3** — moving only the fight's *end* cannot move a press before it, so drifting presses mean the sweep varied the plan, not just the duration; and **CONTROL 4** — L2a is *half-satisfied* by `swing(d_model) < 0.05`, which a model that ignored `T` entirely would meet perfectly, so the model's level is required to RISE across the sweep (0.3546 %, bar 0.05 %), with a `FREEZET=1` knob injecting exactly that T-blind fault. And the leg's own model script repeated §19.9's mistake: it scored the presses we **asked for** (`d_model` 0.2902 %) rather than the ones the sim **ran** (0.1732 %) — **68 % too large**. The rewritten pair then answered the question properly: truncation is **real and large** (level sawtooth 0.276–0.284 %, 38× the floor) but **97.3 % common-mode**, cancelling to 0.0076 % = 1.0× the print quantum — because the MQG window ends ≥98 s before the kill and no haste buff is live in either arm after `t≈222`, so there is no *differential* terminal phase for §19.1's mechanism to act on. **Lesson: before reporting that an effect is absent, make the instrument prove it could have seen it.** | PHASE8 §19.11; ROADMAP round-9 block; `$SP/p8/{r9l2.sh,r9l2model.mjs,r9l2stat.mjs,r9l2fixgen.mjs,r9l2ctl.sh}` (32/32). |
| 07-25 | §18.6's J1, carried through rounds 8–9 as a live suspect: *"the model mis-composes two overlapping haste multipliers"* — a **buff-composition** defect that should show up wherever two haste windows overlap, with the larger multiplier making it worse. Round 8 could not test the `BL` clause (`K2` was voided by a −0.89 s control cascade), so the clause stood unfalsified. | **★★ Too broad on the one clause it never tested — and its repair UNIFIED the family instead. `Δresid` is a function of the CLIP FRACTION alone; the stacking partner is irrelevant.** L3 re-ran the cell properly (MQG@170 *entirely* inside `BL` 162–202 and touching nothing else, against a matched solo MQG@140 reference 30 s earlier, both runways ≫ K3's 78 s so kill-proximity is out of the design). At `R = 70` **`Δresid` = +0.0159 pp** against L3a's 0.15 bar — **stacking onto `BL` is priced CORRECTLY** while stacking onto `IV` is not (**−0.1808 pp**, same haste, same instrument), so the distinguishing feature is **not "stacking"** and L3b fires. Then ★ **L3c, THE UNIFICATION**, fires too: across the whole sweep `max |Δresid|` = **0.0396 pp** (bar 0.10) with mean **−0.0187**, only **0.0120 pp** from L1's well-clipped mean −0.0067 (tol 0.06) — and this is decisive *because of the geometry*: `BL`+MQG (×1.30 × ×1.209) is **floored at every gear haste in range** (its crossing is at `R = −72`, unreachable), so the BL contrast **never visits** the crossing where L1 localized the defect, yet its residuals lie on **L1's own curve** (clip ranges 4.6–23.9 % vs 0–17.6 %). **`K2` and `K3` unify and buff composition is RETIRED as a separate suspect**; what survives as B2's cause is §19.10's single finding — a **crossing-location** error, the model's sign flip one grid step late. Secondary: the repaired like-for-like number **+0.0352** vs round 8's voided **+0.0402**, so that contamination was **real but immaterial to the answer** (non-promotable — `T = 229` vs `T = 300`). Both scoring columns agree on all three verdicts, so none depends on §19.9's phase treatment. **Lesson: a clause that survives because it was never tested is not a surviving hypothesis — and when the untested cell finally reads clean, ask what the two cells have in COMMON rather than patching the hypothesis to exclude the new one.** | PHASE8 §19.13; ROADMAP round-9 block; `$SP/p8/{r9l3.sh,r9l3model.mjs,r9l3stat.mjs}` → `r9l3/{model,l3}.json`. |
| 07-25 | §19.12 pre-registered a **reference-invariance control** for L3 — `Δresid(C0→D1) − Δresid(C0→D0)` must equal `Δresid(D0→D1)` within 0.02 pp — reasoning that if the verdict were an artifact of *which* solo press was chosen as the reference, this check would expose it. Written before any L3 number existed, so it looked safe by construction. | **★★ A "control" computed from the SAME numbers as the thing it checks is an IDENTITY, not a measurement — and that is the false-pass class round 9 exists to hunt.** All three contrasts are formed from the same three per-arm DPS values, so the check is algebraic up to a `C0`-vs-`D0` denominator worth ≈**2e−5 pp**; it reads **0.0001 pp** on the real data and **could not fail for a measurement reason.** Caught while writing the control battery, i.e. by asking of my own pre-registration the question the battery asks of every guard: *what data would make this fire?* Kept (it does catch arithmetic corruption) but relabelled **WEAK — cannot carry the claim**, with the reasoning in-comment so no later reader re-promotes it; and the **substantive** form added — re-read the same falsifiers against the **other** solo reference, `C0→D1`, which is a genuinely different sim pair the primary never touches (real data: `−0.0030 pp` at `R = 70`, max 0.0512, **same three verdicts** ✓). The distinction is then *demonstrated, not argued*: the `refinv` fixture moves **`D0` only**, so `D0→D1` clears L3a while `C0→D1` still reads L3b — the two references reach **different verdicts** and the leg must stop — **while the additive identity still holds exactly.** One fixture is thus simultaneously the positive control for the real check and the proof the registered one was never load-bearing. Sibling control from the same battery, and the sharper of the two: **`blindl1`**, the §19.11 sensitivity control's *own* control — a complete, well-formed L1 whose model is rewritten to match its sim exactly, so the reduction returns **0 pp everywhere**. A flat L3 reading from that instrument is indistinguishable from a real absence, so the reducer must refuse to grade (it does; "L1 is present" is **not** sufficient for §19.11). Battery **30/30 green first run** — 13 derived fixtures whose sim-side perturbations are **aimed** at target residuals (`deadband` → +0.125, `l3afires` → +0.52, `refinv` → +0.30) so each provably sits where the battery claims, 22 negative controls and **8 positive** ones (a guard that over-rejects is the same defect class, so *every* verdict must be reachable on purpose-built data). **Lesson: for every control, ask what independent measurement it consumes. If the answer is "none — it re-arranges numbers I already have", it is bookkeeping; a control must be able to fail for the reason it was written.** | PHASE8 §19.13; `$SP/p8/{r9l3fixgen.mjs,r9l3ctl.sh}` (30/30). |
| 07-25 | §19.10's second headline — *"the model never inverts a ranking the sim can resolve: 6 AGREE / 0 DISAGREE / 2 ties"* — is a settled result of round 9; it is quoted in ROADMAP and DIARY and can carry the phase-level synthesis. | **★★★ It had NO instrument: no script, no fixture, no control — it was computed BY HAND**, and it is an **ABSENCE** claim, the exact shape §19.11 had warned about *in the same round* (a census that cannot see an inversion and a census over data containing none print the same zero, and the blind one exits 0). Found by tracing it back to the code: `r9l1stat.mjs` computes only a Spearman rank *correlation*, and `grep -rln DISAGREE` over the apparatus hits only a different check plus two commit drafts. The instrument now exists (`r9census.mjs`, battery **18/18**), reproduces the hand count **exactly** on both scoring columns, and takes its §19.11 sensitivity control from **real data rather than a fixture** — round 6's G1 endpoint pair (`d_sim` +0.3602 vs `d_model` −0.0370, **3.7× BAR**) must read DISAGREE or the run refuses to grade. **★ And instrumenting it CHANGED WHAT IT SAYS:** the same census returns DISAGREE on the two-plan pair and 0 DISAGREE on the isolated press at all 8 hastes, so the claim is about the **PRESS**, not about B2 — a per-press residual below the sim's resolution still flips a resolvable *plan-level* ranking via §17's path additivity (0.0007 pp). **B2 is real AND no single press inverts anything**, and §19.10 must not be read as an exoneration. Both battery failures on the first run were **my expectations, not the guards** (L1's lesson in mirror form): a huge bar blinds the *sensitivity pair* before it thins the census, so `MINRES` needed its own control at `NSD=7.2` or it was dead code; and a tiny bar unmasks **1** tie, not 2, because `R=120`'s `d_sim` is **exactly 0.0000** and no positive bar can ever resolve it. **Lesson: a number that has been quoted often enough to feel settled is not the same as a number that has been measured — before a claim carries a synthesis, find the script that produced it.** | PHASE8 §19.14; `$SP/p8/r9census{,fix}.mjs`, `r9censusctl.sh`. |
| 07-25 | PHASE8 §6's own prescription for the harness gear gap: *"add `t5two: true` to **both** cfg builders"* — two named sites, a two-line fix, sized and scheduled by the round that found it. | **★ Seven sites, six spellings, five tools.** Executing it found `sp: 1387` inline in every cfg, the plain-cast normalizer hand-typed twice as `(GAME.AB.AVG_BASE_DMG + GAME.AB.COEF * 1387) * …`, twice more as the fully-numeric `(720 + (2.5/3.5)*1387) * (1 + 0.38*0.8175)`, and once as the bare literal **`2241`**. Patching the two named sites would have left five tools still describing a different mage than the sim runs. The defect was **duplication**, not a missing field — so the fix is a module (`tools/reference-gear.mjs`) whose `REF` is **spread** into cfgs, because `sp` is conspicuous and `t5two` is not, and `cfg.t5two ? 1.2 : 1` reads a missing key as `false`. | PHASE8 §20; TOOLING "THE REFERENCE GEAR"; `tools/reference-gear.mjs` header. |
| 07-25 | A mechanical rewire (thread one constant through six tools) is verified by `node --check` plus a grep that the constant appears at every site — there is no behaviour to test. | **★★ Both passed; the tool was broken.** `brute-grid.mjs` threaded `REF` into the grid evaluate and **not** the `--tool` one. `{...undefined}` is silently legal in JS, so the certification cfg lost `sp`/`critPct`/`t5two` without throwing, `simulate` returned `NaN`, and every `NaN` comparison being false walked the verdict ladder to a graded `MISS — investigate` computed from a number that never existed (in `haste-ladder` the same drop falls the other way, to a clean **PASS** — the false-pass class proper). A 20-second smoke run found it; the static checks could not. **Grep proves a name is present, not that it is bound.** Both certifiers now exit 2 rather than grade a non-finite score. | PHASE8 §20.3; TOOLING "THE REFERENCE GEAR" ★★ bullet. |
| 07-25 | PHASE8 §20.2's pre-flight probe measured the reference-gear correction as **rank-neutral** (same argmax plan under every cfg at every haste on its fight; one 0.011-eff-AB reorder at h150, ~3 orders below the 0.3 pp bar), and that finding was carried forward — into the landing commit, into the `xval-results` banner, into the round-5 launch decision — as **"the plans will not move."** | **★★ Overturned by the very round it was meant to predict: 6 of 40 cells — 15% — changed plan.** `tools/xval-round-diff.mjs` on the first four completed round-5 tables against their round-4 predecessors: `isc-scb-short` **3/10**, `isc-scb-medium` 0/10, `mqg-skull-short` **2/10**, `mqg-skull-medium` **1/10**, on a systematic **−0.4…−1.1 % eff level shift** — the REPRICING signature, not a tie-break. The probe is not buggy; it is **selection-blind**. It measured one fight (`T=300`, BL@60) at four hastes; the round is 36 fights at 7–10 hastes ≈ 300 cells, and a −0.6 % repricing flips every cell where two plans sat within 0.6 % — which is not a rare shape but **the entire subject of Phase 7**, a corpus that exists *because* neighbouring-haste plans score a fraction of a percent apart. **Lesson: a rank-neutrality result inherits the SELECTION of the cases it was measured on** — applying one measured on a typical case to a corpus chosen for marginal cases is the same error shape as validating a tie-break on a case that has no ties. What does **not** change: the correction is right on grounds that never depended on Q2 (a harness must describe the mage the sim runs); Q2 was a *scheduling* estimate, not a justification; §20.2's Q1 repricing table is untouched and is independently confirmed at the same magnitude by the −0.4…−1.1 % band; and the one verdict that has moved so far moved the **right** way (`mqg-skull-short` diagWorst 0.04 % → 0.02 %). | PHASE8 §20.5/§20.6; `tools/xval-round-diff.mjs`; `tools/xval-results/README.md` banner (claim withdrawn in place). |
| 07-25 | PHASE9 §4.1, the top of the refactor catalogue: *"The pass stack's inner loop is, **almost everywhere**, this shape"* — `repair` → `sameCounts(counts…)` → `clipOf` → `simulate` — so fusing the four walks into one `repair` that emits `{s, sig, counts, clip}` as it goes is a near-universal win, and the only stated risk is blast radius (*"every call site must be converted"*). | **★ "Almost everywhere" measures at 20%, and a THIRD of the sites want none of it.** `tools/pass-sites.mjs` (static, zero CPU — safe to run under an acceptance round) censuses **41 `repair` call sites** in the engine block: the full quadruple is **8 (20%)**, **14 (34%) are BARE** — they consume only the schedule — and the remaining 19 take one or two of the three guards. So an unconditional fused `repair` would compute three outputs that a third of its own sites discard: **new cost paid to remove old cost.** The surviving design is an **opt-in output mask**, strictly larger than §4.1 sketched. Two sub-claims survive: `sigOf` has only **3 textual occurrences** (definition included), confirming its ~2M walks come from being called *inside* `simulate` — so that half's site conversion really is small; and static **42 `cloneS` vs 41 `repair`** sites echo §4.5's runtime **2.08M vs 2.02M**, two independent directions agreeing on the ~1:1 ratio. **The load-bearing caveat: a site count sizes the CONVERSION WORK, never the WIN** — one bare site in the hottest loop outweighs eight quadruple sites in cold branches, so §4.13.1's landing order still waits on §4.7's *runtime* census. *(Instrument sibling, same family as row 206: the first draft counted **42** sites and 64 `simulate`s because `index.html:2248` is a **comment** reading "…without this, `repair()` would move only the cooldown-bound icon…", and the 8-line classification window was reading commented-out guards as live. This file carries its theorycraft **inline by design**, so a census that cannot tell comment from code over-counts in both directions at once. Fixed, and the fix is backed by a built-in self-test on exactly that pathology plus a negative control — running with the stripping disabled makes the self-test fire and the tool exit 2.)* | PHASE9 §4.15; §4.1 amended in place; `tools/pass-sites.mjs`. |
| 07-25 | Row 219's closing reassurance, written when four round-5 tables were complete: *"the one verdict that has moved so far moved the **right** way (`mqg-skull-short` diagWorst 0.04 % → 0.02 %)"* — a 1-of-1 sample read as a direction of travel. | **★★ False at eight tables, and the framing was wrong, not just the sample.** `isc-scb-long` moved **0.05 % → 0.11 %**, i.e. *worse*, against `mqg-skull-short`'s improvement — and the churn itself is running at **17 of 80 cells (21 %)**, not 15 %. The deeper error is expecting a direction at all: a **repricing re-ranks near-ties in whichever direction each one happened to sit**, and it was never a change to the *model*, so there is no mechanism by which it should reduce a deficit. What is genuinely reassuring is unchanged and is the part that was never about direction: **no verdict flipped** (CLEAN↔DEFICIT) either way, `deficitTables` held at 7→7, and `monoDip` stayed 0.00 % everywhere, so invariant A is untouched. **Lesson: read no acceptance signal off the SIGN of a repricing's moves — only off the invariants and the verdicts.** | PHASE8 §20.5 (table completed to 8, claim replaced in place); `tools/xval-round-diff.mjs`. |
| 07-25 | PHASE9 §4.7's suspicion 2, and its price tag. It groups *"two nested fixpoints over overlapping pass sets"* — the groom loop and the final hop↔normalize fixpoint — and reasons that *"if the outer loop converges in one round on every case, rounds 2–3 are pure cost"*; §4.7 and §4.14 then both close by pricing the census that would settle it at *"hours of CPU on 4 cores"* which *"must not overlap an acceptance round"*, which is why it has sat unrun through every CPU gap of this phase. | **★★ The two loops are not the same shape, and the price is for half the instrument.** Read against the file: the hop↔normalize fixpoint (`index.html:2786-2798`) **already early-exits** (`if (stable) break;`), so its rounds 2–3 were never pure cost; the groom loop (`:1667-1740`) has **no** groom-level break in its 72-line body. The suspicion collapses onto one loop — and it needs **no census at all**: rounds 1 and 2 are the *same function* (`f1 = f2 = challengePass ∘ sweeps`; only round 0 differs, lacking the `challengePass`), so a no-op round 1 provably implies a no-op round 2, making the exit byte-identical by argument rather than by hope (checked, not assumed: the body's 27 names are all loop-local and `challengePass` re-allocates its two `Set`s per call). **And the census's own price is asymmetric:** proving a pass redundant is universal (∀ cases, zero firings) and costs the corpus; proving one **not** redundant is existential (∃ one firing) and costs **one case**. Since §4.6 already expects most passes to be non-redundant, the cheap pass thins the list today and the CPU gap is owed only to the survivors — and the existential half is **structurally immune to §4.14's false-pass defect**, because a silently-skipped insertion cannot manufacture a *presence*. **⚠ Sized honestly and it is a small win:** the inner sweep loop already self-terminates (`sweep < 8 && sweptMoved`), so a dry groom round costs one `challengePass` + **one** sweep, not eight. Land it for the structural inconsistency, not for a predicted speedup. **Lesson: before pricing a measurement, check whether the claim it would settle is already settled by reading the code — and check whether the price quoted is for the verdict you actually need.** | PHASE9 §4.17; §4.7 suspicion 2 + cost note and §4.14's closing amended in place. |
| 07-25 | `optimizeCore`'s progress bands are *"banded by the **MEASURED cost profile** (seeds ~4%, hop dominates, fixpoint rounds mostly cache-served)"* (`index.html:1450`) — a hand-written second cost model, installed specifically so the bar would be *"truthful, instead of stalled at 97% for a second run as long as the first"*, and never cross-checked against §1's profile. | **★★ The model stops 4 % before the program does, and the omitted 4 % is a `polish`-per-candidate loop.** Three findings, all static: **(1)** there are exactly three `setStage` and two `stageFrac` sites in the file, and `awk 'NR>2792 && /setStage|stageFrac/'` returns **zero lines** — nothing past the hop↔normalize fixpoint reports at all. **(2)** the band comment claims *"round 3 [0.955,0.98]"* but its own formula `0.82 + 0.16*(1 − 0.5^fx)` yields `[0.82,0.90] [0.90,0.94] [0.94,0.96]` — 0.98 is the halving's *limit*, not its third term, so the bar tops out at **0.96**. **(3)** what runs in that unreported band is the **drop-one-use escape** (`:2799-2827`): a full `polish()` per single-use drop candidate, over every unfixed track with ≥2 uses, up to **4 rounds**, with a full `basinHop` + `normalize` after each improving round — against a seed phase of **14** polishes that is given **10 %** of the bar, and a `basinHop` the bar prices at **58 %**. So the pathology the comment was written to prevent was fixed for the Cold-Snap comparison (which restarts the bar, `:2848`) and **reintroduced** by an escape added later and never banded. **⚠ Sized honestly it is bimodal and the common case is cheap** — `again2` needs a strict improvement and `break`s at once, so a losing scan is one round of ~15 polishes and no hop; the expensive mode is exactly the one that leaves the user at 96 %. **The re-band is display-only** (`setStage`/`stageFrac` only write `prog`/`stage.label` and call `onProgress`; nothing computed reads them) and so is plan-neutral by inspection — **but it is NOT a speedup**, it makes nothing faster, it makes the slow part visible. **Lesson: when a program carries a second, hand-written model of itself — progress bands, a cost comment, a stage list — cross-check it against the measured one; two models built from different evidence are a free instrument, and the cheapest thing they find is the stage nobody named.** Consequence for §4.7: a census whose stage list was taken from the bands would inherit the identical blind spot. | PHASE9 §4.18; §4.13.1 ladder item 0d. |
| 07-25 | The worker pool is described in `index.html:1224-1231` as fanning `optimizeAsync`'s **hot loops** across cores, backed by a profile — *"basinHop's teleport-polishes are ~93% of a 285s run"* — and by a byte-identical-plans guarantee (*"the pool changes only WHERE a candidate is polished, never which candidate is accepted next"*), which is where task #57's CPU question was left. | **★★ It covers two of the FIVE candidate loops, and the split tracks *history*, not independence.** `poolMap` is called at exactly two sites — the seed phase (`:1456`) and `basinHop` (`:1336`). Three `polish`-per-candidate loops run **sequentially** with `POOL` live in scope: the snap-to-whole-seconds pass (`:1502`, **6 candidates, always**), the drop-one-use escape (`:2815`), and the Cold-Snap chain search (`:2882`). The sharpest instance: `:1502` sits **twelve lines below** the pooled seed loop, in the same function, and is a *purer* case of the identical shape — six independent polishes, running max, no data dependence — while the loop above it carries a comment explaining why independence makes pooling safe. **Two of the three are strictly EASIER to pool than the one already pooled:** `:1502` and `:2882` are running maxima with no `break`, so every candidate is polished either way — no accept-order to preserve and **zero discarded work** — and `poolMap` already returns results in item order; `:2815` is `basinHop`'s first-accept shape *minus* the restart, since an acceptance exits the whole scan. **⚠ A naming collision hides it:** `cfg._noPool` is checked at one site (`:1379`) and disables the **cross-haste** pooling, not `POOL` — so the reader who arrives at `optimizeCore({...cfg, coldSnap:false, _noPool:true})` immediately above the unpooled chain search concludes that path runs off-pool deliberately. It does not. **⚠ Sizing: the static half is checkable (~8–30 s of sequential polish per solve at the file's own ~0.3–1 s per polish, against §1's `wall=31.49 s`); the Amdahl half is an INFERENCE and is labelled one** — the 93 % is a *pre-pool* measurement and the 7 % remainder's composition was never measured, so "the remainder now owns 13 % (N=2) to 38 % (N=8) of the wall clock" is a prediction, not a result. **Lesson: a shipped optimization's coverage is a fact about when it was written; re-census its call sites before crediting it with the shape it describes.** Composes with §4.18: the unbudgeted stage is also an unpooled one — two independent readings landing on the same twelve lines. | PHASE9 §4.19; §4.13.1 ladder item 1b. |
| 07-25 | Task #57's brief lists five CPU suspects — *"simulate() inner-loop allocation, sigOf/JSON.stringify keying, redundant fixpoint re-scoring across the pass stack, **breathe() cadence, worker pool sizing**"* — and the phase had attacked only the first three, leaving the last two open and the user's actual words (*"gotten slow **again**"*) unread. | **★★ `breathe()` is falsified, pool sizing is reframed, and "again" turns out to be about the SESSION, not the solve.** **(a) `breathe` — CLOSED.** All 20 `await breathe()` sites sit on **outer** loops; checked at six (`:1930`, `:2202`, `:2298`, `:2560`, `:2695`, `:2736`) and at `:2202` the contrast is explicit — `breathe` is on the `(track, use)` index while the `t ≤ cfg.T` candidate loop directly beneath runs unbreathed. So it fires ~10²–10³ times against `repair`'s **2,019,660**, and §1's profile is correspondingly silent (`breathe`/`tick`/`performance.now` absent from 100,392 samples that found room for `rateAt` at 0.5 %). *The profile half is an **absence** (§19.11's warning) and is admissible only because the static half is a **presence** that explains it.* **(b) pool sizing — reframed, not resolved.** `n = max(1, min(8, hardwareConcurrency − 2))` reserves a core for the orchestrator, but during a fan-out the orchestrator is **blocked by construction** (`await Promise.all` over port round-trips) — so on a 4-core box **2 of 4 cores are busy during the 93 % phase**. This is a knob to **sweep**, not to argue (`hardwareConcurrency` is logical cores; `−1` oversubscribes a hyperthreaded 2-physical box) — the static argument only shifts the prior. **(c) ★★ the lifecycle, which is what "again" meant.** Every mid-solve input change hits `engineWorker.terminate()` (`:3339`), and that path accumulates **three** things: a `Blob` holding a full copy of the engine block — **141.5 KiB**, because `URL.createObjectURL` is called at `:3314` and **`revokeObjectURL` appears nowhere in the file**; one live GC-rooted `MessagePort` per pool worker, because `pool-port`'s handler registers a new `onmessage` and never closes the ports it registered before; and up to `n` **abandoned in-flight `polish` jobs**, which a worker cannot be interrupted out of, so each finishes (~0.3–1 s) against a dead port *and* delays the new run's first fan-out. **⚠ Sized honestly:** the first two are memory and slow-burning (~14 MB per hundred interruptions) and worth fixing because they are nearly free, not because they are the bottleneck; the third is the CPU one and is **irreducible without a cooperative interruption check inside `polish`**, so it is **recorded, not fixed**. All three are in the **UI script**, hence plan-neutral by *construction* — yet they still wait, because `tools/xval-*.sh` load `index.html` per case and editing it mid-round would mix two baselines inside one round. **Lesson: read the user's report as evidence — "again" is a claim about degradation over a session, and no per-solve profile can see it.** | PHASE9 §4.20; §4.13.1 ladder item 0e. |
| 07-25 | PHASE8 §20.5, written at 8 of 36 tables: the round-4→round-5 churn *"is running at 21%"*, and the deficits *"do not move in one direction"* — one improvement, one regression, six unchanged. The section's own status line called itself partial and asked for the full comparison when the round lands. | **★★ At 18 tables the two claims separate: one strengthens, one was under-counted.** The churn is **50/184 cells = 27.2%**, and it has grown monotonically with the sample (4 tables → 15%, 8 → 21%, 18 → 27.2%), so 21% was not an estimate of the round — it was an estimate of the first eight tables. **27% is a floor, not a converged figure.** The direction claim, by contrast, holds harder the more it is measured: **4 better · 3 worse · 11 unchanged**, with the invariants untouched throughout — **no verdict flips, `deficitTables` 16 → 16, `monoDip` 0.00% on every table** — and a consistent **−0.437%…−1.108%** eff band, which is §20.6's REPRICING signature (a tie-break would show `eff ≈ +0.000%`). Worth recording because it is the *next* temptation after row 221's: the two **largest** single moves are both improvements (`isc-mqg-medium` 0.38→0.09, `isc-mqg-medlong` 0.40→0.26), which reads like the correction fixing the worst cells. It is not — a bigger move means a bigger near-tie gap was crossed, nothing more. **Lesson: a repricing has no preferred direction AND no preferred magnitude; read the acceptance signal off the invariants, never off the moves.** *(Instrument micro-lesson, same family as row 206 and §20.1's `t5two`: the first invocation was `node tools/xval-round-diff.mjs A B | tail -50; echo "EXIT=$?"`, which printed `EXIT=0` — `tail`'s status, not node's. Under the tool's contract `0` means **graded clean**, i.e. a complete comparison, so I was one step from filing "exits 0 on an incomplete round" as a defect in a tool that was behaving correctly. Redirected to a file, it exits **2** — could not grade — because two tables were mid-write. **When an instrument reports success, check you are reading the instrument's answer and not the plumbing's.**)* | PHASE8 §20.5 (rewritten to 18 tables). |
| 07-25 | **The iteration loop's gate.** For seven phases the per-change confidence check was `tests/exact-match.mjs`, and the "are we done yet" check was the hours-long xval acceptance round — with nothing in between. The user challenged this directly after a 10-hour absence: *"aren't you wasting a lot of time doing too few changes between running the full simulation of everything?"* | Measured instead of defended, and the defence lost. **exact-match takes 9m07s** (`real 9m7.355s`, 25 passed) — not a fast gate. It runs 25 cases **sequentially in one chromium page** (zero parallelism) and compares the **copy-as-text plan with times FLOORED to seconds**, so a sub-second schedule shift inside the same floor bucket **passes it**. It is also a **final-state** comparison: a refactor that changes a mid-pipeline accept decision, then gets washed out by later passes, goes green on all 25 and bites on case 26 — a real false-pass hole in the only per-change gate. ⚠ **One claim in that list was my own over-claim and died on measurement:** I also wrote that a *sub-second press shift* could hide inside a floor bucket. It cannot — **all 273 press times across all 25 cases are integers**, so flooring loses nothing. Same instrument error as §4.15/§4.21 (reasoning from the *shape* of the code — a `Math.floor` exists — instead of the *values* through it), caught before it reached a living doc. The surviving, honest argument for the differ is different: exact-match compares derived **fire times** (presses snapped to cast boundaries, genuinely fractional, hence floored), while the differ compares **`best.s`, the schedule the optimizer chose** — the upstream quantity, so at least as sensitive, strictly more so wherever the floor absorbs a press change. Whether such a case exists in the corpus is UNMEASURED. Fix: a **bare-node** plan sweep + A-vs-B differ (`PHASE9 §5`) — the engine block was confirmed DOM-free and evaluates in plain node (`load=4ms`), so the gate no longer needs a browser at all. Measured: full corpus **239s** (vs 547s), quick tier `--max-t=200` **33s for 16/25 cases — ~16× faster than exact-match**. Both controls run, because a differ that only ever says IDENTICAL *is* the defect class: swept twice at different job counts (different partitioning) → IDENTICAL, which also re-certifies determinism as independent of process layout; seeded perturbation (`isc.dur 20→18`) → 8/16 changed, exit 1. |
| 07-25 | Where the optimizer's CPU goes. The Phase-9 catalogue (§1–§4) hunts **constant factors** — redundant walks, fusable steps, `sigOf`/`JSON.stringify` keying. | The bare-node sweep gave the first clean per-case cost profile (one process per case, no browser noise) and it relocates the target: **cost is ~exponential in the number of presses that FIT, not in `T`** — 7 presses ≈2.0s, 9 ≈3.7s, 12 ≈28.6s, 14 ≈53.4s, 15 ≈115.0s, 18 ≈168.7s, i.e. **~1.35–2× per added press slot**. `T` matters only through how many presses it admits (`3:20`/11 presses = 3.8s vs `4:00`/12 presses = 28.6s — length cannot explain that, an opened press slot can). The 5 longest cases are **73% of all CPU**; one case sets an **Amdahl floor** the full sweep can never beat. Does **not** invalidate the §4 catalogue (still lands cheapest-first, still gated byte-identical) — it adds the target the catalogue lacked: why an extra press slot costs 1.35–2×, and whether that is irreducible or re-exploration of settled prefixes. |
| 07-25 | **Verification was scoped to the corpus, not to the change.** Every re-check re-ran everything, whatever the change touched. | The user's correction, and it has two halves. **(1) Cost:** if a plan is bit-identical and the sim did not change, the cached verdict is still valid — re-simming buys nothing, so the changed-cell set *is* the work list and a rule that moves four cells costs four cells of sim time. **(2) ★ Correctness — the half we had wrong:** when a plan *does* change, sim **old vs new head-to-head at that cell**; do **not** read the table aggregate, because `monoDip`/`diagWorst`/the CLEAN-DEFICIT verdict are summaries that **can hold or improve while one cell regressed**. This indicts PHASE8 §20.5 directly: 50 of 184 cells changed plan with no verdict flips and monoDip 0 everywhere, and the section concluded to read no signal off the moves — right about the *available* columns, wrong to stop there. ⚠ The duel cannot be done by subtracting two rounds' tables (§20.6's repricing makes round-4 and round-5 scores incomparable); both plans must be re-simmed under **one** harness. ⚠ And a plan-diff finds cells a rule *did* move, never one it *should* have moved and didn't — that population is the standing-DEFICIT list. Written into `TOOLING.md` (methodology) and `ACCEPTANCE.md`. |
| 07-25 | **What a xval PASS proves.** The acceptance round was treated, in practice, as "the tool is correct." The user pushed on exactly this: *"maybe even the big test isn't a definitive 'is the tool reliably correct in every single scenario' either."* | Correct, and now stated in `ACCEPTANCE.md` instead of left implied. The round is a **sampled, grid-limited comparison**: the reference is a **5-second-grid sim search**, not an optimum; the haste points sample a continuum; the 36 shapes sample fight space; Ashtongue is excluded (stochastic) and KT's AoE is simmed as downtime. So CLEAN means *"agrees with a grid-limited reference on the sampled points"* — strong, but not "optimal." Its role is the once-per-campaign **"are we done yet"** measurement, never the per-change gate; using it as one is what made the loop slow. |
| 07-25 | **The §4.7 runtime census, RUN on all 25 cases — three owed measurements, and one of them thins a confidence rather than confirming it.** Instrumented a *copy* of the engine (§4.14) so the repo's `index.html` stayed frozen for the running xval campaign, and **proved plan-neutrality before reading a single count** — the census build and the shipping build sweep to `PLAN-DIFF IDENTICAL` on 16/16, so the probes measure the real optimizer. **(1) §4.7's groom early exit is sound, and ~half the size it looked.** The groom loop runs **exactly twice per case, all 25** (so "three grooms" is two). Round 1 no-ops 44% of entries, round 2 no-ops 98%. The proposed `if (groom >= 1 && unchanged) break;` fires in **19/25 cases**. §4.17's f1=f2 purity argument needed one falsifier — *round 1 no-op → round 2 change* — and across 50 executions **none exists**: the corpus's single round-2 change is in `5:00 lust 0:05`, whose `r1noop=0` means both its executions changed in round 1. First empirical test, passed clean. ⚠ **But size it honestly:** the guard reclaims **22 of the 49 wasted round-2 executions**, because you may skip round 2 only when round 1 *was* a no-op, never because round 2 *will* be — **~45% of the waste, not 98%.** **(2) §4.21's coverage precondition: CONFIRMED, and thinner than "likely".** `dodgeDowntime` (`:2666`) fires in **1 of 25 cases** (Al'ar, 4 firings) — and *all 4* post-`sameCounts` candidates fire, so the corpus never exercises that site's **rejection** paths at all. `apSnap` (`:2347`) has 101 candidates and **1 firing in 1 case**. Each Class-D site is protected by **exactly one golden**. The instinct behind "'likely' is the wrong confidence for a guard" was right; the margin is one case per site, and the decision not to convert them rests where it always did — on *reading the code*, with the suite a thin second line. **(3) §4.1's fusion premise, as a number:** repair : simulate : sigOf = **29.39M : 29.33M : 29.18M ≈ 1:1:1** across the corpus — the *dynamic* counterpart to §4.15's static correction, and it confirms the three walk the same schedule in lockstep. Concentration tracks §5.6 exactly: `7:20 lust 0:05` alone is **6.98M repair calls in 168.9s**, 24% of all repair traffic in one fight. | PHASE9 §5.8 (§4.7, §4.21, §4.1 answered in place). |
| 07-25 | **The DUEL is built (`tools/plan-duel.mjs`) — and its 4th control caught the tool telling a confident lie.** `plan-diff` says which cells moved; the duel says whether each move was an upgrade, because monoDip/diagWorst/CLEAN-vs-DEFICIT are aggregates that can hold while one cell regressed. | It never subtracts two sweeps' recorded scores (the §20.6 repricing trap) — both schedules are re-scored under one engine at a time — and it **measures** whether the scorer moved rather than trusting the caller. **Two asymmetries let it grade without a sim.** (A) **THE CONFESSION RULE — circularity has a DIRECTION.** *New engine prefers the new plan* is true by construction and proves nothing; *new engine prefers the **old** plan* is a confession that the search left value on the table under its own objective, and no repricing can manufacture that sign. It was safe to add precisely because it can only turn CANNOT-GRADE into a **failure**, never into a pass — a refinement that can only get stricter cannot introduce the false-pass class. (B) **THE FEASIBLE-SET GUARD**, tested first, and the reason (A) doesn't lie. ⚠ **(B) was NOT in the first implementation and controls C1–C3 all passed anyway.** C4 broke it: `isc.cd` 120→180 is a **pure constraint change**, so `simulate()`'s pricing is bit-identical, `scorerMoved` reads FALSE, and the tool took its most confident branch — reporting **9 cells as "★ REGRESSION under an UNCHANGED objective."** All 9 false: the old `isc:[5,127]` is 122s apart and simply *illegal* at cd:180, so `newOnOld` credited the new engine with a use its own rules forbid. The search never regressed; the **feasible set shrank**. ★★ **Generalisable: `scorerMoved` and "are these two plans comparable" are DIFFERENT QUESTIONS** — pricing and the feasible set move independently. Any comparison instrument that checks only *did the score function change?* inherits this exact mode. It is a false *failure*, the milder half of the class — but a precise, cell-named regression report would have sent us hunting a search bug that does not exist. Side benefit: C3 gave **5 confessions of 8 changed cells**, making the duel a cheap **sim-free search-quality probe** (~27s/sweep) for the Phase-7 SEARCH-MISS question. | PHASE9 §5.9; `93680a9`. |
| 07-25 | **Two INVARIANCE leads fell out of the duel's controls — the finishing passes, not the random restarts, may be what determines the plan.** | C2 shifted the PRNG seed 1337→4242: **0 of 16 plans changed.** C2b clamped `starts` 14→1 — dropping 13 of 14 random restarts: **`PLAN-DIFF IDENTICAL`, 0 of 16 changed, CPU 75s→61s (~19%)**. Neither was accepted on its face, because *a perturbation that silently fails to apply produces exactly this result*: verified `rand()` has **exactly one consumer** (`index.html:1108`, the restart-seeding line), `starts` **exactly one** (`if (seeds.length >= starts + 8) break;`), and the shifted constant was confirmed in the **loaded** file, not just on disk. ⚠ **COVERAGE CAVEAT — leads, not conclusions: both cover only the 16 QUICK cases (T≤200).** The 9 long cases have the most presses and the largest search space — *exactly where restarts should matter most* — and cost is ~exponential in press count, so the QUICK tier is the sub-population **least** likely to need them. **Do not cut `starts` on this evidence**; the confirming run is the full 25 plus an xval haste-ladder spot-check. | PHASE9 §5.10. |
| 07-25 | **CORRECTION (same day) — both invariance leads are DEAD, and the way they died is the more important result.** The full 25 was run for both. `starts` 14→1: **2 cells changed** (1 ★REGRESSION −233.09, 1 tie). Seed 1337→4242: **2 cells changed, both ★REGRESSION** (−233.09, −52.82). Full-corpus CPU 695s→583s, so **that 16% is buying real search quality on exactly the most expensive cases.** `starts` stays. | Both duels report `scorerMoved=false` (a search parameter is not a pricing one), so the model arbitrated directly with no sim — and that also retroactively licensed `plan-diff`'s raw Δscore subtraction *here*, the currencies provably matching. ★★ **THE INSTRUMENT LESSON: a fast gate's blind spot is CORRELATED with what makes it fast.** §5.6 established cost is ~exponential in press count, so the QUICK tier is cheap *precisely because* its cases have few presses and a small search space — *precisely* where search is easiest and restarts least needed. It is not a random sample of the corpus; it is the sub-population selected for being easy, so its silence on a **search-quality** change is uninformative **by construction**. The tier stays (it is right for pricing and refactor changes, where cost and coverage are uncorrelated) but **any change to the search — starts, seeds, anchors, basin hops, accept tolerances, iteration caps — must be gated on the full 25** (~360s wall at 2 jobs). ★ **Fragility exposed:** the −233.090261 at `5:40 lust 0:05` is **bit-identical under both perturbations** and both land on the **same** inferior plan — so ONE lucky restart out of 14 at seed 1337 finds the aligned `[5,127,307]` basin and the finishing passes cannot reach it from a generic start. That plan is **luck baked into the goldens**, and a SEARCH-MISS waiting to happen: any future edit perturbing restart draws silently drops 233 points there and the golden churns for reasons that look unrelated. Fix is not more restarts but a **basin-hop anchor at the fully-aligned layout** — Phase-7 work, with this cell as its ready-made test. | PHASE9 §5.10. |
| 07-25 | **Tier 1 landed: exact-match parallelised, 547s → 270–337s.** The 25 cases were always independent; the suite was sequential only because nobody had made it otherwise. | Fanned across `JOBS` browser pages (default `cores-1`), same round-robin slicing as the sweep and for the same reason (row 228: cost tracks press count and the presets are `T`-ordered, so contiguous blocks hand one page every long fight). **Harness-only — the engine is untouched, so the change carries no determinism risk of its own.** ⚠ **But parallelism introduced one, and it was nearly missed:** `results` was keyed by name and `JSON.stringify` preserves *insertion* order, so `--update` would have written `golden.json` in whatever order the pages finished — **the golden churning on job count alone.** Fixed by re-emitting in preset order; verified separately that the existing 25 keys are already in that order, so a future `--update` writes byte-identical ordering. ⚠ **And one guard was written UNREACHABLE.** The draft refused to write a short golden — but every omitted case already pushes an error and errors exit 2 first, so the branch could never execute. *A guard that cannot fire is not protection, it is the appearance of protection* — the false-pass class one level up, in the safety net rather than the test. Repointed at the real risk: a `PARTITION HOLE` check on the round-robin cover, where a dropped index would raise no error and silently shrink the corpus. (It also removed a latent crash: a missing `got` would have hit `got.split` in the FAIL branch.) Both controls run — positive: `JOBS=3` scores **25/25 against a golden produced by the sequential runner**, which is a stronger byte-identical proof than re-running `JOBS=1`; negative: a new `INDEX=` override points the suite at a scratch copy, and one throwing for `T>200` gave **9 ERRORs, exit 2, 30.3s** — and did *not* print "16 passed", which is precisely the mode being guarded against, since those 16 short cases genuinely did pass. **Remaining honest limit: parallelising made the gate faster, not deeper — it is still a final-state comparison** (a mid-pipeline accept change that later passes wash out still goes green), which is exactly what ladder item 0a is. | PHASE9 §5.7; `tests/README.md`. |
| 07-25 | PHASE9 §4.16, and §4.13.1's ladder item 0a on the strength of it: the legality prefix `repair` → `sameCounts(counts…)` → `clipOf` is *"written longhand at **9 sites**"*, so extracting one `admit` helper is *"byte-identical **by construction**"* — the safest item in the document, worth landing on legibility alone. | **★★ Nine sites share the SHAPE; only seven share the RULE.** Writing the patch out site-by-site splits them four ways: **5** convert as pure text motion; **2** convert with different control flow (`:2766`'s stringify no-op stays at the call site, `:2702` `break`s instead of `continue`s); **1** (`:2306`) is a **reordering**, not text motion — it hoists `clipOf` ahead of a `simulate`, which is plan-identical but whose *performance* sign is unknown **in both directions**, because `clipOf` sits last in a `||` chain and is short-circuited away whenever a robust gate already rejects; and **2** (`:2347`, `:2666`) **must NOT be converted at all**. The clip guard is missing from those two **deliberately**, and at `:2666` the file says so in the comment above it — *"No clip guard: sliding to the exit ends the window later (more past-kill seconds) but the LIVE portion is unchanged — `clipOf` would wrongly veto a pure wash."* That pass slides a press out of an intermission it starts inside; the dead seconds are dead wherever the window sits, so the move is a score wash that `clipOf` — which counts past-kill seconds without asking whether they were ever live — reads as a regression. Through `admit` the pass stops firing on exactly the case it was written for. **Lesson: §4.15 over-counted work by trusting a grep; this would have shipped a BEHAVIOUR CHANGE under a "pure text motion" label — the same instrument error, in the direction that actually costs something.** And the gate would not reliably have caught it: `:2666` only fires when `cfg.segments` carries an intermission, so the 25 goldens catch a wrong conversion only *if one exercises that pass* — likely, but "likely" is the wrong confidence for a guard, so 0a now carries a confirm-the-coverage precondition. **Spin-off, and it is a MODEL question, not a perf one:** `:2666` justifies its missing clip guard and its sibling `:2347` does not — either that omission is deliberate for an unwritten reason or it is a real gap, since a press can be snapped onto a fixed window at the cost of clipping further past the kill with only a +0.5 robust margin against it. Recorded for Phase 7's ledger and explicitly fenced off from the refactor: **do not "fix" it while landing 0a.** | PHASE9 §4.21; §4.16 and §4.13.1 item 0a corrected in place. |
| 07-25 | **CORRECTION (same day, row 234) — the `5:40 lust 0:05` fragility is NOT "one lucky restart out of 14", and the proposed fix ("a basin-hop anchor at the fully-aligned layout") was aimed at a mechanism that does not exist.** The belief came from a real observation — the −233.090261 is bit-identical under both `starts=1` and `seed=4242`, and both land on the same inferior plan — read as *one draw finds a basin the others miss*. | **★★★ THE ENTRANT PLATEAU.** Three probes opened the pipeline instead of inferring from its output. (1) Reconstructing `optimizeCore`'s 14-seed list verbatim and reporting each seed's `polish()` basin: **0 of 14 seeds reach the good layout at all** — 13 distinct basins, top 6 spanning 19 points. So no draw finds it; it is manufactured DOWNSTREAM. (2) Running the snap step at full precision: **four of six snapped candidates score 581647.29654275789 — identical to the last bit** — from visibly different layouts, and the pre-snap leader `[11] rand12` lands 1.2e-7 *below* the plateau and loses on `>`. **The snap to whole seconds is a many-to-one map on score, so "the best snapped candidate" is settled by the sort order of a PRE-snap score the snap has already erased.** (3) Hopping every tied entrant: 582662.018 / 582662.018 / 582455.531 / 581789.158 — **spread 872.860**. `basinHop` is the only pass that can tell these layouts apart and it runs AFTER the pick: *the pass that chooses the entrant cannot see the difference; the pass that can see it does not get to choose.* **Corpus-wide: 9/25 cases pick the entrant off a plateau of ≥2 distinct layouts; 2 of the 9 BITE** — `5:40` (872.860, 0.1498%) and Kael'thas (380.235, 0.0616%) — **and they are the two most expensive cases**, which is causal, not luck: more presses ⇒ more near-equivalent layouts ⇒ wider plateaus. KT names the mechanism outright: `[13] rand14` differs from `[5] phase@105` by **one second** on Berserking (`[106,286]` vs `[105,285]`) and its hop output equals its input — basinHop found NOTHING — versus +380 from the other. Because every teleport anchor is derived from the *current champion's own* presses (`index.html:1296–1312`) and `teleportRep` moves every press at second X together, an aligned layout is exactly one teleport away **if the anchor exists**; shift one press by one second and the hop goes blind. **`basinHop`'s reach is entrant-dependent by construction, and the plateau hands it the entrant at random.** ★★ **The tolerance needed for the fix is not a tuned constant:** margins from the plateau max to the first non-tied candidate are either **≤1.2e-10** (one number computed two ways) or **≥1.7467** across all 25 — ten orders of magnitude of empty space — so `1e-9·|val|` is float-noise detection. **THEN THE FIX WAS BUILT AND RUN, and the verdict is mostly NEGATIVE — which is the more valuable half.** Hopping the whole plateau leaves the emitted plan **BIT-IDENTICAL on BOTH biting cases**: `5:40` 582688.620970 → 582688.620970 (post-hop spread 872.860) and Kael'thas 617037.203958 → 617037.203958 (spread 380.235). ★★★ **THE DOWNSTREAM PASS STACK ABSORBS THE ENTIRE POST-HOP SPREAD** — the groom/tie-break/finishing passes converge visibly different entrants onto the same final plan, so an 872-point difference at an internal boundary reaches the user as **zero**. **Therefore a mid-pipeline measurement is not evidence about the output**: the plateau is real, the spread is real, the mechanism is proven, and none of it surfaces. Same defect class as row 235's unreachable guard, rotated — there a guard that could not fire, here a *finding* that could not surface. The fix's only measured gain is **+13.716 on `4:00 lust 0:05`** (428257.758 → 428271.475), and it comes from the **tolerance** (that case's margin is 5.8e-11, so `===` already separated it), **not** from hopping the exact-tie plateau — the one benefit and the headline mechanism are unrelated. **NOT LANDED:** ≈+13.6% CPU for one cell, with the stated justification measured inert. ★★ **Two follow-on hypotheses also FALSIFIED, so `5:40` is still unexplained:** (a) the fix does *not* make it seed-robust — under `mulberry32(4242)` it yields 582455.531, the identical −233.090, because at that seed the *entire plateau* tops out 233 below and there is no good entrant to hop; (b) the good layout is *not* a missing "align-on-Lust-then-chain" seed class — built explicitly, `align@0 → 582553.499` and `align@5 → 582455.531`, neither reaching 582662; the tell is that a clean cd-chain from Lust gives `[5,125,245]` while the good plan is `[5,127,307]`, a +2s offset and a skipped use, i.e. a cast-boundary/ramp artifact rather than an alignment rule. **Three candidate mechanisms tried and killed; the cell stays an open SEARCH-MISS.** ★ **Carried caution, now MEASURED rather than suspected: a higher post-hop value is not a lead, it is noise, unless it survives to the emitted plan.** ★ **THE SHAPE OF THE ORIGINAL ERROR, which is the transferable part:** a per-cell *symptom* (this cell is fragile to restart draws) was read as a per-cell *cause* (one draw finds a basin), skipping the four passes between a draw and the emitted plan. **A change in output under a perturbation localises the SYMPTOM, never the MECHANISM** — to localise a mechanism you must open the pipeline and watch each stage. The sensitivity was real; the mechanism was invented. | PHASE9 §5.11 (new); §5.10's closing subsection retracted in place, kept visible for the lesson. |
| 07-25 | Ladder item **0c** — the groom loop runs 3 rounds unconditionally while its sibling fixpoint 1100 lines away already breaks on a no-op. §5.8's census said the guard `if (groom >= 1 && unchanged) break;` would fire in **19 of 25 cases** and that no execution in the corpus shows the falsifying pattern (round-1 no-op → round-2 change) — but that is 50 samples of a universal claim, not a proof. | **Measured on the full 25: plan-neutral, −10.1% CPU.** `PLAN-DIFF compared=25 changed=0 … IDENTICAL`; `DUEL: NO CHANGED CELLS`; CPU 695.1s → **625.1s**, wall 362.7s → 333.8s at 2 jobs. **The proof is structural, not statistical:** rounds ≥1 are the SAME deterministic function of `s` — each begins with `await challengePass()` and round 0 is the only one that skips it — so a round that returned `s` unchanged means round N+1 starts from a bit-identical state and must reproduce it. `groom >= 1` is not a safety margin, it is exactly the condition under which the two rounds are the same function. **The one audit hole was closed by reading all 8 `val =` sites in the groom window:** 7 are `Math.max(...)` and the single bare assignment (`val = pick.v`, `:1894`) is guarded by `rr3.robust > val + QTOL` and coupled to `s = pick.rep`, so it fires only when it RAISES `val` and only with the `s` that earned it — `val` is non-decreasing, so no out-of-`s` state can loosen a later accept. The saving lands exactly where §5.6 predicts: `7:20 lust 0:05` −23.5s (−12.8%) and Kael'thas −15.1s (−12.5%) are 38.6s of the 70s; the apparent +8–11% on the shortest cases is ≤0.6s absolute (jitter, plans bit-identical). ★ Also the one case where the QUICK tier would have been **legitimate** — a control-flow skip is not a search change, so §5.10's blind-spot correlation does not apply; the full 25 was run anyway because it doubles as the perf measurement. ★ **Footnote worth keeping:** §5.11's plateau fix was estimated at ≈+13.6% CPU and provisionally accepted on "correctness outranks speed" — i.e. it would have consumed this whole saving, and it then measured out at +13.716 points on 1 of 25 cases. Landed together the ledger would have read ~0% CPU change for ~0 plan change and called it a correctness fix paying for itself. **Measuring them separately is the only reason either verdict is legible.** | PHASE9 §5.12 (new); §4.17(1), §5.8(1). |
| 07-25 | The `5:40 lust 0:05` seed fragility had three killed hypotheses and **no mechanism**; row 237 could say only that *"whatever manufactures it lives in the downstream pass stack."* Each of the three was a guess about search dynamics checked by **perturbing an input**. | **★★★ LOCALISED — by instrumenting the STEPS instead of perturbing the input.** A stage-traced scratch copy (`$SP/mkstage.py` + `stage-run.mjs`: 6 snap candidates · post-main-hop · post-groom · each of `normalize`'s four components · every re-hop round · the drop-one and Cold-Snap branches · pre/post final canonicalization, with the PRNG seed overridable so good and bad runs are the same binary) names it exactly: the good plan is manufactured by **`rehop#1`** — the *second* round of the re-hop↔canonicalize fixpoint at `index.html:2785` — not by the seeds, not by the snap, not by the main basin hop. At 1337 `rehop#0` lands `{4,126,306}` and `rehop#1` shifts it +1 to **`[5,127,307]`, +159.800**; at 4242 the entrant is the `190/310` family, round 1 reports `stable` and the loop **exits after two rounds instead of three**. The winner is a genuine **polish FIXED POINT** (`repair` = `polish` = 582688.621), so the miss is purely one of **reach**, not scoring. Three further hypotheses died on the same trace's evidence — CS-press-slide (−233.090), LOCKSTEP-STACK seed (−135.122), and the CS-FREE-SLOT family widening (ties at +0.000 across 78 candidates; **2** of 25 corpus cases gain anything, 0.011 % and 0.0024 %). Six mechanisms now killed; two leads carried: **(L1)** `stable` tests whether the *hop* improved but `normalize` runs *after* the test, so a canonicalization-only round ends the loop — untested, and a SEARCH change, so full-25 gate; **(L2)** re-hop anchors are entrant-derived, so the `190/310` family may be unable to reach `127/307` at any round budget. ★★★ **Lesson, which outranks the finding: when the question is "which step does X", instrument the steps.** Three perturbation experiments over two sessions produced three wrong mechanisms and zero localisation; one stage trace produced the exact pass, round and entrant, and cost less than any one of them. *A perturbation localises the SYMPTOM, never the MECHANISM.* | PHASE9 §5.13; `$SP/mkstage.py`, `stage-run.mjs`, `cs-align.mjs`, `stack-seed.mjs`, `cs-free.mjs`. |
| 07-25 | The **REPRICING TRAP** was on the books as a *sim-side* rule (rows 219/226/229/232; TOOLING): don't subtract two rounds' recorded scores. Understood as a fact about harness input fixes. | **★★ Re-entered it inside my own instrument, one section after quoting it — and the contaminated column was the FLATTERING one.** `cs-free.mjs` compared `polish(repair(candidate))` against the score the **sweep recorded** for the shipped plan, and printed **7 of 25 "★ BEATS shipped"**. False: a shipped plan does not carry `simulate()`'s maximum for its own layout, it carries the **legibility trade grooming paid for it** (`castVal/8` on a non-use-adding Cold Snap, `EPS = 0.5`, `coPressAlign`'s pressability give-back), so `polish(repair(shipped))` out-scores the recorded number **with nothing changed** — on 7 of 25 cases, by up to +344. **Five of the seven "wins" were the baseline out-scoring itself**; against the honest baseline (the shipped plan through the *identical* treatment the candidates get) the count is **2**, both noise-scale. The tell was in the output all along — those five rows were tagged `via (shipped)`, i.e. no candidate won. **The rule generalises past the sim: never compare a freshly-computed score against a RECORDED one; if two numbers in one comparison came out of different code paths, the comparison is not a measurement.** Dangerous precisely because it *manufactures wins* — nothing about the output looks broken, so it reads as a discovery. | PHASE9 §5.13 (hypothesis 6 + its ⚠ subsection); TOOLING (model-side statement added beside the sim-side one). |
| 07-25 | **(L1)** — row 239 localised the `5:40` miss to the re-hop fixpoint and named the likeliest cause: `stable` tests whether the **hop** improved, but `normalize` runs *after* the test, so a canonicalization-only round is graded stable and ends the loop. At 4242 it exits after two rounds; at 1337 the third round is where the +159.8 lands. The lead was filed as untested-but-live. | **FALSIFIED at single-case cost, with both controls.** Three scratch engines (`$SP/mkL1.py`, never the repo's `index.html`): `L1base` (seed override only), `L1fix` (`stable` → a real layout fixpoint, `sigOf(s2)===sigOf(s)`), `L1r6` (round budget 3→6). Positive control, seed 1337: **all three** return 582688.620970 / `icyVeins[5,127,307]` — neither variant *loses* the good plan. Negative control, seed 4242: **all three** return 582455.530709 with **byte-identical layouts**, −233.090. **The round budget was never the binding constraint** — the loop was not being cut short, it had genuinely converged. `stable` firing early is a true observation about the *test* and an irrelevant one about the *case*. Same pass also **retracts hypothesis 6's `5:40` row as VACUOUS**: `cs-free.mjs` derives its candidate family from the case's own baseline, and `5:40`'s baseline **is the winner**, so the family was generated from the target and could only tie — a test that cannot fail measures nothing (the repricing trap's sibling: two sides that are not independent). The real reason that family misses is sharper and feeds (L2): **127 is not a co-press second of the 4242 entrant at all** — the nearest are 124 (`isc[4]+120`) and `basinHop`'s 5 s-grid snap 125, which measure 582400.409 with polish dragging IV back to 189. **Sole surviving lead (L2), now nameable:** `basinHop` teleports **one** window per move on strict improvement only, while the winner needs a coordinated **3-move** (`0→5`, `190→127`, `310→307`) — the textbook local-search wall, which is *why* no round budget escapes it; and its anchor alphabet snaps natural next ticks to a **5 s grid** (`index.html:1297`), putting 127 out of reach. Candidate fix as a **rule, not a hand-fitted anchor**: snap next-tick anchors to **cast boundaries** instead of the grid. SEARCH change ⇒ full-25 gate. | PHASE9 §5.13 ((L1) FALSIFIED table + (L2) subsection); Open debts (g). |
| 07-25 | **(L2a)**, proposed in the row directly above — `basinHop`'s natural-next-tick anchors are snapped to a 5 s grid (`index.html:1297`), so `isc[4]+120 = 124` becomes 125 and the winner's second **127** is unreachable; fix = snap anchors to cast boundaries. It was the most attractive lead left: a real code fact, a real missing second, and a fix expressible as a rule rather than a hand-fitted anchor. | **★★ FALSIFIED — and by refusing to argue about WHICH anchors are missing.** Instead of adding 127 and re-running, hand the hop **the entire alphabet**, every second `0..T−1`, from the stuck 4242 plan (`$SP/l2-reach.mjs`): **+0.000**, across 2712 pairs and 1769 polishes, best single move a tie. Adding just 127, or 5+127+307, likewise **+0.000**. Both controls held to the bit (stuck repairs to 582455.530709; winner polishes to 582688.620970, fixed point). And `teleportRep` (`:1260`) is already a **GROUP** move — every non-fixed press at second X shifts together — so move *arity* was not the wall either. The stuck plan is a **strict local optimum over the complete single-move neighbourhood**: no anchor rule of any kind could ever have helped, and cast-boundary snapping would have been pure wasted engine work. **★ ROOT CAUSE, by elimination with every alternative independently falsified (anchors, rounds, scorer, operators): a MISSING SEED CLASS.** If no move reaches the basin, the basin must be **entered**. Decoded structurally rather than numerically, the winner is unremarkable — its press seconds are a chain whose gaps are drawn from the kit's own cooldowns (`5 —+120→ 125 —+180→ 305`, `polish` supplying the +2 to 127/307), with every track pressing at each group second it is UP for, Cold Snap buying Icy Veins one extra use, and a haste track permitted to **skip the opening group**. That last clause is precisely what hypothesis (e) got wrong: (e) advanced greedily on the **shortest** cd and put group 3 at 245, while the winner **skips a use it could have taken, to stay stacked.** Seeded from that rule (80 candidates, **no hand-fitted seconds** — 125 and 305 fall out of cooldown arithmetic): **582662.018**, +206.5 over stuck, structure exactly right down to `berserking[127,307]`; then `basinHop` **with its own existing, unmodified anchors** closes the last 26.6 to **582688.620970 — the winner, exactly** — in 3 rounds / 66 polishes / **5 s**. A `GROUP NUDGE` operator (shift a whole press-second group by ±1/±2/±3) was built too and is **not needed**: +16.9 alone, never reaches. **Lesson, and it is the cheap one:** when a search fails, the reachability question (*can the operators get there from here, given EVERY anchor?*) is decisive, costs 89 s, and is worth asking **before** designing any fix to the operators. Three of this debt's eight killed mechanisms would never have been written down had it been asked first. | PHASE9 §5.13 ((L2a) FALSIFIED table + the seed-class subsection); Open debts (h). |
| 07-25 | The fix from row 242 (`groupSeeds`) was built and gated the way every SEARCH change in this phase has been gated — `plan-sweep` on all 25 plus `plan-diff`. It came back **`PLAN-DIFF compared=25 changed=0 … IDENTICAL`**, byte-identical on every case. On the phase's own reading rules that is the signature of an inert change, and the natural conclusion was that the seed class does nothing. | **★★★ The differ is STRUCTURALLY BLIND to this change, and IDENTICAL was the correct result.** `plan-sweep` varies *the case* and holds *the PRNG seed* fixed at 1337 — and §5.10 had already established that at 1337 `5:40` finds the winner **by luck** ("ONE lucky restart out of 14"). A fix whose entire content is *stop depending on the seed* cannot move a seed-1337 corpus: not weakly visible, **invisible by construction**. The instrument that varies the right axis is a seed sweep, and it shows `5:40` going **2 of 6 seeds correct → 6 of 6** (+233.090 at three of them), the identical layout every time, and *faster* on the seeds it fixes (19–22 s vs 31–52 s — the good entrant reaches its fixpoint in fewer hop rounds than the bad one spends failing to escape). Control on the same axis: 3 other cases × 3 seeds, **every cell bit-identical**, zero regressions. Running the corpus differ was still right (it proves the other 24 cases did not move); reading it as *the verdict* would have discarded a working fix as a no-op. ★★ **A second measured rejection inside the same change: a SEED'S RAW SCORE IS NOT A PROXY FOR ITS BASIN.** Pre-scoring the family and polishing only the top-K is the obvious economy and it is self-defeating — the polish-best sits at raw rank **13/40** on `7:20` and **12/12, dead last**, on both lust-4:20 cases; a top-3 cut loses **8087.794** corpus-wide, top-1 loses 24814.300. These seeds exist *because* the good layout is unreachable by local moves, so ranking them by the local objective they were written to escape selects against exactly the ones that work. Generation is 88 ms for all 25 cases; the polish is the expense; **the cheap thing cannot be used to budget the expensive one.** **Lesson: choose the instrument by the AXIS THE CHANGE ACTS ON — a gate that holds a variable fixed cannot grade a change to that variable's influence.** | PHASE9 §5.14 (new); RULES §4b (new); ARCHITECTURE (seeds + `groupSeeds`); Open debts (a) CLOSED. |
| 07-25 | The two Phase-9 landings were each measured against pristine and their CPU savings quoted as headline numbers off those tables (groom exit −​10.1%, from 695.1 s → 625.1 s at `jobs=2`). Absolute sweep CPU was treated as a stable property of the engine, comparable from one table to the next. | **Two corrections, both from the interaction gate that neither change had had.** (1) **They were never measured TOGETHER.** `groupSeeds` adds entrants; the groom exit changes when a round stops improving them; "each is plan-neutral alone" does not imply the pair is. Swept back-to-back vs `HEAD` on idle cores: **`PLAN-DIFF compared=25 changed=0 … IDENTICAL`**, and **−8.5% CPU / −5.3% wall** — *less* than the groom exit alone, which is the right sign, because `groupSeeds` buys robustness with up to 40 extra polishes on the longest case. The honest ledger entry is the **netted** figure, not two headlines that each claim the whole saving. (2) **★ ABSOLUTE CPU IS NOT COMPARABLE ACROSS SWEEPS.** The *same pristine engine* measures **695.1 s at `jobs=2` (contended) and 635 s at `jobs=3` (idle)** — nothing in the file changed and the number moved 9%, which is larger than most wins this phase is chasing. Every perf claim must be a delta against a baseline swept **in the same session, at the same `jobs`, under the same load**; a figure lifted out of one table and set beside a figure in another measures the machine, not the change. | PHASE9 §5.14 (interaction gate); ROADMAP P9. |
| 07-25 | The acceptance banner's `B FAILS on 34/36 tables` is the phase's work list — 34 tables failing means the model's haste adaptation is broadly wrong, and the ~11 "length-robust (long/xl)" loci `xval-verify.mjs` prints are where to dig. | **★★★ The banner is an EXISTENCE test evaluated at a near-perfect tie, and it is close to arithmetically forced.** B asks "does ANY rival beat native in ANY column" — ~10 columns × ~9 rivals ≈ **90 comparisons per table** — and in the 210 columns where the diagonal *wins*, its **median winning margin is 0.003%**, an order of magnitude BELOW the harness's own CRN/10k resolution (~0.02%, PHASE7 §5.4). At the observed 39.1% per-column borrowed-win rate, an existence test over ~10 columns returns DEFICIT with probability `1 − 0.609¹⁰ ≈ 99.3%`; observed CLEAN is 2/36 = 5.6%, i.e. **better** than that null. So the banner **would read `B FAILS` for a converged model and a broken one alike** — the mirror of this ledger's own rule that a test which cannot fail measures nothing. The positive half is equally load-bearing: pure noise would give ~99.8% borrowed-win columns, not 39.1%, so **the diagonal is genuinely dominant in the majority of columns**; and the failures are LOCAL — of 135, **0** have a byte-identical borrower, **80 (59.3%) borrow from the immediately adjacent haste**, 114 (84.4%) from ≤2 grid steps, 34.1% of margins are ≤0.02%. The "length-robust" list is not a persistence test either — it means "appears in a long or xl table", which for a near-tie is a coin flip that landed on a long fight. The **real** test has one possible shape (one specific rival better at MOST fight lengths) and needs no thresholds: run unrigged over all 57 kit-columns it names **exactly 2** (`isc-mqg h40` ← rival h70, 5/5; `isc-skull h20` ← rival h100, 4/5). **The bar stays zero columns (user-directed); the WORK LIST is two.** | `tools/xval-persist.mjs` (committed instrument, controlled 4 ways); ACCEPTANCE "What the B BANNER can and cannot tell you"; PHASE7 §5.15b. |
| 07-25 | Having seen round 5, a three-filter sieve was the way to reduce 34 failing tables to something tractable: keep a column only if the rival wins ≥4/5 lengths **AND** borrows from ≥2 grid steps away **AND** the worst margin is ≥0.10%. It named four columns and they looked like a credible target list. | **★★★ Thresholds chosen AFTER seeing the data are not evidence — and here they were simply wrong.** The threshold-free consistent-alternative test disagrees **completely**: all four sieve survivors FAIL it (best 3/5, with magnitudes inconsistent by 20× — `isc-skull h130`'s rival h185 gives 0.009% / 0.014% / 0.279%), while **both** columns the unrigged test names had been *rejected* by the sieve — `isc-mqg h40` killed by the distance filter (maxDist only 1 step), `isc-skull h20` by the magnitude filter (worst 0.087%). Two of the three filters were actively harmful: borrower *distance* is a proxy for nothing (a genuinely better layout can live one grid step away), and a magnitude floor discards exactly the consistent-but-small defect that a *persistent* rival produces. Nothing from the sieve is recorded as a finding. **Lesson: prefer the test that names the only physically possible SHAPE of the defect — a length-persistent error is a property of the (kit, haste) cell, so it must beat native at most lengths — over any filter stack you tuned to the answer you already saw.** | PHASE7 §5.15c; ACCEPTANCE §6 of the banner section. |
| 07-25 | The remaining explanation for the diagonal deficit was the harness gear: the model side had been optimizing for `sp: 1387` with no `t5two` — a mage the sim does not run (PHASE8 §6/§7/§20) — so correcting it (round 5) was expected to move the verdict, and PHASE8 §20.2's pre-flight said the plans themselves would barely move ("rank-neutral"). | **Both halves were wrong, in opposite directions.** The repricing was **large**: `xval-round-diff` reports **124 of 345 plan cells changed (35.9%)** across 34/36 tables, on a −0.4…−1.1% `eff` level shift (KT −6.1…−6.8%) — so the "rank-neutral, plans won't move" pre-flight is **withdrawn** (it was one fight family at four hastes; the round is 36 fights at 7–11 hastes). And the verdict moved **not at all**: `deficitTables 34→34`, **zero verdict flips**, `monoDip` 0.00% on both sides. **★★★ So the B failure is NOT a reference-gear artifact** — correct the gear and the same 34 tables fail by the same widths. A third finding falls out: **per-table widths wander BOTH ways under a pure repricing** (12 improved / 8 worsened / 16 unchanged; net 0.42 pp over 36 tables), including `scb-skull-short` 0.02%→**0.23%** and `scb-mqg-short` 0.03%→**0.21%** — a table can go from essentially-clean to among-the-worst **with no model change at all**, so round-over-round headline drift (142→135 columns, 0.40%→0.38%) must not be read as convergence. Same trap as TOOLING's *"DUEL what changed"*, one level up: it applies to whole rounds, not just cells. | PHASE7 §5.15a; ACCEPTANCE "Current status (round 5)". |
| 07-25 | With the sieve dead (row 264), the way to route the two persistent columns was the tool built for exactly that: `tools/diagnose-deficit.mjs`, which partitions each deficit into **SEARCH-MISS** (the model would prefer the rival if it saw it ⇒ fix the search) vs **SCORER-GAP** (the model genuinely ranks the rival lower ⇒ fix the pricing). Open debts still pointed at it as the next step. | **★★★ THE PARTITION IS TAUTOLOGICAL ON ANY POOLED ROUND — and P7.3 is what emptied it, silently, two tasks after P7.2 built it.** `xval.mjs:176-190` turns cross-haste pooling ON by default (`const POOL = POOL_ENV !== '0'`) and its own comment states the consequence: *"no borrowed plan can out-score the native."* The emitted native plan **is** the model-argmax over the union of all cross-haste champions, so `model(B,H) ≤ model(N,H)` holds **by construction** and SEARCH-MISS is arithmetically forced to zero. Measured on round 5: **`SEARCH-MISS 0 · SEARCH-MISS(other) 0 · SCORER-GAP 135`**, with `modelMargin > 0` in **0 of 135** and `modelBest === modelN` in all 135. That is not a result about the optimizer — it is **B1 restated**. Same "a test that cannot fail measures nothing" shape as the B banner (row 263), the vacuous `cs-free` family (row 259), and the unreachable golden guard (row 253) — the fourth instance this phase, and the first where **one landed fix invalidated another task's instrument with nothing in the repo saying so.** Routing work between search and scorer again requires a `POOL=0` round. ⚠ **What the tool still measures honestly on a pooled directory is the model MARGIN and the track-level diff** — *how close* the model thinks the two layouts are, and *what* differs — which is what §5.16 actually used it for: of the 135, **132 have identical press counts on every track** (pure retimings; only 3 structural), model `|Δ|/N` median **0.035%** / max 0.376%, **14 exact ties**, and **58/135 at or under the ±0.02% ruler**. ★ **The load-bearing number is 58/135: where the model's own margin is below the resolution, the model has no opinion to be wrong about, so no scorer term and no search fix can reach those columns — they are a METRIC-DESIGN problem.** The other 77 carry a real model preference a scorer term could address. ⚠ And "retiming" is a claim about press *counts*, not about size: per-row worst press shift is median **24 s**, max **360 s**. | PHASE7 §5.16 (new, a–d); ACCEPTANCE (B1 bullet ⚠ · banner item 4 · instruments list); the ⚠⚠⚠ header block in `tools/diagnose-deficit.mjs` itself; Open debts amended. |
| 07-25 | Reading PHASE8 §8's prose ("the surviving residual has the WRONG SIGN for B2"), the sign looked inverted — the numbers seemed to support B2 rather than falsify it — which would have re-opened the SP-under-haste candidate that §8 killed and put §13.8's correction in doubt. | **DISPROVED — §8 and §13.8 are correct as written; the suspicion came from not reading §12's window table.** `err = model − sim`; h0: 1.321 − 1.401 = **−0.080**, h300: 1.316 − 1.108 = **+0.208**. The mechanism I had overlooked is that h40's `isc@29` is a **perfect-fit** window — `D/Δ = 20.000` exactly, `frac = 0`, lost ABs **0** — so h40's larger *total* floor loss (0.140% vs h70's 0.104%) comes from deeper **AP** fractions (0.786, 0.577 vs 0.254, 0.577), and the floor term is therefore **anti-B2 by 0.036 pp**, exactly as §8 says. Nothing to correct in the phase doc. ★ **Lesson: a sign read off a section's PROSE is not a measurement.** §12's own banner already records one sign-reading error in this family; this is the second, caught only by recomputing from the window table. Recording it so the next reader of §8 doesn't spend the same hour. | PHASE8 §8 / §12 banner / §13.8 (all unchanged — this row exists to stop a third re-litigation). |
| 07-25 | Two statistics and one direction about the 135 columns were carried forward in working notes and about to be written into a living doc: ratio sim%/model% median **1.4**, max press shift **122 s**, and *"native rides the SP trinket ON the haste stack; the sim prefers it bare."* | **All three WRONG, caught only by recomputing every number before writing it.** Re-derived from `dd5.json`: ratio median **1.18** (n=121), max shift **360 s** (per-row worst-shift median 24 s), and the stacking direction is **exactly BACKWARDS** — native puts the on-use damage/SP trinket **OFF** the haste stack (`isc-mqg` h40 `Icon[29,183]`, `isc-skull` short `Skull[69]`) while the sim's preferred rival **stacks it ON** (`Icon[5,182]` with AP/Zerk/IV all at 5; `Skull[0]` with `IV[0,20]`). The 122→360 s correction also changed the *framing* — it is what forced the ⚠ "retiming ≠ small" caveat into §5.16(b) and the banner. And the corrected direction is capped at **2 of 4 rows** (the other two are an MQG placement swap and a near-uniform +1 s translation), so it is explicitly **not** promoted to a rule. ★ **Rule for future sessions: trust no statistic you did not just recompute, especially one that survived a context compaction — a summary preserves the *claim* and drops the *derivation*, so a wrong number arrives looking exactly like a right one.** This is the cheap version of row 258's repricing trap: there, two numbers from different code paths; here, one number from no code path at all. | PHASE7 §5.16(b)(d); ACCEPTANCE banner item 4. |
| 07-25 | PHASE8's B2 was a single anomalous column (`isc-mqg medlong @sim70`), and round 5's #1 persist target (`isc-mqg h40`, rival `plan@h70`, 5/5) was a separate low-haste placement issue belonging to the low-haste debt. | **★★★ THEY ARE THE SAME TWO PLANS IN THE SAME TABLE, ONE COLUMN APART, AND THE PREFERENCE IS CROSSED.** Off one round-5 matrix: `@sim40` native@40 2731.4 vs **plan@70 2738.5** (+0.260%); `@sim70` native@70 2786.5 vs **plan@40 2787.5** (+0.036%). **Each plan wins at the other's native haste ⇒ swapping them wins BOTH columns.** Two consequences. (1) **The search is exonerated by construction** — both plans are in the pooled champion set, so the model looked straight at the better layout and ranked it lower, twice, in opposite directions; this is the concrete case where the row above's degenerate SCORER-GAP verdict is not an artifact but correct. (2) **B2 itself has collapsed to the resolution boundary and its mirror has not:** round 4 archived `DEFICIT 0.40% [@sim70: plan@40 (2787.5) > native@70 (2776.5)]`; under the §6/§7 harness correction `plan@40` held at **2787.5 exactly** while `native@70` rose to 2786.5, leaving B2 at **+0.036%** (precisely §12's 0.036 pp) against the mirror's **0.260%** at 5/5 lengths. **⇒ Every future B2 probe runs on the `@sim40` column; the `@sim70` column no longer has the signal to grade a fix by.** ⚠ Two guardrails on the reading: the layout contrast is *grouped-at-t=5* (rival) vs *staggered open* (native), i.e. the model wants staggered at low haste and grouped at high while the sim wants the reverse — the shape a mis-weighted stacked-haste/GCD-floor term would make (§18.6's named mechanism), but **post-hoc**, and the model is **not** floor-blind (`GCD_FLOOR` applied at `index.html:819,903`; overcap passes at `:2466`,`:2521`), so any candidate is mis-weighted, never missing. And it is a **plateau**: `plan@110` is within 0.02% of the winner at *both* columns, so a pre-registered test must predict the **ordering of the ridge**, not a single winner. §19.10's L1 ("never inverts a *resolvable* ranking") survives — the h40 model margin is 0.0145%, under the ruler; the new handle is the **two-sidedness**, since a near-tie erring in the same direction at both hastes is not noise-shaped. | PHASE8 §20.8 (new); Open debts (the low-haste narrowing now names B2's mirror). |
| 07-25 | **The open debt "haste covering the opening ramp is under-credited by ~+0.079 pp"** — on the books since PHASE8 §15.5 F5, recorded in RULES §3 and ROADMAP as *"a genuine defect in that axiom"*, with a patch queued and a pre-registered test (PHASE7 §5.16c). The evidence was strong and one-sided: 6/6 haste legs positive in the sim, mean +0.079 pp, against a model that pays **exactly 0.0000**. | **★★★ THE MODEL IS STRUCTURALLY RIGHT — the "defect" was a REGIME the measurement never entered.** The model's ramp-coverage credit is *exactly floor-slack recovery*, and `tools/ramp-marginal.mjs` (decision rule pre-registered in its header, so neither outcome could be re-framed after the fact) splits it cleanly: **Regime A, floor-free ⇒ EXACTLY 0.0000 pp on all 6 legs, with bit-identical `robust` values** (`156409.113097` six times for IV@R=40) — and that is the **correct** answer, provable by algebra: coverage `= 3 + (Dh + T − Rb − D)/c` and interior `= 3 + (Dh − Rb + T − D)/c` are the *same expression* (both 68.000 casts at T=100). **Regime B, Lust@0 with the floor binding ⇒ the model PAYS**: +0.3298 (IV R=40) / +0.3455 (MQG R=40) / +0.4063 (IV R=70) / +0.4077 (MQG R=70) pp, monotone in the wasted slack (0.062→0.086 s/cast). **The within-regime control is what makes it a mechanism rather than a correlation: Berserking under the same Lust reads exactly 0.0000 at both hastes** — its steady cast (1.023/1.004 s) never crosses the 1.0 s floor. Same Lust, same ramp, weaker haste buff ⇒ zero. And an independent hand derivation predicted **+0.27 casts** for IV+Lust@R=40 where the probe measures **+0.296** — two derivations, one number. **F5's flat reading came from a SINGLE-BUFF, therefore FLOOR-FREE sweep** (IV at R=40: m = 1.0254×1.20 = 1.230 ⇒ steady 1.219 s, 0.219 s of slack) — the one regime where 0.000 is right. RULES:108-109 had already stated the mechanism in words (*"Above the floor the pull gains only via floor headroom"*); nobody checked whether the code implemented it, and it did (`index.html:797-822` applies window haste to the ramp's cast durations). ⇒ **Debt cancelled, patch cancelled, `index.html:919-931` correct as written.** The sim's +0.079 pp is now a residual with **no identified mechanism** (0.054 casts on 68) ⇒ a **sim-setup audit trigger** per CLAUDE.md, not a model bug (PHASE8 §7 forbids encoding a sim lattice artifact). ★ **Lesson: a constant measured on one side of a piecewise boundary is not a constant.** The axiom was *conditional* (`max(GCD_FLOOR, …)` puts a kink in it), the sweep sampled one side, and "flat to 0.0000 pp across the whole interior" was read as a statement about all inputs. Before calling a 0.000 a *gap*, ask which regime the instrument was standing in. | RULES §3 (bullet rewritten); ROADMAP P8 round 5 (retraction); PHASE7 §5.16c (RESOLVED block appended); ACCEPTANCE (round-5 status); Open debts (the ramp bullet, now WITHDRAWN); `tools/ramp-marginal.mjs`. |
| 07-25 | With the ramp mechanism dead, round 5's **worst non-KT column** — `isc-skull short T=99 @sim40 ← plan@70`, sim **0.362%**, model **0.006%**, differing in exactly one track (`IV[8,28]` vs `[0,20]`) — had **no explanation at all**. It had been the debt's "named largest-consequence cell". | **★★★ IT IS THE TAIL-LATTICE RIPPLE, AND IT IS AN INSTRUMENT ARTIFACT.** First, the cell was never a ramp cell: **both layouts are floor-free at the opening** (IV alone at R=40 ⇒ m=1.2304, steady 1.219 s, 0.219 s slack), so the model's 0.006% is *correct physics*. The real gap is one the objective-width match had hidden: `KILL_WINDOW = 0.5` (`index.html:717`) matches xval's `--var 0.5` in **width** but not in **kind** — the sim's expected damage under a uniform kill in `[T−KW, T+KW]` is a **sum over integer casts**, the model computes its **continuum limit** (`:990`). Derived and verified numerically to 4 decimals, that residual is a sawtooth of peak-to-peak **`1 − W/c` casts** (`W = 2·KW = 1.0 s`, `c` = tail cast period): **exactly 0 at the GCD floor** (the built-in sanity check — at `c = W` the taper spans a whole period and smears the lattice perfectly), then 0.0225 at c=1.023 · 0.1796 at 1.219 · **0.3164 at 1.463** · 0.3750 at 1.600. A *fixed* cast count over an `N`-cast fight ⇒ the percentage scales as **1/N**, so the artifact is **short-fight-and-low-haste only** — exactly the shape of the residual deficit family. **Proved on the cell with NO sim run**, by evaluating the sim's own formula on the model's own cast list (the wowsims runner had been lost with the ephemeral scratchpad, which forced the better instrument): **continuous −0.0062%** — reproducing PHASE7's reported 0.006% *to the digit*, which validates the instrument — vs **discrete +0.6046%**, a **sign flip**, against wowsims' **+0.3617%**. The diagnostics name it outright: native's last cast completes at **99.6216 s — past `T+KW = 99.5`, weight 0, a wholly wasted cast** — while the rival's completes at **99.3041 s** (weight 0.196) and fits one more. It is the razor-edge whole-cast parity trap `xval.mjs` documents for `var=0`, **surviving at var=0.5** because the taper (1.0 s) is *narrower* than the tail cast period (1.463 s). **⇒ And `diagWorst` is POSITIVELY BIASED FOR A PERFECT MODEL:** it maxes a *two-signed* ripple over ~10 rival rows, each with an independent tail phase — Monte-Carlo (20k, seeded) gives +0.037/+0.065% at R=1 rising to **+0.094/+0.165%** at R=10 (c=1.219/1.463, n=81). So *"no length-persistent diagonal deficit"* **is not reachable by construction** on short/low-haste tables, and the criterion needs restating (a **user call**, filed in ACCEPTANCE's coverage gaps, not made unilaterally). This is the **tail** face of the same integer-vs-continuum law whose **interior-boundary** face is PHASE8's FLOOR LAW. ★ **Lesson: matching an objective's WIDTH is not matching the objective.** Both sides taper over 1.0 s and the comment in `xval.mjs` calls var=0.5 "the MODEL-MATCHED metric" — true of the width, false of sum-vs-integral, and the difference is bounded by a closed form nobody had written down. When two scorers are declared equivalent, derive the residual; don't check the parameters agree. ⚠ Also: the first ripple formulation was **wrong** (phase swept at the *front* of the lattice, so it deleted a cast from the fight and reported ~0.9995 casts at *every* period including c=1.000) — caught by the physical sanity check it violated, not by review. | RULES §8 (new ★★★ bullet); ACCEPTANCE (round-5 status rewritten + a new coverage gap); PHASE7 §5.16c RESOLVED; `tools/lattice-ripple.mjs`. |
| 07-25 | The row above hands over an obvious fix: the sim counts integer casts, the model integrates — so **make the model count integer casts**. On the disputed cell it works, decisively: the discrete sum picks the rival (+0.6046% vs the sim's +0.3617%) where the integral picks native (−0.0062%). One code path, the ranking error gone. | **★★ THE CONTROL REFUSED IT — and the control is the whole point of the row.** Scoring **all 11 plan rows** of the table at sim-haste 40 (not just the two in dispute): the discrete sum does pick the sim's argmax (row 70) where the integral picks 40 — it **fixes the disputed ranking** — and it is a **worse predictor across the column on BOTH metrics**: `r = 0.7910` / RMSE **0.2948** pp vs the integral's `r = 0.9337` / RMSE **0.2431** pp, with large **two-signed** errors (+0.669 at row 260, +0.469 at 130, −0.241 at 40). **Discretization adds VARIANCE; it does not remove BIAS.** That independently re-derives what `index.html:875-877` has said since the model was built: *"A per-cast SUM (the old model) quantised each window to a whole number of casts… that blindness produced the phantom 'press 2s before lust' gains"* — the change would have re-introduced a defect the project had already paid to remove, while the one cell it fixed applauded. **Nothing was patched; the integral stays.** ★★ **Lesson, and it is the transferable one: a fix validated on the cell that motivated it is not validated.** The disputed pair is a sample of size 2, drawn *because* it was extreme; the column is the population. Run the candidate over the population and report **both** directions — "fixes the ranking" and "worse overall" are both true here, and quoting only the first would have been an honest-looking over-claim with a real regression behind it. Sibling of row 264 (thresholds tuned after seeing the data) and row 259 (a family generated from its own target): three shapes of the same error — **the evidence must not be selected by the hypothesis.** | RULES §8 (the ⚠ clause); ACCEPTANCE (round-5 status); PHASE7 §5.16c RESOLVED; `tools/lattice-ripple.mjs` section 3, which prints the control beside the cell so the next reader cannot see one without the other. |
| 07-25 | The ripple was established **on one cell**, and the standing reading of the 135 round-5 deficit columns was that they were *one* population — a "low-haste micro-placement slack" — to be attacked cell by cell. Extending the closed form to the corpus needed the wowsims runner, which no longer exists. | **★★★ THE 135 CAN BE PRICED WITH NO SIM AT ALL, AND IT SPLITS INTO FOUR MECHANISMS.** The unlock is that every round-5 column was already measured at `--var 0.5` — the taper width the closed form assumes — so the floor is **arithmetic on the model's own cast list**: `ripplePct = 100·(1 − W/c)/Nt`, with `Nt = robust/dmg_tail` (the fight total in **tail-cast equivalents**; the tail cast is unbuffed, so a raw cast count would overstate the floor). `tools/ripple-audit.mjs` prices all 135 in seconds. **97/121 decided columns = 80.2% sit INSIDE the floor**, median deficit **0.035%** against median floor **0.134%** ⇒ the typical column is **3.8× below the instrument's own ruler**. Out-of-sample confirmation the derivation was never fitted to: the worst non-KT cell **saturates its ceiling to 0.002 pp** (0.362% vs 0.360%). But Spearman **ρ(floor, deficit) = +0.118** — positive, and weak ⇒ the floor is a **ceiling, not an explanation**; it bounds the artifact, it does not predict the deficit. And the bound is **not vacuous**: 24 columns exceed it, splitting four ways — **KT-AoE (6**, incl. the two worst overall; carries its own AoE + wall-parity channels ⇒ no scorer evidence**) · SATURATED (5**, <0.03 pp over, *expected* since the amplitude is peak-to-peak and `diagWorst` maxes over ~10 rivals**) · RESIDUAL (4**, slow tails**) · FLOOR-TAIL (9)** — and FLOOR-TAIL is the finding: its kill-edge period sits at/near the GCD floor, so **the ripple is provably ~0 (exactly 0.000% for three of them)** while the deficit is 0.040–0.166%. Its shape is the **mirror image** of the ripple-explained family — sim-haste median **240** (min 70), T median **395** (min 218) vs **110 / 218** — so **one mechanism cannot be behind both.** ⇒ The remaining acceptance target is **9 cells, not 135**, cleanest being `mqg+skull xl T=395 @265` (deficit 0.090%, floor exactly 0.000%, `c = 1.000`, `Nt = 351`, and *not* a boss row ⇒ no wall/AoE channel available to explain it away). ⚠ **Two errors made and caught inside this audit.** (1) To price the 28 cells whose tails were not a constant regime, `c` was switched to **min-over-the-last-3-casts**, reasoned as *conservative* (smaller `c` ⇒ smaller floor ⇒ tighter bound). Two independent checks killed it: the pre-registered **monotonicity self-check flipped to FAIL**, and a low-haste cell landed in FLOOR-TAIL where it makes no physical sense. The ripple is governed by the lattice spacing **at the kill edge** — the taper is only 1.0 s wide, so only casts completing in `[T−0.5, T+0.5]` can contribute, and a min can reach back into a **different buff regime**. Fixed to `last.interval`, with the min kept as a sensitivity read and any cell whose verdict *flips* between them reported **INDETERMINATE** (14 cells, held out of both buckets — no false pass in either direction). ★ **Lesson: being conservative about the NUMBER while being wrong about the DERIVATION is not conservative.** (2) The first FLOOR-TAIL write-up **asserted** the family was "high-haste / long-fight" while a low-haste cell sat in its own printed list; fixed by making the tool **compute and print** both families' haste/T distributions. ★ **Lesson: if a claim about a population is worth making, the tool should print it, not the author.** ✔ What made the result trustworthy rather than merely favourable: **five predictions pre-registered in the file header before the first run**, including a **vacuity guard** that would have returned a NULL result (*an upper bound everything satisfies is not evidence* — its second clause **did** trip; only the 80.2% coverage kept the finding informative) and a **discrimination** requirement that the KT-420 family must ESCAPE the bound (6/8 did). | ACCEPTANCE ("THE 135 DECOMPOSES" + the ⚠ what-it-does-not-license clause); RULES §8 (consequence 3); ROADMAP (round-5 status + next target); TOOLING (Pieces: `ripple-audit.mjs`, with both lessons as design notes); `tools/ripple-audit.mjs`. |
| 07-25 | Row 273 handed down the **9-cell FLOOR-TAIL family** as "the finding" and "the remaining acceptance target", on the strength of its ripple floor being provably ~0 while its deficit was 0.040–0.166%. Two mechanisms were then obvious candidates: a **tie-break** failure (the rate integral is flat past the GCD floor ⇒ the model is *indifferent* where the sim's Δ=1.0 s lattice is a staircase) or a mis-sized **floor-slack/ramp credit** (`index.html:919-931`, worth +0.33…+0.41 pp exactly when the floor binds — the right size to *be* these deficits). | **★★★ BOTH CANDIDATES DIED, AND THE TARGET LIST ITSELF WAS MIS-ORDERED — BY THE RULER.** (1) `tools/floor-plateau.mjs` re-scored **both** plans of every column at the **common** haste through one identical call and pre-registered that a tie-break failure means median |model Δ| is **SMALLER** where the floor binds. It is **LARGER** — 0.0543% vs 0.0325% — so the pre-registered **P3 falsifier fired and H_PLATEAU is dead**: at the floor the model is not indifferent, its own margin is *comparable to the sim deficit it is supposed to be blind to*. The stratification is nonetheless real (P4 self-check: time at the GCD cap **15.6% vs 5.7%**, p=0.0076), and the floored stratum's deficit excess is genuine but only **conditional on haste** — unconditionally p=0.16, because **haste is a NEGATIVE confounder** (within the slow stratum, simH≥200 med 0.020% vs simH<200 med 0.041%, p=0.021 — the ripple's own 1/N); controlling for it gives floored 0.074% vs slow 0.018%, **p=0.022**. (2) The ramp-credit candidate **died on sign**: ρ(capFrac, deficit) = **−0.135**, i.e. *more* time at the cap ⇒ *smaller* deficit. (3) ★★★ The decisive move was changing the **currency**. A cross-val column reports `pct` = how much the rival won *in the sim*, which hides the fact that the model had an opinion too; the rankings are apart by the **sum**, `joint = dModel + pct`, and that is the gap a fix must close. Ranked by `pct` the over-floor families put FLOOR-TAIL first; ranked by `joint` it is **LAST of the four** — `inside` **0.081 pp** · FLOOR-TAIL **0.106 (1.30×, inside the homogeneity band)** · KT-AoE **0.236 (2.90×)** · SATURATED **0.268 (3.29×)** · RESIDUAL **0.274 (3.37×)**. FLOOR-TAIL looked sharp only because its *own* floor is 0.022 pp, **6× below `inside`'s 0.139** — an ordinary gap with no ruler over it. ⇒ **the remainder is the 15 cells of KT-AoE + SATURATED + RESIDUAL, not the 9.** `tools/ambient-gap.mjs` pre-registered the stronger claim (`H_AMBIENT`: one homogeneous ~0.1 pp scale, the floor merely masking it) and **that is falsified too** (spread **3.37×** ⇒ at least one localized mechanism remains). Two corollaries: the ambient joint gap is mostly `dModel` (median `pct` inside the floor is only 0.025 pp) ⇒ **the model is routinely ~0.06 pp more confident than the sim confirms, corpus-wide**; and `SATURATED` must be **re-derived** — it is *defined* by ceiling-proximity, not smallness, so row 273's "confirmatory, not a defect signal" was inferred from the definition while in the joint currency it is the second-worst family. ⚠ **Three errors.** (1) **An assumption was smuggled into a verdict string:** `ambient-gap`'s A4 falsifier text asserted that a failure would mean *the floored corner* carried the excess — because that was the family under investigation. A4 fired and the blame lay elsewhere; leaving it would have planted a false claim inside a committed tool. Fixed by making the report **compute** which labels break the band, correction dated in-file, test untouched. ★ **Lesson: pre-registration must fix the TEST, not narrate the expected explanation.** (2) The analysis opened on the premise that `skull` was a **spell-power** trinket and built a story about stacking SP into AP's ×1.30 window; `index.html:608` says `kind:"rating"`, **+175 haste** — which inverted it (`mqg+skull` is haste+haste, and at h=265 that is exactly what clamps `c` to 1.000). ★ **Lesson: check the buff table before reasoning about a kit.** (3) "All 9 FLOOR-TAIL cells contain `skull`" (base rate 51.9%, naive p=2.7e-3) looked like a trinket effect; it is **near-tautological**, since skull's rating is *what floors the edge*. Killed with a discriminating check rather than more confirmation: **off the floor, skull cells have if anything a SMALLER deficit than non-skull ones (0.031% vs 0.036%, n=59/64)** ⇒ confounder, not cause. ⚠ Nothing here localizes anything to a line of the engine and **no `index.html` change is licensed**; H_PLATEAU's death is *not* a licence to discretize the scorer (the column control already refused that, r 0.7910 vs 0.9337). | PHASE7 §5.16e; RULES §8 (consequence 4 — the joint currency); ROADMAP (retarget); ACCEPTANCE (FLOOR-TAIL demoted, SATURATED flagged, over-floor list re-ordered); TOOLING (Pieces: `floor-plateau.mjs` + `ambient-gap.mjs`, with all three lessons); `tools/floor-plateau.mjs`, `tools/ambient-gap.mjs`. |
| 07-25 | Row 274 handed down two things as settled: that the right currency for the over-floor residual is **`joint = dModel + pct`**, and that on that ranking the remainder is the **15 cells of KT-AoE + SATURATED + RESIDUAL** — with `SATURATED` specifically **flagged for re-derivation** because it came out second-worst (0.268 pp, 3.29× ambient) while row 273 had called it "confirmatory, not a defect signal". The implicit premise underneath both: that these four families *can* be ranked, and that the top of the ranking is the target. | **⛔ THE FAMILY-TARGETING PROGRAMME IS RETIRED — the corpus cannot rank these families at all, so two rounds of re-ranking were the wrong kind of work.** (1) `joint` is **defective**: it adds a term carrying a known instrument budget (`pct`, capped by `ripplePct`) to one carrying **none** (`dModel` is model-vs-model — no sim, so no Δ=1.0 s lattice anywhere in it), across families whose ceilings differ **9×** (FLOOR-TAIL 0.022 pp vs SATURATED 0.189), so a cross-family `joint` comparison largely compares **ceilings**. Corrected: `unexplained = dModel + max(0, pct − ripplePct)` — subtract the budget from the term that *has* one. (2) That gives a **third** ordering, and **not one family holds its rank across the three**: RESIDUAL 1st→1st→3rd · KT-AoE 2nd→3rd→**1st** · SATURATED 3rd→2nd→**last** · FLOOR-TAIL 4th→4th→2nd. **U1 PASSED and retracts row 274's own claim: SATURATED is VINDICATED** — 0.0367 pp = **0.92× ambient**, mean rank **4.03 of 5**, the *least* anomalous family in the corpus. `joint` flattered its defect precisely because SATURATED is *defined* by having its `pct` nearly covered by its own large ceiling; row 273's original reading was right and needed no re-derivation. (3) ★★★ **The decisive measurement was a stability check, not a fourth currency.** U4's fallback ("the ordering is instrument-dependent") rested on an **n=3 anecdote** — three formulas I wrote, three orderings — which says nothing about the data. A **seeded 20 000-resample bootstrap** of the 135 columns settles it: the nominal worst family holds first place in only **60.8 %** of resamples (RESIDUAL 15.5 %, FLOOR-TAIL 14.5 %, SATURATED 9.2 %), i.e. **the ~0.1 pp between-family differences are the same size as the instrument's own per-cell ceiling (corpus median 0.134 pp)** — the ranking is **noise-dominated** and no currency could ever have ordered it. The instrument-dependence is a property of the **DATA**, not of the formulas. ⇒ the remaining unit of work is a **CELL with a fresh sim duel**, or **more columns** for power — and explicitly *not* a fourth currency (pre-registered as U3's binding instruction before any of this was seen). ⚠ **Four errors, three of them mine from row 274.** (1) **Medians are not additive.** "The model is ~0.06 pp more confident than the sim confirms" came from `median(joint) − median(pct)` = 0.0814 − 0.0250 = 0.0564. Measured directly, `median(dModel)` inside the floor is **0.0398 pp** — the qualitative claim survives, the number was inflated ~40 %. ★ **Lesson: measure the term you mean, never subtract two medians.** (2) **A ratio spread is a defective criterion near zero.** U2 pre-registered that the max/min spread across labels should *shrink*; it **rose** (3.37× → 4.34×) even though **every** family's absolute gap fell (top −42 %) — the ratio grew only because the minimum fell further (−55 %), and one term's spread is literally *infinite* (`inside`'s median is exactly 0.0000 by construction). U3's canned diagnosis ("the residual is dominated by `dModel`") was **also** wrong: the `dModel` spread is 4.83× only because its minimum is tiny — its *level* is uniformly small (0.0101–0.0487 pp) while `pct−ceiling` ranges 0.0000–0.1328 pp, and the top family is driven by `pct−ceiling` 4:1. ★ **Lesson: pre-register an ABSOLUTE threshold when the quantities can approach zero.** (3) **A first reading of U6 was wrong and was checked before being written down.** From the primary column alone (FLOOR-TAIL 0.1047 vs RESIDUAL 0.1045) the ordering flip looks like a meaningless tie between two equal families, which would have made U6 over-strict. It is not: under the alternative edge period **RESIDUAL swings +49 %** (0.1045 → 0.1553) while the other three are bit-identical. **U6 stands on its merits** and `verdict=ORDERING-FRAGILE` is correct — n=4 with 14/135 cells INDETERMINATE between periods is exactly why. (4) The defect in `joint` was found by auditing my **own just-pushed commit** rather than moving on — the right habit, but it means row 274 shipped a wrong ranking, a wrong number, and a wrongly-flagged label. ⚠ **No `index.html` change is licensed by any of this**; §5.16c's full-column control still refuses a discretized scorer (r 0.7910 vs 0.9337). The acceptance-criterion restatement remains a **user call**. | PHASE7 §5.16f; RULES §8 (consequence 4 ranking superseded, consequence 5 added); ROADMAP (family programme retired); ACCEPTANCE (FLOOR-TAIL superlative withdrawn, **SATURATED vindicated**, over-floor list un-ordered); TOOLING (Pieces: `unexplained-gap.mjs` + the bootstrap-first / artifact-budget / median / ratio-spread lessons); `tools/unexplained-gap.mjs`. |
| 07-25 | Every `pct` in the cross-val corpus is a **single-seed** point estimate, and the tool built to arbitrate a single cell — `plan-duel.mjs` — bands it with seeds `11,12,13,14,15`. TOOLING had said for weeks that contiguous seeds are not independent replicates, but the *cost* was never measured, so the default stood. | **★★★ THE BAND IS EXACTLY ZERO, AND A ZERO BAND PASSES EVERYTHING.** Measured on a real cell (KT/T=420/@195, iter 6000, `tools/cell-band.mjs`): seeds 12–15 reproduced seed 11 **to the printed decimal on both plans** ⇒ `sd = 0.0000 pp` where far seeds give `0.0058`. Since the tool's test is `|mean| > band`, it declared **every** nonzero delta significant — a false-PASS in the one instrument whose entire job is arbitration. Fixed: defaults spaced by 10⁵ **and a hard guard that dies if `min seed gap < iter`**. Audited: no committed verdict rests on it (every `plan-duel` control in the docs is sim-free). ★ **Two lessons.** (1) When a band comes out at or near zero, **suspect the design before believing the result** — a zero band is a broken replicate, not precision. (2) **Put the guard in the tool, not in the doc**: the correct rule was written down and the wrong default shipped anyway. | TOOLING "Statistical protocol" (★★★ bullet); `tools/plan-duel.mjs` header + guard; PHASE7 §5.17-S3. |
| 07-25 | The 24 over-floor cross-val cells are **one population**, gradeable against one ruler (the tail-lattice ripple floor), and boss cells being **2.8× more likely** to sit over it is a signal about the model. | **★★★ THE CORPUS IS TWO INSTRUMENTS, AND THE BOSS ONE HAD AN UNPRICED ±0.13 pp CHANNEL — so 6 of the 9 boss "deficits" are not deficits.** The floor prices **one** wall (the tail); a boss column has **seven** (KT: 15,42,69,94,105,160,306), six interior and unpriced, and a boss cell is a **5-variant wall-jitter mean** while a class cell is a single un-jittered run. Measured at the top boss cell: seed sd **0.0058 pp** (negligible — the single-seed design is **vindicated** for count-preserving cells) vs wall-jitter variant sd **0.1427 pp** over 33 geometries ⇒ 95 % band **±0.1251**, **12×** the seed band and the size of boss cells' median ceiling (0.1024). Only 3 of 9 survive it. Class cells are untouched and their floor is **complete** — `downtime`/`aoeWins` are populated only inside `if (BOSS)` (`xval.mjs:139-163`), so a class fight genuinely has one wall — which is the whole of the 2.8×. Two further findings: the variant spread is **bimodal, separated by exactly one cast** (3375 damage; PHASE8's FLOOR LAW appearing on a walled fight as a discrete parity mode, so quote the parity-free subset, not the mixture), and `VARIANTS[0]` is **δ=0 by construction**, so the un-jittered geometry carries **20 %** of every boss cell (+0.067 pp at this one; recorded, not acted on — changing it moves every committed boss cell). ⚠ The 0.1427 sd is measured at **one** cell; transferring it is a first-order prior, strongest where the survivors are. ★ **Lesson: before grading a cell against a ruler, check the ruler was built for that cell** — `ripple-audit.mjs`'s own header already called itself a lower bound for boss rows, and it was used as a bound anyway. | RULES §8 consequence 6; ACCEPTANCE (over-floor list repriced 24 → 18); TOOLING (Pieces: `cell-band.mjs` + five harness lessons); PHASE7 §5.17. |
| 07-25 | The last surviving cross-val deficit is a **model** defect of unknown location — burn model, search, or tail lattice — to be hunted by further decomposition. | **★★★ IT IS 100 % AoE-WINDOW VALUATION, AND ONE ABLATION SHOWED IT.** Deleting `_aoe` from both specs (press times, walls, 33 geometries and seed **identical**; `--targets` dropped) collapses the deficit from **+0.2930 ±0.0068 pp to −0.0063 ±0.0048** — statistically zero, i.e. the AoE window accounts for **102 %** of the parity-free gap. Outside the window the model ranks this pair **exactly right**, which excludes burn model, search and tail lattice in one measurement. What is left is **within-window placement**: the model packs the cooldown cluster against the window's END (AP/Gem [130,145], Icon [125,145], Zerk [130,140]), the sim prefers it **~10 s earlier** ([120,135] etc.) — and both candidates are entirely inside [105,145], so **neither** RULES §9 Correction 1 (SP dilution at a boundary) **nor** Correction 2 (exit-re-ramp straddle) applies. Corollary: the parity mode is AoE-**independent** (the same 7 geometries flip with and without the window, worth 3375 vs 3428 damage ⇒ an ordinary AB, not an AE cast), so the two channels are cleanly separable. Filed as the **Correction-3 candidate** — one cell family, cause not yet named, **no engine change made**. ★ **Lesson: an ablation that holds everything else byte-identical localizes in one run what five rounds of decomposition could not.** | RULES §9 ("⚠ OPEN — Correction 3 CANDIDATE"); ACCEPTANCE (the 3 survivors are one mechanism); PHASE7 §5.17-S5. |
| 07-25 | Row 299 named the survivor as **within-AoE-window placement** — "the model packs the cluster against the window's END, the sim wants it ~10 s earlier" — an unnamed valuation term, expected to need a fresh sim campaign (five work items W0–W5 were pre-registered, three of them sim sweeps) to characterise. | **★★★ IT IS ONE ARCANE EXPLOSION CAST, AND THE "10 s EARLIER" READING IS RETIRED — the model's press-time curve is FLAT across the window interior and spikes only AT THE WALL.** The mechanism, closed on **both** sides: a press inside an AoE phase **fires ~0.58 s late**, the AE lattice is **hard-anchored to the phase START** (first AE at exactly `phaseStart`, then +Δ), and the APL stops casting AE at the phase end — so a window placed **flush** (`press + dur == phaseEnd`) has its slipped tail **clamped** and loses its last lattice point, where an interior window's slip is **self-cancelling** (what the start loses the end regains). **One-sided ⇒ a ranking error, not a level error.** Sim side: an assumption-free **aura-state ledger** over the two combat logs — label every AE cast with the set of buffs up at its cast moment, pool damage per state across both plans so crit cancels — shows both plans cast **37 AE** with exactly **one** moving from `Icon+IV` (7785) to `AP+Gem+Icon+IV` (10780) = **+2995 damage** vs measured **2919 ± 35** ⇒ **102.6 % closure**. Model side: the scorer is flat **to the decimal** for P ∈ {125..129} (correct translation invariance) then spikes **+368 damage at exactly `P = phaseEnd − dur`** — proven an artifact by moving the phase end (**bit-equal** P129/P130 at E=146/147/150; **−1540** at E=143/144) and by non-additivity (AP alone −29, Gem alone −68, Zerk alone 0, all three **+368**). **368 + 2995 = 3363 damage = 0.347 pp** = exactly the observed sign flip (model prefers native by 0.0536, sim prefers borrowed by 0.2930). A **pre-registered predictive sweep** then confirmed it with **all three falsifiers failing to fire**: sim pct vs the model's champion is flat at +0.2926/+0.2943/+0.3019/+0.2960 for P ∈ {126..129}, the 129→130 step is **370 SEM** and **98.5 % of exactly one cast**, and all five wall-jitter variants put the cliff at the **same P** (sd 0.0018) ⇒ the snap is **δ-invariant**, as a phase-start-anchored lattice requires. Root-caused to two guards in `index.html`: **`:855` `prevCastRamp = !isAoe && …`** switches off the deterministic snap at `:773` — whose **own comment** justifies snapping where "boundaries are sparse and DETERMINISTIC, locked to the ramp start, no phase freedom", which is *exactly* an AoE lattice — and **`:820` `cast: 0`** makes `:853` yield `prevCastEnd === t`, so `:785`'s expected-slip fallback is unreachable inside AoE. ★★★ **The method is the transferable finding: the whole decomposition cost ZERO extra sim runs.** W0 was only a *legality gate* (do the presses fire? — they do, within 0.62 s, including borrowed's two-presses-one-cooldown-apart trap); the `SIMLOG=1` logs captured for it already contained the answer, while three sim campaigns had been pre-registered to attack the same question. ★ **Lesson: capture the combat log for BOTH arms of every duel as a matter of course — the cheap legality check is the excuse to obtain expensive-looking data.** ⚠ **One prediction of mine MISSED**: I predicted P=120 would sit ≈0.03 pp under the plateau; parity-free it sits **0.08 pp** under, ~2.5× off — and for a *different, correct* reason (that sweep holds Icon at 125, so ~4 casts are bought without it: ordinary Correction 1). Moving Icon **with** the cluster returns it to the plateau (+0.2930), which is why the plateau is properly stated as *"cluster coherent AND wholly inside the window"*. ⚠ **No `index.html` change made**: both edits move every AoE-phase plan, so the fix owes plan-sweep + exact-match 25/25 + a **DUEL of every changed cell against its previous layout**. | RULES §9 (Correction 3 promoted **CANDIDATE → CONFIRMED**, with the retired framing called out); ACCEPTANCE (Phase 7's diagnostic mandate **discharged**; what remains is an engine fix, not a diagnosis); ROADMAP; TOOLING (the SpellID/ItemID map — the log prints **numbers, never names** — and ★★ "decompose a duel by walking the log"); PHASE7 §5.18. |
| 07-25 | Row 300 root-caused Correction 3 to **two guards** (`:855` `!isAoe`, `:820` `cast: 0`) and left the model's +368 cusp described only as "an artifact at `P = phaseEnd − dur`". | **★★★ THE CUSP IS ONE RAMP CAST, THE PLATEAU IS EXACT TRANSLATION INVARIANCE, AND THE PRESCRIBED FIX WAS THE WRONG FIX.** An instrumented engine clone split `robust` into its two additive parts: integral delta **0.00**, rampCast delta **+368.39**, *all of it* in ramp cast index 12 (`tc = 147.374`, dmg `2822.5 → 3190.8`) — a cast that STARTS at 145.15, so with `RAMP_JITTER = GCD_FLOOR = 1.0` its state window is **[144.65, 145.65]**, and P=130's windows end at exactly **145.0 (inside)** where P=129's end at 144.0 (outside). The pre-registered jitter-window hypothesis: **CONFIRMED**. The same instrument proved the flat interior is not luck — integral buckets move as a **pure translation** (84576.58 across 129→130, 38443.90 across 139→140, 5551.03 across 144→125) and the deltas sum to **exactly 0.00**. ⚠ **Row 300's specified fix was rejected on inspection**: flipping `:855`/`:820` drags the ramp bookkeeping and the AE step function with it. The landed fix adds an explicit AoE case to the event-firing branch plus a per-segment **anchoring test** — an AoE lattice is EXACT iff the phase's first cast boundary *is* the phase start (it follows an intermission, or the pull), in which case the press **snaps** (`eff = t`); after a **burn** phase the lattice inherits the AB stream's arbitrary phase and it falls back to `slip = prevInterval/2`. **So the fix is scoped by the same determinism criterion §3b.1 already uses, not by segment type** — which is what makes it a rule rather than a KT tune. Design risk was retired **before** the edit by emulating the snap with fractional press times (feeding a lattice point in makes the engine set `eff = e.ts, slip = 0` — exactly what the snap sets), which previewed both the sign flip and an unintended `+474` interaction while the engine was still untouched. **Result: every measured cell moved toward the sim** (P130 `+0.0460 ✗ → −0.0221 ✓`, P131 `−0.1254 → −0.3112`, P132 `−0.3765 → −0.4905`, P124 `−0.0156 → −0.0000`), and the DUEL flipped **−0.0536 pp (prefers native, wrong) → +0.0081 pp (prefers borrowed)**, matching the sim's **+0.2930**. Blast radius bounded analytically (**exactly one** corpus preset has an `aoe` phase) then confirmed (`plan-diff compared=16 changed=0`). ★ **Lesson: when a doc prescribes an engine edit, re-derive the edit — the diagnosis can be right while the prescription is wrong.** | RULES §9 Correction 3 (**★ FIXED IN THE MODEL**, with the anchoring test and the result table); ARCHITECTURE; PHASE7 §5.19. |
| 07-25 | *(re-derivation, logged for the link it makes — **not** a new finding; row 214 already had it.)* Chasing the fix's residual undershoot re-read the KT log and re-established that **wowsims reads value-buff state at cast COMPLETION, unconditionally** — in both directions: a cast `145.14 → 147.36` whose AP (`S12042`) and gem (`S37445`) faded at 145.58/145.59 reported the *unbuffed* multiplier ratio **1.2361**, identical to a cast with neither; while a cast starting 260.18 that gained the gem at **260.19** (0.01 s mid-flight) reported `SP: 1836.2` = 1456.2+225+155 at completion — **it got the mid-flight buff**. | The value is the **link**: the **+474 residual at P=130 IS the known PHASE8 back-edge over-credit term** (`frac(D/Δ) × premium`), the one that is deliberately unimplemented because charging it moves the B2 deficit the wrong way (§8). **P7's last residual and P8's open charter are the same term** — which is also why the post-fix magnitudes undershoot the sim (P130 7.5 %, P131 52 %, P132 64 %) while every *sign* is now right. Two pieces of stale living text still asserting the falsified *mechanism* were corrected in the same commit (`index.html`'s `rampCastDmg` preamble; `ARCHITECTURE.md`'s ramp bullet); RULES §3b.3 already carried it. ★ **Lesson: grep the docs before writing up a "discovery" — a re-derivation is worth logging, but only as one.** | RULES §3b.3; PHASE8 §5b; PHASE7 §5.19. |
| 07-25 | The AoE press-snap fix was measured, its blast radius bounded, and the landing was expected to be mechanical: update the KT golden and commit. | **★★ THE LANDING GATE CAUGHT TWO METHOD ERRORS, AND ONE OF THEM LOOKED LIKE THE END OF THE WORLD.** (a) **A phantom determinism violation.** Bare node emitted `icyVeins:[93,113,381]` where the browser's rendered plan showed `[105,125,381]` — which, if real, invalidates the exact-match premise wholesale. It was **not** real: scoring both candidates under one engine gave the *same* `robust = 617296.6744`. I had compared `optimizeAsync().s` (**press intents**) against a rendered plan (**fire times**) — the exact distinction CLAUDE.md states and `plan-sweep.mjs`'s own header warns about. ★ **Lesson: the difference between the two conventions LOOKS like non-determinism, so adjudicate it with a 30-line scoring script instead of reasoning about it** (cost of inferring: ~40 min; cost of measuring: 2 min). (b) **A near-miss on haste portability.** All the fix's evidence is at **haste 195**; `GOLDEN_DEFAULTS` runs KT at **haste 0**, and since the AE lattice pitch Δ = `max(1.0, 1.5/m)` is haste-dependent (≈1.11 s at h195-with-IV vs 1.25 s at h0-with-IV), *whether a window's tail clips the wall does not transfer between hastes*. Re-duelled at the golden's own config (5 base seeds × 9 wall-jitter variants, 90 sims): model **+0.058 pp**, sim **−0.0067 ± 0.0047 pp**, 0/5 seeds positive — resolvable but **−0.14 DPS ≈ 1/75 of a cast**, ~19× inside the ±0.1251 pp boss band. **Verdict LAND**: +0.2930 pp where the fix was derived, a sub-noise trade at the golden, and the ~0.065 pp over-claim is the known PHASE8 back-edge term. exact-match **24/25, Kael'thas only** — the exact predicted radius. ★ **Lesson: a fix gated at one gear config is not gated for a golden at another; re-duel at the golden's own `GOLDEN_DEFAULTS`.** | RULES §9 Correction 3 ("★ Gated at the golden's own config"); TOOLING (duel halves 3 and 4); ACCEPTANCE; PHASE7 §5.20. |
| 07-25 | The cross-val corpus ("the big test") was treated as a faithful executor of the tool's plans — the open question was always whether the **model** was right, never whether the harness was *running the right plan*. | **★★★ IT IS NOT: `xval.mjs` FEEDS THE SIM PRESS INTENTS, NOT FIRE TIMES.** `xval.mjs:194` does `res[H] = { spec: toSpec(bestH), … }` on `optimizeAsync().s`, and `toSpec` (`:167`) rounds those raw **intents** straight into genapl; it never calls `simulate(s, cfg, true).actEff`. **So the corpus sims schedules the tool would never print.** The two diverge whenever a press lands inside or just before an intermission: the model defers it to the phase resume (`simulate()` walks `t` to `seg.end`) while the sim fires it at the next real cast boundary — into untargetable downtime. Priced on one KT plan at haste 0, the **identical** plan reads **−1.5432 %** under the intent transcription and **−0.0104 %** under fire times, because a `93` IV intent (fire time `105`) burns ~12 s of Icy Veins inside the 94–105 intermission. **That artifact is 5–10× the 0.2–0.4 pp deficits the entire acceptance campaign has been chasing**, so some fraction of the recorded boss-side deficits may be harness fiction rather than model error. ⚠ **The convention was NOT changed** — every gathered round is comparable *because* it was constant, so a switch owes a written justification, an ACCEPTANCE announcement and a re-gather. Opened as **P7.15**. ★ **This is the standing user invitation cashing out** — *"It's possible the 'big test' also has a logical flaw to it, I don't think so but maybe"* — and it does, in its **transcription layer**, not its statistics. ★ **Lesson: audit what the instrument EXECUTES, not just what it reports; a harness that silently re-times its input is indistinguishable from a model error in every summary column.** | ACCEPTANCE (⚠⚠ harness-fidelity caveat on every boss table); TOOLING (duel half 3); PHASE7 §5.21; task P7.15. |
| 07-26 | §5.16's fix was believed complete modulo one documented −0.001 hair; the tail winner-take-all was FILED as a follow-up with two candidate remedies, expected to wait. | **THE ROUND-7 IDENTITY FILTER MADE IT DUE THE SAME NIGHT — and the third instance inverted the lesson's direction.** At `isc-mqg-xl @h20`, round 6 rode the group line on a **1e-10 float-dust** snap lead, §5.16's rule correctly preferred the base hop (+5.85), and the group line's TAIL would have finished +14 higher — every hop-exit selection rule provably loses some tails. The filed remedy landed as **`finishLine(entrant)`** (PHASE9 §5.17): the ~1,400-line tail became a callable run once per hop arm, finals decide, ties keep the old-rule carry, the recursive no-CS solve memoized across arms. The §5.16 h20 hair (−0.001) **heals**; goldens are **byte-identical (25/25, no re-record)**; CPU +10.4 % (cumulative ≈ +18 % vs pre-§5.12 — the phase's CPU win is fully spent on correctness; the reclaim catalogue is now the live §4 work). One residual remains, and it is structurally NEW: the same cell still reads −0.005 vs round 6 because the Cold-Snap comparison's chain-candidate family derives from the **recursive no-CS sub-solve**, which differs between ENGINES — not a selection defect; the coin flip relocated into a sub-solve's candidate generator. Chase stopped there by decision; the cell is the named `--observe` trade for round 7. ★ **Lesson: monotonicity under entrant addition had to be engineered at FIVE narrows (seed fill, snap cut, hop entry, tail entry, and — unclosed by choice — the sub-solve candidate generator); each fix relocates the lottery one stage deeper at ~5× smaller stakes, so decide the stopping depth by stakes, not by zeal.** | PHASE9 §5.17; ARCHITECTURE (two-arm tail); the round-7 `--observe` note. |
| 07-26 | `groupSeeds` (§5.14) was landed as PURE ADDITION — "a missing seed class, not a tuning knob" — and the −8.5 % CPU landing was gated plan-identical on the goldens, so the round-6 `worse=7` cells needed attribution to groom-exit vs groupSeeds vs interaction. | **★★★ ALL SEVEN ARE GROUPSEEDS ALONE — an additive seed class is only additive if every downstream selection point is additive too, and this pipeline had THREE that were not.** Single-variable isolation (bare-node reproduction of xval's champion search + pooling, validated bit-for-bit against the committed round-6 table first): groom-only ≡ round 5, seeds-only ≡ round 6, at every tested cell — the groom exit has zero casualties. The three exclusive narrows: (1) the seed fill counted group seeds toward `starts`, silently EVICTING one random start per chain seed (h265: every random gone, one of them held the winner); (2) the snap stage's `sort().slice(0,6)` with `best=null` let a high-raw group entrant crowd the true winner out of the only six candidates the final is rebuilt from; (3) winner-take-all into `basinHop` — h160: a **+34** snap lead in a DEAD-END basin (hop +0) displaced a basin that climbs **+3594**. Fix: fill-first + base-top-6-preserved-with-group-extras + hop-both-with-strict-improvement-override (ties carry the old selection — measured: two hops meet at EXACTLY 1059624.234589852 on two different layouts, and a tie-to-base first cut lost a legitimate round-6 gain at `scb-skull-xl @h260`). Validated on all 60 cells of the 6 affected tables (≥ r6 everywhere, ≥ r5 at 59/60); golden gate `changed=1` — **Kael'thas, Δscore +17.79, strictly BETTER** (terminal cluster 381→396), re-recorded on the arbiter rule. CPU: **+16.5 %** vs the landed engine — the −8.5 % win given back and more; reclaim stays open in PHASE9 §4. Residual: the finishing TAIL is a fourth winner-take-all (h20 −0.001 / h260 +0.005, one each way), filed with two remedies (callable `finishLine` double-run; cluster-shift move class) and the h20+h260 pair as its two-sided test. ★ **Lesson: "adding entrants never removes entrants" is a property you must ENGINEER at every narrows of a pipeline, not a property of the entrant list — and a gate that certifies plan-identity on one corpus certifies nothing where plans move.** | PHASE9 §5.16; ARCHITECTURE (seeds order + two-arm hop); ROADMAP §0; the h20/h260 follow-up pair. |
| 07-25 | Round 6 (the full 36-table `emit=fire` re-gather) was expected to grade cleanly against its two assertions — `emit=fire` + `artifact=0` on all 36 — and possibly dissolve boss deficits that the intent convention's inflated plans had manufactured (§5.22's phantom-deficit mechanism). | **★★ THE ROUND GRADED THE MODEL *AND* TWO OF ITS OWN INSTRUMENTS.** (1) **`artifact=1` on KT mqg+skull was the GUARD lying, not the harness**: `h295 MQG@49` inside `[42,55)` is a FAITHFUL in-place press — the engine's documented convention ("gap presses fire at the press moment, slip 0") is game-true (a trinket is usable while the boss is untargetable, the wall seconds are zero-valued by model and sim alike, and the early press cycles MQG's 300 s cooldown), verified by direct probe (`actEff` = 49.000 for intent 49). The guard's premise "fire mode must defer every press past the wall" only ever described *cast-gated* and *AoE-anchored* events; it now stamps `wallPress=` info under fire and reserves `artifact=` for intent. (2) **The EFF-AUDIT pooled a scoped repricing into `worse=`**: P7.14 changed every KT spec, so the corpus-global scorer-identity proof (259 pinned cells) never covered KT, and KT's intended repricing deltas (the removed AoE over-credit, up to −0.13 %) read as "search regressions". The audit now buckets moved cells of pin-less tables as `ungraded=` (round 6: `worse=7 better=5 tie=44 ungraded=30`; the 7 are the attributed groupSeeds cells). **The model findings survive the instrument corrections**: monoDip 0.00 % on all 36 · the persistence work list is **the same 2 columns as round 5** (`isc-mqg h40` 5/5, `isc-skull h20` 4/5 — harness-fix-robust) · KT isc+scb's 3 over-floor survivors **DISSOLVE** under P7.14+fire (0.38 % → 0.09 %, inside the ±0.125 pp band — the fix discharged the cells it was derived on) · KT mqg+skull re-shapes 0.15 % → **0.31 %** with a new worst at `@sim0` (native h0 presses `AP@109`, 4 s into the AoE window, vs the rival's flush `AP@105` — the round-7 look, after the groupSeeds landing re-baselines KT). ★ **Lesson: a guard is a model of the thing it guards, and it goes stale the same way docs do — when a guard fires, adjudicate guard-vs-engine by direct probe before trusting either.** | PHASE7 §5.23; `tools/xval.mjs` (wall-press note); `tools/xval-round-diff.mjs` (`ungraded=`); PHASE9 §5.15/§5.16. |

| 07-26 | **The shipped "Check in the benchmark sim" button verifies the plan you are looking at.** Every piece of supporting evidence was real: the page and the terminal build byte-identical requests (`tests/sim-request.mjs`), the shipped wasm equals the native runner to the decimal (`tests/sim-duel.mjs`), and the whole chain is shared rather than reimplemented. | **IT CANNOT SEE BLOODLUST OR ANY TRINKET (PHASE10 §8.7).** `sim/model-ref.json` carries **zero equipment items** and `raid.buffs.bloodlust: false`. An on-use trinket is only castable while its item is **worn**, and wowsims does not complain when it is not — the press is a **bit-identical no-op**; `raidBuffs.bloodlust` likewise does not auto-apply a Lust (BENCH §3b), it makes one *castable*. Measured one-press-vs-never-press: IV **+49.269**, AP **+39.715**, Zerk **+16.100** fire; **Bloodlust, Icon, Skull and MQG are each exactly +0.000**; and the Serpent-Coil press is **−2.982**, *worse* than a no-op, because it spends a GCD casting a Mana Emerald and collects none of the +225 SP that motivates it. Decisive test: two plans differing **only** in where one press goes read **exactly 0.000 DPS** apart — `Icon@6` vs `Icon@80` (74 s), `Lust@5` vs `Lust@60` (55 s) — where the fixed character reads **+0.756 %** and **+1.290 %**. ★ **The two most consequential decisions the planner makes were the two it could not measure**, and the failure renders as *"too close to call"* — a confident null on exactly the question asked. ★★ **Why every gate missed it: they all compared the page to the TERMINAL, and both ran the same crippled character.** Agreement between two paths is not evidence about either one. The gap only opened when a *third* thing was asked — does the press do anything at all? — which is the same lesson as the 07-26 "three independent proofs shared one derived premise" entry, in the opposite direction. ⚠ And a structural limit the fix must *report* rather than paper over: wowsims has **two** trinket slots and the planner offers **four** on-use trinkets, so a kit naming three can never be fully equipped — going gear-less made every kit equally unverifiable instead of one kit partly so. Guard landed immediately (`tools/bench.mjs` refuses any arm pressing something dead on its character, and `--kit a,b` equips a pair); the page-side fix needs `index.html`. | PHASE10 §8.7; `sim/planspec.mjs` `REQUIRES_EQUIPPED`; `sim/README.md` Known gaps; `tools/bench.mjs`. |

| 07-26 | **A cross-val table that parses is a cross-val table.** Every guard in the campaign checked structure — a header, ten `plan@h` lines, a matrix, a trailing `XVAL-DONE` — and that had always been enough. | **A CORRUPTED TABLE PASSED ALL OF IT (PHASE10 §8.10).** Testing `SKIP_EXISTING` I pointed `XVDIR` at the **live** results directory; `run_cell` opens with `>`, which **truncates**, so a file the running campaign still had open at a high offset was cut out from under it and the kernel filled the gap with NUL bytes. The result kept its header, all ten plan lines, a full 10-row matrix and a valid `XVAL-DONE` — every structural check green. Only a byte-level look found **3019 NULs** and two interleaved stderr streams (timestamps running `16:45:48 → 16:45:33`, which is two writers). Blast radius **measured, not assumed**: 3019 NULs in that one table, **0 in every other**; the cell was re-run and diffed line by line, so the *numbers* were never wrong, only the file was dirty. ★ **The lesson, which is this project's oldest one in a new costume: a file can be well-formed and still not be a record of anything.** Fixed at both ends — a writer lock in `run_cell` (verified: a held lock refuses the cell **and creates no `.txt`**, so nothing is truncated) and, the load-bearing half, `xval-verify.mjs` + `xval-collect.mjs` now **reject any table containing a NUL**, because the lock removes the *cause* while the *effect* passes every other check. ⚠ Sub-lesson, and a small one worth keeping: my first corruption scan was itself broken — bash `$'\0'` is an **empty string**, so `grep -c $'\0'` counted every line and made five healthy tables look corrupt. The instrument checking for the false pass produced one. | PHASE10 §8.10; `tools/xval-bench-campaign.sh` (the lock); `tools/xval-verify.mjs`, `tools/xval-collect.mjs`. |

| 07-27 | **BENCH §3e: "the gear A→B re-export moved B2's sim preference ~0.39 pp AND CHANGED ITS SIGN."** Measured 07-26 (h40 2499.5 vs h70 2500.3 ⇒ ≈ −0.028 %, three spaced seeds), stated as the *first* gear-B observation, and it propagated fast: it became the justification sentence in BENCH §1, the gear-A archive README, ACCEPTANCE's banner, GEAR-AGNOSTIC §0, CLAUDE.md's Phase-8 pointer, and **PHASE10 §1.3 bullet 2** — "any Phase-8 work started today would aim at a number that is not there." | **⛔ IT DOES NOT REPRODUCE. THE QUARRY HAD NOT MOVED.** §3e itself listed three confounds (gear · bench conditions · which cell "B2" names) and separated **none** — separating them was carried as its own next-step (a)/(b) and never done. Doing it, one knob at a time on the same runner: §3e's **published conditions verbatim** (`var 0`, `mana 500k`, `iter 20k`, its own seeds 1/500k/1M) give **+0.666 %**; the **corpus protocol** (`var 0.5`, ∞ mana) gives **+0.389 %** against gear A's **+0.360 %**; and `--mana` is **inert** — the two `var 0` rows are identical to the digit, so of the two protocol suspects only `--var` matters (0.28 pp), which is TOOLING ★★ restated. Cross-checked on the *other* engine: `plan-walk` on the committed wasm reads **+0.375 % ± 0.81 DPS**. The model half is **−0.037 %**, matching PHASE8 §20's −0.04 %. ⇒ **B2's ranking error on gear B ≈ 0.43 pp; the ≈0.445 pp target STANDS.** The only knob that flips the sign is **whether MQG is worn** — §3e's conditions on the export *as shipped* (isc+scb, no MQG) give **−0.236 %**, because both `MQG` presses become bit-identical no-ops (the same-day PHASE10 §8.7 failure mode) — but the absolute DPS matches neither reconstruction, so that is a hypothesis, not a diagnosis, and §3e's provenance is unrecovered. ★ **The lesson is about BLAST RADIUS, not about B2: a single off-protocol measurement, published as an "observation" with its confounds honestly listed but unseparated, re-planned a phase.** The honest label was there — nobody read past the headline. ⚠ And the *policy* it was cited for is untouched: absolute DPS, the trust anchor and every calibrated constant remain baseline-dependent, so BENCH §1 stands, and one cell landing in the same place licenses nothing about the other 35 tables. | BENCH §3e retraction banner; PHASE10 §1.3 bullet 2; GEAR-AGNOSTIC §0; `gearA-pre-20260726/README.md`. |
| 07-27 | **The freeze that protects a cross-val round is `index.html`, plus `sim/simreq.mjs` and `tools/xval-bench.mjs`.** Written that way in four places (CLAUDE.md, GEAR-AGNOSTIC §6.2, ROADMAP's resume block, PHASE12 §1.1e), each with the correct reason: the campaign spawns a fresh `xval-bench.mjs` per cell, and the plan cache keys on `index.html`'s bytes. | **THE FREEZE IS THE WHOLE IMPORT CLOSURE, AND THE REASON IS THE SAME SENTENCE READ ONE STEP FURTHER.** `ENGINE_ID = sha1(index.html)` (`xval-bench.mjs:179`) is the *entire* engine half of the cache key — so every OTHER file the driver imports can be edited mid-round and the cache keeps serving plans computed by the old code **under an unchanged key**. The damage is silent and partial: only cells gathered after the edit are affected, and nothing in the table says which. The frozen set is `index.html` · `xval-bench.mjs` · **`tools/engine-node.mjs`** (it *builds* the engine) · `genapl-core.mjs` · `reference-gear.mjs` · `planspec.mjs` · `simreq.mjs` · `benchmark.mjs`. **Found by nearly doing it**: PHASE11 §1.1 B6's proper fix is the single exported `cfgFor()`, and adding the missing `t5two` there is inert for every existing preset — which is exactly the shape of edit a freeze written as a file list does not stop. ★ **Lesson: `"provably behaviour-neutral"` is not the test; `"in the closure"` is** — and a freeze stated as a list of files is a freeze stated at the wrong granularity. ⚠ The durable fix is not a longer list: fold the closure's hashes into `ENGINE_ID`, so a mid-round edit **misses** the cache and re-solves rather than silently mixing instruments. A freeze a tool enforces beats one a doc requests. | PHASE12 §1.1e; CLAUDE.md · GEAR-AGNOSTIC §6.2 · ROADMAP · PHASE10-RESUME §2. |
| 07-27 | **PHASE11 §1.1 is "8 confirmed bugs, fix first"** — the 07-26 audit's findings ledger, verified against HEAD line by line when it was written, and quoted with that count by CLAUDE.md and ROADMAP. | **SIX OF THE EIGHT WERE ALREADY FIXED, AND THE LEDGER SAID NOTHING.** Re-checked against HEAD: `netlify.toml` no longer serves the unhashed wasm `immutable`; `plan-duel.mjs` transcribes `actEff` fire times and takes its flag *shape* from `runnerFlags()`; `census-build.mjs` is content-anchored; `sim-duel.mjs` uses `pathToFileURL` on both sides; `sim-request.mjs` hoisted §0 above the `RUNNER` gate; `bench.mjs` has `--targets` **and refuses** an `_aoe` spec that omits it. Each fix had even left an in-file comment citing its own B-number — the fixes documented themselves and the ledger did not. B6 was the only one genuinely open and is fixed here. **Both survivors (B1's page half, B2's listener leak) are in `index.html`** — not a coincidence: the one file the split exists to break up is also the one frozen while a round gathers, so its bugs are the ones that cannot be fixed in passing. ★ **Lesson, and it is this cleanup's whole theme: a findings ledger rots the same way a living doc does, and faster, because every entry is a thing someone is actively trying to remove.** "Fix these first" read as a to-do list for weeks while three quarters of it was already done — which makes the doc worse than absent, since it prices the phase's remaining cost at four times reality. Audit a ledger's *status* on the same schedule you audit its *contents*. | PHASE11 §1.1 status table; CLAUDE.md + ROADMAP pointers. |
| 07-27 | **The aura-state walk "needs NO assumption about coefficients, amp curves or stacking order — it is pure observed damage", and pooling across both arms cancels crit variance "because the states are identical, only the counts differ."** TOOLING has called it the cheapest instrument in the project since 07-25, on the strength of P7.14, where it closed a 0.29 pp AoE deficit to **102.6 %** after five sim campaigns had been pre-registered against the same question. | **IT CARRIES ONE UNSTATED ASSUMPTION, AND IT IS FALSE IN GENERAL: that the casts which MIGRATE between states are interchangeable with that state's other casts.** An Arcane Blast's damage depends on its stack and its position in the ramp, so "the average no-buff cast" is polluted by cheap opener casts that a migrating mid-fight cast is nothing like. Two controls on the rebuilt tool: dropping Icon from a plan entirely (12 casts migrate) gives the right *shape* at **187 % closure — 1.9× too big**; and `Icon@6` vs `Icon@80`, where the counts are identical in every state and only *which* casts sit in the window changes, is **completely invisible — every Δ exactly 0**. P7.14 was the one shape where the assumption holds (37 near-identical Arcane Explosions inside a single AoE window), which is exactly why it worked there and why nobody noticed. **The fix is a second, exact ledger, not a caveat:** when both arms cast the same *number* of times — the common case under CRN with one seed and no haste change — cast `i` is the same cast in both fights, so `Σ_i (dmg_B[i] − dmg_A[i])` grouped by that cast's state **transition** needs no pooling at all and closes to **100.0 %** on both controls, including the one the pooled form cannot see. ★ **Lesson: "assumption-free" was a claim about the ARITHMETIC, and the arithmetic really is assumption-free — the assumption was hiding in the SAMPLING.** A method validated on one duel shape had its scope silently generalised by the sentence that praised it. ⚠ Bonus, and it is the honest limit on the whole instrument: on the B2 pair the paired ledger's *same-state* rows — casts that changed value without changing which buffs were up — carry **+2378** against a **−1675** measured Δ. A near-cancellation needs a seed campaign; a log walk cannot decide it at one iteration. | TOOLING "DECOMPOSE A DUEL BY WALKING THE LOG"; `tools/duel-walk.mjs` (both ledgers + the controls). |
| 07-27 | While *doing* the measurement above, my first two runs printed a clean, plausible, fully self-consistent table (`§3e verbatim +0.887 %`, `corpus protocol +0.186 %`) and I nearly wrote it up. | **EVERY NUMBER IN IT WAS THE STANDARD DEVIATION COLUMN.** The runner's TSV line **begins with an empty tag field**, i.e. a leading TAB; `out.trim().split(/\s+/)` drops it, shifting every column left, so `[4]` reads `stdev` (~72) instead of `meanDPS` (~2691). The ratios stayed internally consistent across arms and seeds, so nothing in the output looked wrong — it was caught only by printing an absolute DPS and noticing 72 is not a mage's DPS. **This is `tools/explore.mjs`'s and `xval.mjs`'s recorded worst defect — "unvalidated `parseFloat(field 5)`" (ACCEPTANCE "The instruments") — committed fresh, in a throwaway script, by someone who had read that warning an hour earlier.** ★ **Lesson: a plausibility guard on a parsed number costs one line and is the only thing standing between you and a beautifully typeset wrong table** — `if (!Number.isFinite(d) \|\| d < 200) throw`. The permanent instruments already have it; scratch scripts are exactly where it gets skipped, and scratch scripts are what corrections get written from. | TOOLING "Output TSV … column 5 is mean DPS"; the guard now in the scratch drivers. |
| 07-26 | **PHASE10 §1.3: the gear-A ceremony — clone wowsims, install protoc, `go build`, hunt the export — "is gone."** Written as the third of three reasons to do the phase now, and §8.2 initially recorded `tests/sim-request.mjs` as unrunnable here because `protoc` was missing. | **TRUE AS CONVENIENCE, FALSE AS AVAILABILITY (PHASE10 §8.8).** `apt-get install protobuf-compiler` supplies `protoc`, the `wowsims/tbc-new` clone and `proxy.golang.org` are both reachable, and BENCH §3d's recipe runs end to end in about **four minutes**. Every gate then ran: shipped wasm **==** native runner; the anti-drift gate **9/9**; and the gear-B trust anchor came back **runner `1146.9 / 86.1 / 1586.3` ≡ wowsimcli `1146.9094876137967 / …`**, identical to every printed digit — *and equal to BENCH §3d's recorded numbers*, from a rig rebuilt in a container that never saw the original. ★ That is a stronger statement than §3d could make about itself, and it closes out the 07-26 "the rig's source is not publicly recoverable" episode from the other side: not only is it recoverable, it is **cheap**. ⚠ The correction that matters: read §1.3 as *"you no longer need the rig to get a number"*, **never** as *"the rig is unavailable"* — the native runner is still the only thing that can run the anti-drift gate, re-certify the anchor, or regenerate a `--dumpreq` template, and the second reading is what would let a template drift uncaught. | PHASE10 §8.8; BENCH §3d/§4b. |

---

| 07-27 | **"The shipped wasm == the native runner", read as bit-identity.** PHASE10 §8.26 gated the boss-half backend switch by re-running a completed class table through the native runner and reporting it reproduced the committed matrix **"BIT-IDENTICALLY"**; the handoff doc turned that into *"the re-run changes the stamp, not the numbers"*, which is why re-gathering the other 30 tables looked like pure ceremony. | **RE-GATHERING ALL 30 MEASURED IT: 24 identical, 6 CHANGED.** `tests/sim-duel.mjs` asserts `delta < 0.05` DPS (`:87`) and prints both sides at one decimal — it never claimed more. Meanwhile `xval-bench.mjs` prints the matrix at `toFixed(1)` (`:418`) but computes `diagWorst` from the full-precision doubles (`:426-431`), so an identical *printed* matrix constrains the underlying values only to ±0.05 DPS. Five tables re-rounded a derived percentage by one step; the sixth **flipped a published verdict** — `scb-mqg-medlong` DEFICIT 0.00% → **CLEAN**, both plans printing `2914.8`, off a ~1e-6 relative difference. A Monte-Carlo sim needs only one flipped comparison in one iteration of 6000 to do that. ★ **THE RULE: never derive a headline at finer precision than the artefact you validated it against.** The gate compared at 1 dp and the conclusion was quoted at 2. Nothing was broken — the *reading* was finer than the instrument. Corollary, and it cuts at ACCEPTANCE's own metric: **a CLEAN/DEFICIT banner that a 1e-6 engine difference can flip is not a thing to steer work by** — which is the B-banner analysis arriving from a new direction. | PHASE10 §8.27; ACCEPTANCE "Current status (GEAR B, round 1)". |

| 07-27 | **`ripple-audit`'s self-checks were passing.** PHASE10 §8.22 recorded the class stratum's ripple row as `priced=112 inside=86 over=16 indet=10 · rho=0.289 · self-check mono=0 · vacuous=0` and read it as clean — and on that basis the ripple decomposition kept supplying half the band's pre-registered scope. | **`mono=0` MEANS THE CHECK FAILED.** The footer is `mono=${mono ? 1 : 0}`, so `0` is failure — while the `vacuous=0` sitting beside it on the same line is the *good* outcome. **Two flags with opposite polarity on one line**, and the prose immediately above it had been saying so in words all along: *"**NOT MONOTONE — this tool is suspect, stop here**"*. On the full round it fails **two** of its five pre-registered predictions: P5 (median floor must fall with fight length — inverts at `medlong 0.103 < long 0.106`, marginally, but the check declares no tolerance) and P3 (the KT family must not be explained away — **3/13** over floor against a bar of 7). ⇒ **no ripple decomposition is quotable on this round** — no "N % is below the instrument's resolution", no family ranking. ★ Same shape as the entry above and found the same way: **by reading the tool's own output instead of its summary line.** The instrument was honest in prose and misleading in its stamp, and the stamp is what got quoted. | PHASE10 §8.30; ACCEPTANCE's gear-B block. |

| 07-27 | **The KT / AoE cells were a live debt.** ACCEPTANCE carried three surviving over-floor boss cells (`isc+scb` KT @95/@195/@245, 0.21–0.38 %) as the AoE half of the acceptance gap — the only boss cells to clear the ±0.1251 pp band. | **THEY DO NOT REPRODUCE, and this was PREDICTED.** ACCEPTANCE's own text ended the thread with *"the boss-side acceptance re-run has NOT been re-gathered under the fix — the 3 surviving boss cells are expected to move, but that is a prediction until a round is run."* Gear-B round 1 is that run: the three columns read **0.07 %**, **0.05 %**, and **not a borrowed-win column at all**, and the whole `isc+scb` KT table now tops out at 0.08 %. PHASE7 §5.19's AoE press-snap fix landed *after* the gear-A measurements, with a predicted blast radius of exactly one preset (KT is the corpus's only `aoe` phase). ⚠ **Re-priced, not closed**: the KT family's magnitude has moved to the *other* kit (`mqg+skull` KT `@0` 0.304 %, `@100` 0.290 %), and the ±0.1251 pp boss-cell band cannot be re-derived from this round because the tool that would price boss cells fails its KT discrimination check (entry above). ★ **A debt that survives re-measurement and a debt that evaporates look identical until the round is run** — which is the whole argument for Phase 10 having existed. | PHASE10 §8.31; ACCEPTANCE gear-B block. |

| 07-27 | **The terminal-cast term was the model's #1 target, and looked tractable.** archive/11 §8.23 diagnosed the worst persistent cell as *one terminal cast the model cannot see*; §8.25 confirmed the mechanism is **shared** by the second-largest column (176→177 and 233→234 casts, both margins ≈⅔ of the cast fraction — a strong out-of-sample agreement). §8.23 closed with a design brief: *"a term that captures the terminal-cast effect **without** replacing the integral."* And the structure made it look easy: `simulate()` already keeps a **discrete board walk** that scoring never reads, beside the **rate integral** that scoring uses — and the integral is only phase-blind inside `[T−KW, T+KW]`, where the taper changes. Replacing it *there only* is strictly weaker than the full discretization already known to fail. | **⛔ CLOSED, IN BOTH FORMS, ON ALL 285 CLASS COLUMNS** (`tools/tail-phase-probe.mjs`, pre-registered). The mechanism is REAL — the two accounts differ by a median **0.1490 %** of `robust`, ~7× the CRN resolution, so P1 passes emphatically. But scoring with the discrete tail is **worse**: mean Pearson r against sim DPS falls **0.9721 → 0.9026**, worse in **256/285** columns, and the ★ falsifier fires hard — it **breaks 48.4 % of the columns the model already gets right** while repairing only 27.7 % of the ones it gets wrong. Net argmax **−39**. The tie-break form (use the tail only where the integral's margin is sub-resolution) is null too: best net **+3 over 285 columns** at ε=0.010 %, and even there it breaks 9.7 % of agreements against a pre-registered bar of ≤5 %, so **P6's bar is met at no ε**. ★★ **The conclusion is sharper than "it didn't work": the phenomenon is real but the model's PREDICTED tail phase is unusable at any threshold.** Its board walk blends cast times by proc probability and cannot know the sim's realized lattice — so the model is not mis-ranking these pairs, and it also cannot be *taught* to rank them by looking at its own tail. What remains untested is a term that makes the objective **hedge** against tail phase rather than predict it. ⚠ **And my own verdict line had this repo's signature defect**: it branched on `net > 0` alone and printed a PASS while the bar was `net > 0 AND broken ≤ 5 %` — the tool's own sweep table had an empty `✓` column the whole time. Sibling of archive/11 §8.30's `mono=0`. | PHASE12 §6.1; `tools/tail-phase-probe.mjs`. |

| 07-27 | **The B2 deficits are a LOW-HASTE basin.** Held since Phase 6 and named in every doc since — the persistence work list is `isc-mqg h40` and `isc-skull h20`, both low-haste, and the debt is literally titled *"the low-haste (≤70) micro-placement slack"*. Mining the round for a directional signal appeared to confirm it outright: the sim prefers a plan built for **higher** haste in 66.1 % of class deficit columns (z = 3.40), and the split by haste looked decisive — `simH ≤ 70` gave 33 higher vs 11 lower, **z = 3.32**. | **⛔ GRID POSITION IS A CONFOUND, AND CONTROLLING FOR IT REVERSES THE PICTURE.** At the *lowest* haste in a kit's grid every rival is higher, so "borrowed from higher" is partly **forced** by where the column sits. Against the correct null — `E[higher] = (#higher rivals)/(#rivals)`, per column — `simH ≤ 70` goes from **z = +3.32 to z = −1.44** (the effect vanishes and if anything inverts), while the significant excess turns out to live at **`simH > 200`: 19 observed against 7.9 expected, z = +4.78**. ★ **So two different facts had been conflated:** *where the length-persistent cells are* (low haste — still true) and *where the model's ranking is systematically biased* (**high** haste — never measured before). Only the first had ever been measured, and the debt was named after it. That makes *"low-haste"* wrong for the **third** time in one day: archive/11 §8.31 had already moved a third persistent column to mid-grid h130 and split the family into two terminal-cast columns plus one value column. **Whatever the debt is, "low-haste" is not a property it reliably has.** ⚠ The follow-on mechanism (the model buying haste the GCD floor eats) has the **right sign and the predicted concentration but is NOT established** — t = 1.71 / 1.41, and its first control was **vacuous by construction** (on agreement columns native *is* the sim's pick, so the statistic read `0.000, t=NaN`, PHASE8 §21.5's defect exactly). | PHASE12 §6.2/§6.3; `tools/floor-overcap-probe.mjs`. |

| 07-27 | **The B2 deficits are a SCORER problem to be fixed with a scorer TERM, and the "one terminal cast" reading of the top cells is the mechanism.** Held all session and inherited from archive/11 §8.23/§8.25. Every attempt today was a candidate term: make the objective see the terminal cast (§6.1), tie-break on it (§6.1), charge for GCD-floor overcap (§6.3). | **⛔ THE PREMISE UNDER ALL OF THEM IS BROKEN: THE MODEL DOES NOT KNOW WHERE ITS CASTS ARE.** Found by taking the user's two prompts seriously — *"shouldn't the effective-casts calculator reward the extra cast?"* and *"we also look at the logs wowsims offers"*. Measured against `SIMLOG` on `isc+scb long T=300 @h0` (both trinkets are SP, so neither perturbs cast timing): the model's cast-start stream is offset from the sim's by a **mean 0.206–0.362 s and a max of 0.963 s — nearly a whole GCD**. ★ **And it corrected a claim I had made an hour earlier in the same session**: I reported that cell as 216 vs 217 casts and called it the terminal-cast mechanism, using the **model's** board count. The **sim casts 217 in BOTH arms** — the counts are *equal* — and it still prefers the rival by 0.1235 %, so that cell is a cast-**value** difference and the "+1" was an artifact of the model's own drift. (§8.25's counts stand; they came from SIMLOG. Mine did not.) ★★ **This one number explains every negative result of the day**: the tail-phase correction breaks 48 % of the columns it touches because it tapers casts placed at the wrong *times*; the fully-discrete "counted" objective — **the objective as CLAUDE.md and MECHANICS §4 actually define it** — ranks *worse* than the integral (r 0.9279 vs 0.9721, worse in 235/285 columns) because it is counting a drifted stream; and the model's characteristic 0.01 % dead ties are what you get when an integral performs *variance reduction on its own timing error*. ⇒ **The implementation deviates from the documented objective and the deviation is an improvement — but only because the stream is inaccurate.** The target is therefore **cast-timing fidelity**, not another term, and it has a cheap gate (`tools/cast-fidelity.mjs`). Separately settled the same run: `tools/deficit-fix.mjs` at **3× restarts** finds **0/4 search misses** (identical scores to four decimals), so the "maybe pooling hides a search miss against the true optimum" loophole is closed too. | PHASE12 §6.5; `tools/cast-fidelity.mjs`, `tools/counted-vs-integrated.mjs`, `tools/deficit-fix.mjs`. |

## Open debts (recorded, to be FIXED in a later phase — not accepted)

> ## ★ RE-PRICED ON GEAR B, 2026-07-27 (PHASE10 §8.31) — read this before any figure below
>
> Every number in this list was gear-A denominated. Round 1 on gear B re-measured the three that
> mattered, and the entries below are **historical from here down** — their *reasoning* stands, their
> *figures* are superseded by the gear-B block in `docs/ACCEPTANCE.md`:
>
> | debt | gear-B verdict |
> |---|---|
> | **B2** | **SURVIVES.** Its cell is the round's worst column: banded **+0.368 ± 0.020 pp, 5/5 seeds, REAL**. Ranking error **≈0.38–0.41 pp** — the ≈0.445 pp target essentially stands. **NOT closed.** |
> | **the low-haste (≤70) micro-placement slack** | **REPRODUCES cell for cell** — so it is a property of the MODEL, not of the reference gear. ⚠ **But it is misnamed twice:** a third persistent column sits mid-grid at **h130**, and the family is not one mechanism — two columns are **one terminal cast** (176→177, 233→234), the third has **equal cast counts** and is a sub-resolution *value* difference (§8.25). Read it as **terminal-cast blindness**, not a low-haste slack. |
> | **the KT / AoE cells** | **DO NOT REPRODUCE** — gear A's three `isc+scb` KT survivors read 0.07 %, 0.05 % and *not a borrowed win at all*, discharging PHASE7 §5.19's standing prediction. ⚠ **Re-priced, not closed**: `mqg+skull` KT @0/@100 now carry the family's magnitude, and the boss-cell noise band is owed a fresh measurement. |
>
> **Unchanged and still open** (structural, not baseline-dependent): the resolution-floor **criterion
> question** (a user call), the absence of exhaustive ground truth above ~h150, and Ashtongue's
> exclusion. **Newly inherited:** `ripple-audit`'s failing P3/P5 self-checks (PHASE10 §8.30).

- **✔ CLOSED 07-25 — `5:40 lust 0:05` SEARCH-MISS.** Root-caused to a **missing seed class** and
  fixed by `groupSeeds` (RULES §4b · ARCHITECTURE · PHASE9 §5.14). Seed-robustness went **2/6 → 6/6**
  with zero regressions elsewhere and `PLAN-DIFF IDENTICAL` on all 25. The eight killed mechanisms are
  preserved in the corrections ledger (rows 234, 237, 239, 241, 242) — **do not re-litigate any.**
- **The low-haste (≤70) micro-placement slack** (~0.1–0.6% DPS, multi-mechanism). The model sits a hair
  under the sim optimum at low gear haste. §4.1 names one component (the IV-into-Lust straddle basin no
  `basinHop` anchor reaches, fix candidate = a half-into-Lust anchor); the cross-val shows the slack is
  broader. **This is a debt to fix, not a state to accept** (user-directed). PHASE6 §4.1/§4.5.
  **↳ NARROWED at round 5 (07-25):** the only two kit-columns in the whole campaign with a *consistent*
  better rival are **`isc-mqg h40`** (rival `plan@h70`, wins 5/5 lengths, margins 0.094 / 0.007 / 0.260 /
  0.101 / 0.073 %) and **`isc-skull h20`** (rival `plan@h100`, 4/5, 0.087 / 0.007 / 0.066 / 0.011 %) —
  both low-haste, which is this debt. ~~Next step is `tools/diagnose-deficit.mjs` on each (SEARCH-MISS vs
  SCORER-GAP)~~ **— that step is DEAD: on a pooled round the SEARCH-MISS bucket is arithmetically empty
  (ledger 07-25 · PHASE7 §5.16a), so the partition cannot route this work.** What the diagnosis *could*
  establish: both columns are **pure retimings** (identical press counts on every track), and `isc-mqg h40`
  is **the mirror of PHASE8's B2** — the same two plans, crossed, so **it is a scorer ranking inversion, not
  a placement search miss, and it belongs to PHASE8 rather than to this debt** (PHASE8 §20.8). The
  magnitude-vs-resolution judgement still stands for the *other* lengths (each column has one at 0.007%,
  inside the ±0.02% ruler); the gradeable cells are `isc-mqg h40` @0.260% and `isc-skull h20` @0.087%.
  PHASE7 §5.15b/§5.16; instrument `tools/xval-persist.mjs`.
  **↳ NARROWED AGAIN, decisively, 07-25 (`tools/ripple-audit.mjs`):** this debt is **not one broad slack**.
  Priced against the tail-lattice ripple floor, **80.2% of the 135 deficit columns are below the
  instrument's own resolution** and the median column is 3.8× below it. The gradeable remainder is the
  **9-cell FLOOR-TAIL family**, whose ripple is provably ~0 and whose haste/length distribution is the
  **mirror image** of the ripple-explained cells (median sim-haste 240 / T 395, vs 110 / 218) — i.e. **this
  debt's "low-haste" framing does not describe what is actually left.** Cleanest target:
  `mqg+skull xl T=395 @265` (deficit 0.090%, floor exactly 0.000%, no wall/AoE channel). DIARY row 273.
  **↳ RETARGETED 07-25 (`tools/floor-plateau.mjs` + `tools/ambient-gap.mjs`): the FLOOR-TAIL family is NOT
  the remainder — that reading was an artifact of ranking by the MASKED quantity.** Measured in the **joint**
  currency (`dModel + pct`, the model's own margin plus the sim deficit = the gap a fix must close),
  FLOOR-TAIL is **1.30× ambient** and *inside* the homogeneity band, while KT-AoE (2.90×), SATURATED (3.29×)
  and RESIDUAL (3.37×) break it. FLOOR-TAIL merely has the smallest **ruler** (its own floor 0.022 pp vs
  `inside`'s 0.139). ⇒ the gradeable remainder is the **15 cells of KT-AoE + SATURATED + RESIDUAL**.
  Also: the ambient joint gap ~0.081 pp is mostly `dModel` ⇒ **the model is routinely ~0.06 pp more confident
  than the sim confirms, corpus-wide** — the scale every remaining family is judged against. DIARY row 274.
  **↳ ⛔ RE-RETARGETED, THEN THE WHOLE FAMILY FRAMING RETIRED 07-25 (`tools/unexplained-gap.mjs`): there is no
  supportable family target list, and the "15 cells" handoff above is WITHDRAWN.** `joint` adds an unbounded
  term to a ripple-bounded one across families whose ceilings differ 9×, so it mostly compares ceilings; the
  corrected `unexplained = dModel + max(0, pct − ripplePct)` gives a **third** ordering and **no family holds
  its rank** (SATURATED falls to *last, below ambient* — **vindicated, not re-derived**; FLOOR-TAIL returns to
  2nd). A seeded 20 000-resample bootstrap tops the nominal worst family at only **60.8 %** ⇒ the ~0.1 pp
  between-family differences are the size of the instrument's own per-cell ceiling (corpus median 0.134 pp) and the ranking
  is **noise-dominated**. The `~0.06 pp` figure above is also wrong — it subtracted two medians; measured
  directly it is **0.040 pp**. ⇒ the remaining unit of work is a **cell with a fresh sim duel**, or more
  columns. DIARY row 275.
- **✔ WITHDRAWN 07-25 — "haste covering the opening ramp is under-credited by ~+0.079 pp" was NOT a model
  defect.** `tools/ramp-marginal.mjs` shows the model's ramp credit is *exactly floor-slack recovery*:
  **0.0000 pp floor-free** (provably correct — the coverage and interior cast counts are the same algebraic
  expression) and **+0.3298…+0.4077 pp once Lust drives the steady cast into the 1.0 s floor**, with
  Berserking under the same Lust at exactly 0.0000 as the within-regime control and a hand derivation
  agreeing (+0.296 vs +0.27 casts). PHASE8 §15.5 F5's flat reading came from a **single-buff, hence
  floor-free** sweep — the one regime where 0.000 is the right answer. **`index.html:919-931` is correct as
  written; the patch is cancelled.** The sim's +0.079 pp is a residual with **no identified mechanism** ⇒ a
  *sim-setup audit trigger*, not a scorer change (PHASE8 §7). Ledger row above; RULES §3; PHASE7 §5.16c.
  **↳ And the cell this debt had claimed as its "largest consequence" is re-attributed.**
  `isc-skull short T=99 lust=49 @sim40 ← plan@70`, sim **0.362%** vs model **0.006%**, one track
  (`IV[8,28]` vs `[0,20]`): **both layouts are floor-free at the opening**, so no ramp physics is in play and
  the model's 0.006% is correct. It is the **tail-lattice ripple** (RULES §8) — the sim counts integer casts
  under the kill taper, the model integrates the continuum limit, residual `1 − W/c` casts (0.3164 at this
  fight's tail rate). Native's last cast completes at **99.6216 s, past `T+KW = 99.5`** — weight 0, wholly
  wasted; the rival's at 99.3041 s, and it fits one more. **The disputed 0.36% is which side of the kill
  window the last cast lands on.** ⚠ **Not fixable by discretizing the scorer** — the full-column control has
  the discrete sum at `r = 0.7910` / RMSE 0.2948 vs the integral's `r = 0.9337` / RMSE 0.2431
  (`tools/lattice-ripple.mjs`). ⚠ **The corpus-wide first-haste-window asymmetry** (33 earlier vs 18 later,
  z≈2.10, p≈0.04) **loses its proposed mechanism** and drops back to an unexplained post-hoc correlation —
  it must not be cited as ramp evidence.
- **★★ NEW 07-25 — the acceptance metric has a positive-biased resolution floor, and the criterion needs a
  user decision.** `diagWorst` maxes the two-signed tail ripple over ~10 rival rows, so its expectation is
  **positive for a flawless model**: +0.094…+0.165% at R=10 on an 81-cast fight, scaling as `1/N` (⇒
  short-fight/low-haste only). So *"no length-persistent diagonal deficit"* is **not reachable by
  construction** on those tables, and a sub-floor deficit is not evidence of a defect. Compounds the
  model-side limit already on the books (58/135 round-5 columns priced ≤0.02% apart, under the model's *own*
  resolution) — the two together say the **ruler**, not the model, is what binds on short/low-haste tables.
  **The restatement is a user call and is NOT being made unilaterally.** ⚠ Do not "fix" it by widening
  `KILL_WINDOW` — that is the half-cast hedge of RULES §8, i.e. the objective itself. ACCEPTANCE coverage gaps.
- **No exhaustive ground truth above ~h150** for the SP-trinket-free kits (the 5s grid can't express the
  off-grid optimum). PHASE6 §4.2.
- **✔ CLOSED at task #53 — ~~KT AoE simmed as downtime~~.** `genapl` emits Arcane Explosion per `_aoe`
  window and the runner gets `--targets N` (`tools/xval.mjs:133,161,177,265`); round-5 KT columns are
  AoE-**valued**. Stale copies of the old claim survived in several tool headers long after it was false —
  when a doc and the code disagree about what a harness emits, **read the code**. *Original entry:*
  genapl needs Arcane-Explosion emission to value KT's AoE window. PHASE6 §7.
- **Ashtongue** is out of the cross-val kits (a random proc, needs different treatment). Phase 7. PHASE6 §4.8.
- **✔ CLOSED 07-25 — the xval harness's two wrong model inputs.** Fixed via `tools/reference-gear.mjs`
  (`t5two: true`, effective `sp: 1450`) and the whole campaign re-gathered as **round 5**. The correction
  moved **124/345 plan cells (35.9%)** and **zero verdicts** — so it was a real repricing *and* not the
  cause of the diagonal deficit. The pre-flight's "rank-neutral, plans won't move" claim is withdrawn.
  PHASE7 §5.15a; ACCEPTANCE "Current status (round 5)". *Original entry, kept for the reasoning:*
- **~~The xval harness has TWO wrong model inputs.~~** (a) It omits `cfg.t5two` while the reference export
  wears Tirisfal 2pc — small on AB-only columns (nearly rank-preserving), largest on AoE/KT columns
  (AB-vs-AE ratio off ×1.2). (b) It passes `sp: 1387` where the export's **effective** SP is ≈**1450**
  (Tirisfal 4pc, `SpellID: 37444`, +70 on crit at 88–94% flat-in-haste uptime). Set `t5two: true` **and**
  `sp: 1450` at `xval.mjs:111` + `xval-model.mjs:53` at the START of the next round and re-baseline —
  **never mid-round.** Rank-neutrality on the B2 pair already checked (all variants pick h70), so gathered
  rounds stay valid. PHASE8 §6/§7; TOOLING.

## The planned road ahead (as understood at 07-23)
Per user: **next phase = FIX** the recorded debts (starting with the low-haste basin), **then** likely
**upgrades**, **then another round of the acceptance test** (`docs/ACCEPTANCE.md`), and repeat. Each round
appends here; the phase docs are kept as the detailed per-round record.
